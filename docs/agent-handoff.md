# HOPI Agent Handoff

Status date: 2026-06-04

This document is the exhaustive handoff ledger for an agent with no prior chat context.

Read [docs/zero-context-continuation.md](/Users/realizer/Code/hopi-claude/docs/zero-context-continuation.md) first if you need the curated start-here guide. This file remains the exhaustive ledger.

## Current State

HOPI is being rebuilt as a Bun-first, file-native autonomous goal orchestration system.

Phase 1 backend is complete:

- The disposable prototype backend was replaced with a deterministic Bun core.
- The backend reads and mutates file-native goal boards.
- A single-step scheduler advances one deterministic unit per call.
- Goal-scoped runtime run/step/message history is now persisted under `.hopi/runtime/**`.
- The runner boundary now streams typed runtime events into step history.
- A real `ProcessAgentRunner` and git `WorktreeManager` now exist behind that runner boundary.
- Goal-scoped durable `write-trace.jsonl` recording now exists for process-backed file writes.
- Configured planner / generator / reviewer / merger process adapters now exist through repo-local adapter config and typed outcome files.
- Built-in vendor transport adapters now exist for Codex, Claude Code, and OpenCode, backed by durable per-step `prompt.md` bundles.
- Built-in vendor transports now normalize machine-readable CLI output into structured step transcripts instead of storing raw vendor JSON lines.
- Deterministic merger execution now performs real git merge completion and settled-run cleanup.
- Durable write traces are now queryable, injected into relevant role context bundles, and surfaced through the Bun API and UI.
- Reviewer and merger prompts now apply explicit write-trace evidence policy, including engineering evidence-gap guidance when no durable traces exist.
- The first Goal assistant substrate slice is now implemented with durable `decisions.yml`, repo `preference.md`, Goal-scoped `assistant-thread.json`, planner-context plumbing for decisions/preferences, and minimum Bun API routes for those stores.
- Live Goal assistant execution is now implemented with an explicit Goal-scoped runtime call, constrained durable actions, assistant run bundles under `.hopi/runtime/**`, and scheduler cleanup for resolved decision blockers.
- Goal assistant inspection APIs and Bun UI surfacing are now implemented for assistant prompts, decision/thread viewing, assistant run summaries, and assistant run detail inspection, including surfaced resolved answer-interpretation formats on assistant action results.
- Exact assistant bundle inspection is now implemented on the Bun product path for `context.md`, `prompt.md`, `outcome.json`, and `result.json`.
- Repo preference editing is now implemented on the active Bun API/UI path, and assistant now supports structured `request_planning` and durable preference lifecycle actions.
- Assistant can now explicitly request decision topics, and the Bun product path now supports direct decision creation and resolution with visible blocker linking.
- Frontend direction changed on 2026-06-06: `packages/frontend` is the product frontend package again. The existing React/Vite code in that package is the baseline to preserve and evolve in place. The backend-served UI under `packages/backend/src/ui` remains useful as a capability/reference surface, but product frontend work should stay in `packages/frontend` instead of creating or extending another frontend tree.
- Durable decision topics now also support optional exact `prompt` text, so assistant/API/UI can preserve the canonical user-facing question directly in `decisions.yml` instead of relying only on short summaries or thread history.
- Durable planning-request answers now also support optional exact `prompt` text, and shared answer interpretation now reuses those planner prompts as matching authority across question/topic reply surfaces when the shorter planner summary is not repeated verbatim.
- Answer-driven `question_blocks`, `question_clauses`, `question_spans`, `question_middle_spans`, `question_closing_spans`, `question_closing_blocks`, and `question_middle_blocks` now also match current durable decisions by exact stored `prompt` text when available, so question-based replies no longer need to repeat topic labels inside the question itself.
- Answer-driven `question_blocks`, `question_clauses`, `question_spans`, `question_middle_spans`, `question_closing_spans`, `question_closing_blocks`, and `question_middle_blocks` now also match current durable decisions by deterministic stored `prompt` core text, so question-based replies can restate the canonical durable question more tersely without falling back to fuzzy topic inference.
- Answer-driven `question_blocks`, `question_clauses`, `question_spans`, `question_middle_spans`, `question_closing_spans`, `question_closing_blocks`, and `question_middle_blocks` now also match current durable decisions by deterministic stored `prompt` keyword anchors, so question-based replies can reorder the canonical durable question’s meaningful words without requiring exact prompt-core containment or fuzzy NLP.
- Durable decisions and durable planner answers now also support explicit `matchHints`, so assistant/API can persist stable product-approved phrases as deterministic answer-interpretation authority when later replies should not depend on exact prompt reuse or ever-looser parser heuristics.
- Durable decisions and durable planner answers now also support explicit `summaryKey`, so assistant/API can persist one stable noun-phrase key as deterministic matching authority when later reusable answer sources should target a known consumer even if the visible summary or prompt wording drifts.
- Durable planner answers now also support explicit `answerKey`, so assistant/API can persist one stable row identity for the same captured planner-answer slot even when later writes change the answer text, and reusable `matching_answer_sources` can target that known planner answer without relying on summary wording or the previous answer payload.
- Brand-new durable decision topics and brand-new planner captured answers inferred from topic-shaped source responses now also get one synthesized canonical `prompt`, so later question-based replies can reuse durable question authority even when the original capture did not come from an explicit question surface.
- Durable decisions and durable planner answers created from stable summary-only payloads now also synthesize one canonical `prompt` automatically, and later explicit prompt input upgrades that synthesized default instead of getting stuck behind weaker summary-derived wording.
- Decision resolution now clears linked visible blockers immediately, and the Bun UI now exposes an explicit `Reconcile Once` control for one deterministic scheduler step.
- Resolving a decision that was blocking engineering work now creates or reuses visible planner follow-through, rewires engineering blockers onto that planning task, and lets richer later planning requests upgrade the generic follow-through instead of duplicating it.
- Goal docs are now inspectable through the Bun API/UI with deterministic `bootstrapped` versus `curated` status, and planner prompts now apply explicit doc-status follow-through policy for durable `design.md`.
- Durable `planning-requests.yml` now exists as the planner follow-through input surface: assistant and API can open file-native planning requests linked to visible planning tasks, planner context consumes them, the Bun UI surfaces them, and planning task completion auto-resolves linked requests deterministically.
- Durable planning requests now also carry decision lineage plus explicit `design.md` / `todo.yml` update targets, and reused open requests preserve newer follow-through metadata instead of dropping it.
- Durable planning requests now also support explicit `goal.md` update targets, and planner follow-through evidence/policy now treats `goal.md`, `design.md`, and `todo.yml` as one inspectable requested-update contract.
- Planning requests now also support validated Goal-local relative requested-update paths beyond the built-in `goal.md` / `design.md` / `todo.yml` trio, and assistant/API/UI/planner evidence all share that same normalized path model.
- Engineering-linked decision resolution can now carry one explicit single or grouped planner follow-through directly through the shared decision-resolution path, and grouped engineering blockers rewire to the current grouped sink tasks instead of relying on a later separate planning action.
- Planning-linked decision resolution can now reuse the current planning task as the explicit follow-through surface, including grouped staged follow-through without introducing a duplicate wrapper planning task.
- Explicit decision resolution can now also create standalone visible planner follow-through before there is a reusable planning surface or affected engineering blocker, while preserving the current default no-follow-through behavior when no explicit follow-through is supplied.
- One durable decision answer can now also fan out into multiple independent visible planner workflows through higher-order `workflow_batch` follow-through, while still using only `decisions.yml` plus `planning-requests.yml` as durable truth.
- One user answer can now also resolve multiple durable decision topics at once and route those resolved topics through one shared visible planner follow-through, still using only `decisions.yml` plus `planning-requests.yml` as durable truth.
- Planning requests can now also carry explicit captured user answers when planning should react to durable answer context that does not map cleanly to decision topics first, and planner context now surfaces those captured answers alongside decision lineage and requested updates.
- Direct planning assistant and Bun API surfaces now also share the same interpreted planner-answer authority as decision follow-through, including `sourceResponse`, reusable `answerSources`, structured `sourceResponseFormat`, root `inferRemainingAnswers`, and workflow-root shared planner answers, so planner answers can materialize directly onto durable planning requests and workflow graphs without routing through a decision action first.
- Decision-answer follow-through can now also carry explicit non-decision captured answers on `planning`, `planning_batch`, and `workflow_batch` leafs, so one action can atomically resolve real decision topics and write the remaining durable answer context straight onto planning requests.
- Assistant and API can now also atomically open more than one independent durable planning workflow directly on the planning surface through shared higher-order `workflow_batch` planning requests, without routing that work through a decision-answer action first.
- Direct higher-order planning workflows can now also reuse one current visible planning surface as their first child workflow, so expanding an existing planning task into a richer workflow batch does not require a wrapper task.
- When direct higher-order planning workflows reuse a planning surface that is already blocking engineering, runtime now fans that engineering blocker out to every current workflow sink instead of only the first child workflow.
- Direct higher-order planning workflows now also support stable `workflowKey`, so later assistant or API calls can extend one durable direct workflow batch and still get full workflow-state reconstruction plus workflow-wide blocker retargeting.
- Standalone children inside direct higher-order planning workflows now also support stable `workflowTaskKey`, so later assistant or API calls can update one durable child in place without relying on low-level request ids or title collisions.
- Direct higher-order planning workflows now also support stable child-to-child `blockedByWorkflowKeys`, so later children can wait on earlier child sinks and keep rewiring as those upstream child sinks extend.
- Direct higher-order planning workflows now also support workflow-root shared `decisionRefs` and captured `answers`, so one durable workflow graph can carry common planning context without repeating it on every child.
- Decision-backed and answer-backed `workflow_batch` follow-through now also reuses that same durable direct-workflow graph surface, including `workflowKey`, `workflowTaskKey`, and `blockedByWorkflowKeys`, instead of maintaining a second weaker multi-workflow path.
- Decision-backed and answer-backed `workflow_batch` follow-through now also supports workflow-root shared captured `answers`, so one answer-driven workflow graph can carry common non-decision answer context without repeating it on every child.
- Workflow-root shared context is now durable across later `workflowKey` extensions by persisting shared decision lineage and shared answers on workflow-linked planning requests, so new children inherit the same workflow-root baseline even when later calls omit it.
- Direct and decision-backed workflow graphs can now also reuse one already-open grouped planning surface as their first child through stable `groupKey` authority, so runtime adopts the whole open group instead of forcing callers to replay grouped siblings just to preserve workflow membership.
- Higher-order workflow graphs now also get a generated durable top-level `W-*` identity by default when callers omit `workflowKey`, so direct and decision-backed `workflow_batch` flows inherit extension authority, blocker tracking, and shared-context persistence without requiring hand-crafted workflow keys.
- Decision `resolve`, `answer`, and `answers` Bun API routes now also return the full shared runtime result, so callers can observe `blockerRemoved`, creation metadata, full `followThrough`, and any runtime-generated `W-*` workflow key instead of losing that authority behind decision-only response bodies.
- Goal assistant `resolve_decision`, `record_answer`, and `record_answers` action results now also return the full shared decision-runtime follow-through shape, so assistant-run responses, persisted run detail, and the Bun UI no longer flatten workflow authority down to ad hoc `followThrough*Keys` summary fields.
- Assistant action-result and thread inspection now also surface the actual captured planner answers and workflow-shared planner answers that live on durable planning-request payloads, and Bun planning-request cards now also show workflow-shared decision lineage plus workflow-shared answers instead of collapsing that authority down to counts or hiding it behind workflow-only views.
- Assistant action inspection now also surfaces the actual planner-answer slots carried directly on planning actions and decision follow-through payloads, so thread/run-detail readers no longer have to infer real planner-answer authority from count-only lines like `Shared planner answers: 1`.
- Answer-driven assistant and Bun API actions now also support one shared `sourceResponse`, so a less-structured raw user reply can be captured once and reused across multiple durable decision topics plus non-decision follow-through answers instead of repeating the same answer payload in every entry.
- Answer-driven assistant and Bun API actions now also support root `answerSources` plus per-item `answerSourceKey`, so one less-structured raw reply can first be explicitly extracted into reusable topic-specific snippets and then mapped across multiple durable decision topics plus non-decision follow-through answers without repeating snippet text or collapsing everything to one shared raw response.
- Answer-driven assistant and Bun API actions now also support excerpt-grounded `answerSources` through `sourceExcerpt`, so reusable extracted snippets can be lifted directly from one shared raw reply with deterministic validation instead of being retyped into the action payload.
- Answer-driven assistant and Bun API actions now also support direct item-level `sourceExcerpt`, so one decision answer or planner follow-through answer can be grounded in a shared raw reply without first introducing a one-off named answer-source bundle.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "labeled_sections"`, so one structured raw reply can automatically materialize multiple durable decision answers and planner follow-through answers without per-topic excerpts or named answer-source bundles.
- Answer-driven `record_answers` assistant and Bun API surfaces now also support `inferOpenDecisions` on top of labeled shared replies, so matching current open durable decisions can be resolved without repeating those same decision topics inside the action payload.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "ordered_items"`, so one ordered list-style shared reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers without labels, excerpts, or per-topic mapping.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "ordered_blocks"`, so one ordered multi-paragraph shared reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers without labels or per-topic mapping, while still preserving more than one paragraph per answer block.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_blocks"`, so one question-and-answer style shared reply can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics even when the answer blocks themselves no longer repeat the topic name.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_clauses"`, so one longer sentence with more than one self-contained natural question-and-answer clause can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics without requiring sentence or paragraph boundaries between clauses.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "single_pending"`, so when current Goal state leaves exactly one unresolved decision answer slot or exactly one unresolved explicit planner-answer slot, one less-structured shared reply can materialize directly onto that one pending consumer without repeating question/topic anchors or ordered structure, and runtime deterministically rejects ambiguity if more than one pending consumer remains.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "pending_clauses"`, so one less-structured shared reply can deterministically feed more than one already-known pending decision or planner-answer consumer in current pending order without repeating question/topic anchors, labels, or ordered-list markers, as long as the reply naturally separates those answers into clause-level segments.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "pending_paragraphs"`, so one less-structured shared reply can deterministically feed more than one already-known pending decision or planner-answer consumer in current pending order without repeating question/topic anchors, labels, ordered-list markers, or clause splitting, as long as the reply naturally separates those answers into paragraph-level segments.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "pending_sentences"`, so one less-structured shared reply can deterministically feed more than one already-known pending decision or planner-answer consumer in current pending order without repeating question/topic anchors, labels, ordered-list markers, clause splitting, or paragraph splitting, as long as the reply naturally separates those answers into sentence-level segments.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "pending_conjunctions"`, so one less-structured shared reply can deterministically feed more than one already-known pending decision or planner-answer consumer in current pending order without repeating question/topic anchors, labels, ordered-list markers, sentence splitting, clause splitting, or paragraph splitting, as long as the reply naturally separates those answers with explicit conjunction connectors inside one sentence.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "pending_answer_sources"`, so one less-structured reply can first be lifted into an ordered reusable `answerSources` bundle and then deterministically feed more than one already-known pending decision or planner-answer consumer in current pending order without repeating question/topic anchors or mapping each snippet to a specific topic.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "matching_answer_sources"`, so one explicit reusable `answerSources` bundle can deterministically match more than one already-known pending decision or planner-answer consumer by source labels and hints instead of current pending order, without repeating question/topic anchors or writing per-topic `answerSourceKey` mappings on each consumer.
- Answer-driven assistant and Bun API actions now also support inferring remaining brand-new decision topics and remaining planner-answer summaries from leftover `pending_answer_sources` or `matching_answer_sources` entries, as long as those remaining reusable sources already carry explicit `summary` authority, explicit `summaryKey` authority, one stable `prompt` authority, exactly one stable `matchHint` authority, or one stable suffixed `answerSourceKey` authority like `launch-shape-answer`. Canonical noun-phrase prompts like `What should the pilot scope be?`, explicit question-shaped prompts like `How should rollout happen?`, explicit keys like `summaryKey: "launch-shape"`, one stable phrase hint like `launch shape`, and one noun-phrase key with an explicit `-answer` / `-source` suffix now all work. Multiple hints still fail closed, and generic unsuffixed `answerSourceKey` values still fail closed, so runtime does not have to guess.
- Leftover `pending_answer_sources` entries now also merge adjacent reusable source entries into one brand-new durable decision topic or one inferred planner-answer row when those leftovers already share explicit key-based authority like `decisionKey`, `answerKey`, or `summaryKey`, while still failing closed on non-contiguous repeats instead of silently splitting one durable topic or row across multiple outputs.
- Answer-driven assistant and Bun API actions can now also combine `inferDecisionTopics` with `followThrough.inferRemainingAnswers`, but only on `pending_answer_sources` or `matching_answer_sources` when every leftover reusable source entry is explicitly routed by durable `decisionKey` or durable `answerKey`. In that mixed route mode, runtime first consumes known decisions and explicit planner-answer slots, then grows brand-new decision topics from the decision-routed leftovers and inferred planner-answer rows from the planner-routed leftovers, while still failing closed on non-contiguous repeats or weaker leftover authorities like bare `summary`, `summaryKey`, `prompt`, `matchHints`, or suffix-only `answerSourceKey`.
- Explicit reusable `answerSources[*].route` is now also a hard family boundary for known consumer matching: `matching_answer_sources` only lets decision consumers see `route: "decision"` or unscoped entries and only lets planner consumers see `route: "planning"` or unscoped entries, while `pending_answer_sources` now fails closed when the next ordered entry is explicitly routed to the wrong family instead of silently consuming it onto the current decision/planner slot.
- Explicit reusable `answerSources[*].route` now also fails closed when a decision-only or planning-only mutation leaves a routed reusable source entry unreachable after materialization, so route-scoped answer sources cannot be silently ignored when the current surface has no valid consumer on that family.
- Repeated exact `sourceExcerpt` grounding now also requires explicit `sourceOccurrence` authority when the same excerpt text appears more than once inside one shared `sourceResponse`, so direct item excerpts and reusable answer-source excerpts no longer silently default to the first repeated match.
- Explicit decision-answer and planner-answer items can now also consume one already-merged reusable answer-source group through `answerSourceGroupKey`, so grouped reusable `answerSources` no longer have to be remapped back through a single-fragment `answerSourceKey` or repeated inline text when one consumer should reuse the full grouped materialization directly.
- Answer-driven and direct planning assistant/API surfaces now also support `sourceResponseFormat: "auto"`, so callers can let shared runtime deterministically choose the strongest successful existing interpretation surface by fixed authority priority instead of hard-coding `question_spans`, `topic_paragraphs`, `matching_answer_sources`, or one weaker pending-order fallback up front; `auto` now also rejects partially successful unit-based candidates that leave their own ordered/question/topic/pending/matching-run units unconsumed, explicit multi-label `labeled_sections` / `inline_topics` authority now fails closed instead of falling through to weaker later reinterpretation surfaces once those labels have been established but left incomplete, explicit reusable `answerSources` authority now also fails closed before weaker raw-reply surfaces can take over once the answer-source family has already been tried and still remains incomplete, and explicit `question_*` / `topic_*` anchor surfaces now fail closed only after they have already matched at least one consumer-specific unit and still remain incomplete, so generic paragraphs or sentences can still fall through to ordered or pending surfaces when no real anchor authority was ever established. Decision/direct-planning API responses and assistant action results now also surface `resolvedSourceResponseFormat`, and the same concrete deterministic provenance now also persists durably as `captureFormat` on resolved decisions plus materialized planner-answer rows, so later inspection of `decisions.yml` and `planning-requests.yml` can still see which deterministic surface produced the current answer after the immediate mutation response is gone.
- Assistant run detail inspection in the Bun UI now also surfaces `resolvedSourceResponseFormat` directly on each interpreted assistant action result, so the durable assistant-run inspection path can show which deterministic interpretation surface materialized a captured answer without requiring users to open raw `result.json`.
- Assistant run detail inspection in the Bun UI now also surfaces structured assistant `action` authority alongside `action_result`, so run readers can inspect the requested mutation shape itself, including reusable-source counts, inferred-answer flags, workflow reuse metadata, and linked durable decision/planning context without opening raw bundle files.
- Assistant thread inspection, bundled assistant context, and Bun assistant-run detail inspection now also surface richer structured `action_result` authority like durable request/task ids, workflow/group ids, preference ids, and grouped result metadata instead of leaving those ids trapped inside raw structured result payloads.
- Assistant `request_decision`, `record_answer`, `record_answers`, and `resolve_decision` action results now also surface the durable decision payload itself when available, so assistant-run results, thread history, and Bun inspection no longer collapse decision mutations down to summary text plus keys while dropping canonical prompts, match hints, capture format, and exact resolved answer text.
- Assistant `request_planning`, `request_planning_batch`, `request_planning_workflows`, and decision follow-through results now also surface the durable planning-request payload itself when available, so assistant-run results, thread history, and Bun inspection no longer collapse planning mutations down to request/task ids while dropping persisted titles, linked `decisionRefs`, captured answers, workflow-shared answers, and requested update targets.
- Assistant thread inspection, bundled assistant context, and Bun assistant-run detail inspection now also surface child-level workflow result authority for `request_planning_workflows` and decision follow-through `workflow_batch`, so persisted `workflowTaskKey` / `groupKey` children no longer collapse down to top-level workflow counts and aggregate request/task ids only.
- Bun assistant-run detail inspection now also surfaces richer structured runtime-event authority like transcript tool names/keys/vendor event types, worktree branch/base metadata, artifact refs, and message levels instead of flattening runtime events down to one summary line.
- Assistant-thread history now also persists structured `action` and `action_result` authority instead of only lossy `actionType + summary`, and both Bun thread inspection plus bundled assistant context now reuse that same structured payload to surface requested mutation details, resolved interpretation formats, follow-through metadata, reusable-answer-source counts, inferred-answer flags, workflow reuse keys, linked decision refs, and other durable mutation authority without opening raw run records.
- Planner role context, Bun planning workflow inspection, Bun planning request inspection, and Bun decision inspection now also surface that durable interpreted-answer provenance instead of treating captured answers as summary-plus-text only: parsed planner context now shows decision `captureFormat` plus planner-answer `captureFormat`, Bun decision cards show resolved answer capture format, and Bun planning request / workflow summaries now keep the same exact `captureFormat` evidence visible on captured and workflow-shared answers.
- Planner role context, Bun decision inspection, Bun planning request inspection, and Bun workflow inspection now also surface the rest of the durable answer-matching authority alongside `captureFormat`: decisions show `summaryKey` and `matchHints`, while captured planner answers and workflow-shared answers now keep `summaryKey`, `answerKey`, `matchHints`, and `captureFormat` visible instead of flattening everything to prompt-plus-text.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "matching_runs"`, so one shared reply that repeatedly revisits the same already-known decision or explicit planner-answer consumer across contiguous paragraphs, sentences, clauses, or one whole reply can deterministically merge those repeated mentions into one answer run instead of over-splitting them into duplicate narrower items. That surface now also fails closed on orphan prose before the first matched run, between different matched consumers, or after the last matched run, while still allowing continuation prose to stay attached when the next matched unit belongs to the same consumer. This surface currently stays narrow on purpose: explicit answer consumers plus `inferOpenDecisions` only, and `auto` may use it as a later generic fallback when stronger existing surfaces fail, but it does not broaden `inferDecisionTopics` or `followThrough.inferRemainingAnswers`.
- Answer-driven assistant and Bun API actions now also let `matching_opening_runs` split one continuous less-structured sentence by embedded known-consumer opening anchors when each matched consumer appears exactly once and starts its own answer stretch, so callers no longer need explicit sentence, clause, or paragraph boundaries just to reuse opening-anchor authority. The embedded path stays fail-closed on repeated same-consumer anchors, overlapping different-consumer anchors, leading prose before the first anchor, or missing trailing prose after an anchor.
- Answer-driven assistant and Bun API actions now also let `matching_closing_runs` split one continuous less-structured sentence by embedded known-consumer closing anchors when each matched consumer appears exactly once and ends its own answer stretch, so callers no longer need explicit sentence, clause, or paragraph boundaries just to reuse closing-anchor authority. The embedded path stays fail-closed on repeated same-consumer anchors, overlapping different-consumer anchors, missing leading prose before an anchor, or trailing prose after the last anchor.
- Answer-driven assistant and Bun API actions now also let `matching_middle_runs` split one continuous less-structured sentence by embedded known-consumer middle anchors when each matched consumer appears exactly once and keeps both leading and trailing continuation around that anchor, so callers no longer need explicit sentence, clause, or paragraph boundaries just to reuse middle-anchor authority. The embedded path stays fail-closed on repeated same-consumer anchors, overlapping different-consumer anchors, missing leading prose before the first anchor, missing trailing prose after the last anchor, or too little gap between anchors to preserve both a trailing and leading continuation token.
- `sourceResponseFormat: "auto"` now also fails closed once `matching_middle_runs` has already consumed one explicit known-consumer run and then runs out of later matched consumers, so partially matched `matchHints` authority cannot silently degrade into weaker pending-order reinterpretation like `pending_clauses`.
- Answer-driven assistant and Bun API actions now also let `sourceResponseFormat: "auto"` probe `matching_middle_runs` as a later deterministic generic-run fallback, so middle-anchored less-structured replies that do not establish stronger explicit topic or question authority no longer fail before runtime reaches the already-implemented middle-run surface.
- Goal-assistant action guidance now also explicitly exposes `matching_opening_runs`, `matching_closing_runs`, and `matching_middle_runs` as concrete `sourceResponseFormat` choices, including when to prefer each anchored less-structured reply surface over plain `matching_runs`, and `inferOpenDecisions` fail-closed validation text now truthfully lists `matching_middle_runs` alongside the other anchored run formats instead of under-reporting accepted runtime authority.
- Answer-driven assistant and Bun API actions now also let `topic_closing_spans` split one continuous less-structured sentence by embedded inferable or already-known topic-closing anchors when each answer stretch ends with exactly one non-overlapping closing topic label, so explicit decision answers, `inferDecisionTopics`, `followThrough.inferRemainingAnswers`, and `sourceResponseFormat: "auto"` no longer need separate sentence boundaries just to preserve closing-topic authority. The embedded path stays fail-closed on repeated same-topic labels, overlapping different-topic labels, missing leading prose before a closing anchor, no new leading prose between adjacent embedded closing anchors, or trailing prose after the last closing anchor.
- Answer-driven assistant and Bun API actions now also let `topic_spans` split one continuous less-structured sentence by embedded inferable or already-known opening topic anchors when each answer stretch starts with exactly one non-overlapping topic label, so explicit decision answers, `inferDecisionTopics`, and `sourceResponseFormat: "auto"` no longer need separate sentence boundaries just to preserve opening-topic authority. The embedded path stays fail-closed on repeated same-topic labels, overlapping different-topic labels, a reply that does not start with the first opening topic anchor, or an anchor that leaves no answer text before the next topic anchor or the end of the reply.
- Answer-driven assistant and Bun API actions now also let `topic_middle_spans` split one continuous less-structured sentence by embedded inferable or already-known middle topic anchors when each answer stretch keeps exactly one non-overlapping topic label in the middle, so explicit decision answers, `inferDecisionTopics`, and `sourceResponseFormat: "auto"` no longer need separate sentence boundaries just to preserve middle-topic authority. The embedded path stays fail-closed on repeated same-topic labels, overlapping different-topic labels, missing leading prose before the first middle anchor, missing trailing prose after the last middle anchor, or too little gap between adjacent anchors to preserve both a trailing and leading continuation token.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_spans"`, so one inline question-and-answer style shared reply can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics even when the answer sentences themselves no longer repeat the topic name or sit in separate question paragraphs.
- Answer-driven assistant and Bun API actions now also let `question_spans` split one continuous less-structured sentence by embedded known question anchors when each answer stretch starts with exactly one non-overlapping known question anchor, so explicit decision answers, inferred current open decisions, and `sourceResponseFormat: "auto"` no longer need separate question-sentence boundaries just to preserve question-opening authority. The embedded path stays fail-closed on repeated same-question anchors, overlapping different-question anchors, a reply that does not start with the first matched question anchor, or a matched anchor that leaves no answer text before the next matched question anchor or the end of the reply.
- Answer-driven assistant and Bun API actions now also let `question_middle_spans` split one continuous less-structured sentence by embedded known question anchors when each answer stretch keeps exactly one non-overlapping known question anchor in the middle, so explicit decision answers, inferred current open decisions, and `sourceResponseFormat: "auto"` no longer need separate question-sentence boundaries just to preserve question-middle authority. The embedded path stays fail-closed on repeated same-question anchors, overlapping different-question anchors, missing leading prose before the first matched question anchor, too little gap between adjacent anchors to preserve both trailing text for the current span and leading text for the next span, or no trailing answer text after the last matched question anchor.
- Answer-driven assistant and Bun API actions now also let `question_closing_spans` split one continuous less-structured sentence by embedded known question anchors when each answer stretch ends with exactly one non-overlapping known question anchor, so explicit decision answers, inferred current open decisions, and `sourceResponseFormat: "auto"` no longer need separate question-sentence boundaries just to preserve question-closing authority. The embedded path stays fail-closed on repeated same-question anchors, overlapping different-question anchors, missing leading prose before the first matched question anchor, no new leading prose between adjacent matched question anchors, or trailing answer text after the last matched question anchor.
- Answer-driven assistant and Bun API actions now also let embedded `question_spans`, `question_middle_spans`, and `question_closing_spans` infer brand-new durable decision topics or remaining planner answers from one continuous less-structured sentence when each answer stretch uses one canonical `What should <summary> be` question anchor, even if no already-known question candidate exists yet. Runtime reconstructs the canonical prompt from that anchor, derives the durable summary from its noun phrase instead of flattening the whole question text into the summary, and stays fail-closed on repeated same-question anchors, overlapping anchors, or shapes that do not fit that narrow canonical question form.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_middle_spans"` and `sourceResponseFormat: "question_middle_blocks"`, so one question-driven reply can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics even when each answer keeps the question sentence or paragraph in the middle, with at least one leading and trailing answer unit around it, and adjacent spans or blocks are split by letting the unit immediately before the next question anchor become the leading unit of the next answer.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_closing_spans"`, so one inline answer-first shared reply can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics even when the question sentence appears after the answer sentences that it closes.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "question_closing_blocks"`, so one answer-first multi-paragraph shared reply can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics even when the question paragraph appears after the answer paragraphs that it closes.
- Answer-driven `record_answers` assistant and Bun API surfaces now also support `inferDecisionTopics` on top of labeled shared replies, so remaining labeled sections can become new durable decision topics even when there is no preexisting open decision surface or explicit `answers[]` mapping.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "inline_topics"`, so one less-structured natural-language reply with inline topic clauses can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics without line-based labeled sections or ordered-list structure.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_clauses"`, so one less-structured natural-language reply with more than one self-contained natural topic clause inside one longer sentence can deterministically feed explicit decision answers, inferred current open decisions, planner follow-through answers, and remaining inferred decision topics without requiring sentence or paragraph boundaries.
- Answer-driven assistant and Bun API actions now also let `topic_clauses` split one continuous less-structured sentence on explicit conjunction connectors when each conjunction-linked segment still carries exactly one explicit or inferable topic, so explicit decision answers, inferred current open decisions, remaining inferred decision topics, planner follow-through answers, `followThrough.inferRemainingAnswers`, and `sourceResponseFormat: "auto"` no longer require commas, semicolons, or sentence breaks just to preserve clause-level topic authority. The embedded path stays fail-closed when any conjunction-linked segment lacks topic authority or one segment implies more than one topic summary.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_sentences"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when each sentence still mentions the relevant known topic somewhere inside it, without forcing inline labels at the front of the clause or ordered-list structure.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_spans"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when one anchor sentence names the topic and the following sentences stay on that same topic until the next anchor sentence appears, without requiring blank-line paragraph or block boundaries.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_closing_spans"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when one later closing sentence names the topic and the earlier sentences in that same stretch stay on that topic, without requiring the topic-bearing sentence to appear first or forcing blank-line paragraph or block boundaries.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_closing_blocks"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when one later closing paragraph names the topic and the earlier paragraphs in that same block stay on that topic, without requiring the topic-bearing paragraph to appear first.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_paragraphs"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when each answer already lives in its own multi-sentence paragraph and that paragraph mentions the relevant known topic at least once, without requiring the topic name in every sentence.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_middle_spans"` and `sourceResponseFormat: "topic_middle_blocks"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when each answer keeps one explicit topic-bearing sentence or paragraph in the middle, with at least one leading and trailing continuation unit around it, and adjacent spans or blocks are split by letting the unit immediately before the next anchor become the leading unit of the next answer.
- Answer-driven `topic_sentences` and `topic_paragraphs` now also match current durable decisions by deterministic stored `prompt` keyword anchors, so known decision answers no longer need to repeat the explicit topic label inside each mapped sentence or paragraph when the durable question wording already supplies enough authority.
- Answer-driven `inferDecisionTopics` now also supports `topic_clauses`, `topic_sentences`, `topic_spans`, `topic_middle_spans`, `topic_closing_spans`, `topic_closing_blocks`, `topic_paragraphs`, and `topic_middle_blocks`, so remaining topic-bearing clause, sentence, and block surfaces can become new durable decision topics without relying on inline labels, labeled sections, ordered structures, or explicit `answers[]` topic mapping.
- Answer-driven assistant and Bun API actions now also support `sourceResponseFormat: "topic_blocks"`, so one less-structured natural-language reply can deterministically feed explicit decision answers, inferred current open decisions, and planner follow-through answers when each answer starts with one anchor paragraph that names the topic and then continues through later unlabeled paragraphs until the next anchor paragraph appears.
- Answer-driven `topic_blocks` now also match current durable decisions by deterministic stored `prompt` keyword anchors, so known decision blocks no longer need their anchor paragraph to repeat the explicit topic label when the durable question wording already supplies enough authority.
- Answer-driven `inferDecisionTopics` now also supports `topic_blocks`, so remaining anchored multi-paragraph topic blocks can become new durable decision topics without requiring every paragraph to restate the topic or falling back to line-based labels, ordered structures, or explicit `answers[]` topic mapping.
- Topic-surface summary inference now also supports deterministic `as <topic>` phrases like `Use Bun-native auth as the auth strategy.` or `Start with five enterprise customers before broader launch as the pilot scope.`, so `topic_sentences`, `topic_paragraphs`, and `topic_blocks` no longer depend only on leading, prefixed, or trailing topic phrases when inferring new decision topics or remaining planner answers.
- Topic-surface summary inference now also supports deterministic copular topic phrases like `Bun-native auth should be the auth strategy.` or `Five enterprise customers should be the pilot scope.`, so `topic_sentences`, `topic_paragraphs`, and `topic_blocks` no longer misread those answer-first predicate forms as the durable topic summary itself when inferring new decision topics or remaining planner answers.
- Answer-driven `planning` and `planning_batch` follow-through now also support root `inferRemainingAnswers`, so the remaining unclaimed question/topic reply items can become shared planner captured answers without repeating non-decision summaries manually; this currently applies to `question_blocks`, `question_clauses`, `question_spans`, `question_middle_spans`, `question_closing_spans`, `question_closing_blocks`, `question_middle_blocks`, `topic_clauses`, `topic_sentences`, `topic_spans`, `topic_middle_spans`, `topic_closing_spans`, `topic_closing_blocks`, `topic_paragraphs`, `topic_middle_blocks`, and `topic_blocks`, and is intentionally kept disjoint from `inferDecisionTopics`.
- Decision-backed and answer-backed `workflow_batch` follow-through now also supports root `inferRemainingAnswers`, so remaining unclaimed question/topic reply items can become workflow-root shared planner captured answers after child explicit planner answers consume their own items, while still staying disjoint from `inferDecisionTopics`.
- Question-derived `inferDecisionTopics` and `inferRemainingAnswers` now also preserve the original question text as durable `prompt` authority on newly inferred decision topics and planner captured answers, so `question_blocks` / `question_clauses` / `question_spans` / `question_middle_spans` / `question_closing_spans` / `question_closing_blocks` / `question_middle_blocks` no longer lose the canonical user-facing question when runtime materializes those items automatically.
- Decision-backed and answer-backed `workflow_batch` follow-through now also supports explicit `reuseTaskRef` and `reuseGroupKey`, so one answered decision can adopt an arbitrary current planning surface or grouped planning surface through the same shared workflow-graph runtime instead of relying only on narrow implicit planning-linked reuse.
- Durable planning workflow graphs are now independently inspectable through Bun API list/detail endpoints plus a dedicated Bun UI workflow section, including workflow-root shared context and child request detail reconstructed directly from `planning-requests.yml` plus current open tasks.
- Normalized tool transcript entries now also persist stable `toolInvocationKey` metadata, and reviewer/merger run evidence now correlates tool calls with their results through that durable key instead of flattening transcript history into unrelated summary strings.
- Normalized tool-call transcript summaries now also capture stable target detail such as shell commands and file paths when vendor payloads expose them, so run detail and reviewer/merger evidence can show what a tool invocation actually touched without introducing a second tool-log store.
- Durable repo preferences now also use one canonical structured `.hopi/preference.md` document with stable `preferenceKey`, active or retired lifecycle state, optional rationale, structured record/retire APIs, and assistant preference actions that no longer rely on append-only deduplicated bullet guidance.
- Planning follow-through now computes requested-update coverage from open requests plus durable write traces, surfaces that coverage in planning contexts, and deterministically sends planning review/merge work back to `planned` when explicit requested updates still lack durable evidence.
- Opening a visible decision blocker for a planning task now also enriches any existing open planning request on that task with the decision key, and defaults missing requested-update targets to `design.md` plus `todo.yml`.
- Planning requests now support optional stable `groupKey`, and Goal assistant can request grouped multi-task planning follow-through in one constrained action with deterministic intra-batch task dependencies.
- When a grouped planning task opens a new decision blocker, runtime now enriches the other open requests in that same planning group with the same decision lineage instead of relying on manual repeated `decisionRefs`.
- Grouped planning requests now also persist durable `groupTaskKey`, and later grouped batches can extend an existing planning group by depending on earlier grouped siblings without replaying the whole batch.
- Engineering work blocked on grouped planning follow-through now retargets to the current open grouped leaf tasks instead of resuming when an earlier grouped planning task finishes.
- Reviewer and merger prompts now correlate prior run history, artifact refs, transcript evidence, and durable write traces instead of relying on write-trace policy alone.
- Planning reviewer and merger prompts now also enforce durable follow-through evidence against open planning requests, planning write traces, and prior run evidence.
- The Bun backend still serves the existing UI at `/` as a compatibility/reference surface while `packages/frontend` carries the restored product frontend ownership.
- Deeper planner/runtime behavior still remains intentionally out of scope for the current implementation slice.

Use this command before and after backend work:

```sh
bun run check
```

Expected result on a clean formatter-normalized worktree: backend typecheck, Biome, and Bun tests pass.

Current verification note for the 2026-06-04 in-flight slice:

- `cd packages/backend && bun run typecheck` passes.
- `bun test packages/backend/tests/answerInterpretation.test.ts` passes.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves deterministic topic-summary inference accepts possessive and slash-bearing noun phrases like `team's auth strategy` and `rollout/rollback strategy` across `topic_sentences` and `topic_spans`, instead of failing closed on prefixed/as-topic/trailing forms or misparsing copular answers as the topic summary itself.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `question_clauses` strips narrow leading conjunctions like `and` before explicit question and canonical-question normalization, so later clauses no longer leak that conjunction into inferred prompts/summaries or fail closed on canonical `What should <summary> be` parsing.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves deterministic `question_clauses` reject explicit or canonical clause answers that still contain another question anchor, instead of swallowing that second question into the first durable answer across explicit decision answers, `inferOpenDecisions`, or planner follow-through capture.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves deterministic `labeled_sections` reject later value sentences that still establish another explicit label/value clause, while still allowing literal colon text inside the first value sentence, and `sourceResponseFormat: "auto"` now fails closed on that malformed explicit-label line instead of falling through to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves direct explicit topic-answer capture rejects over-consumed topic-bearing units across `topic_sentences`, `topic_paragraphs`, and `topic_blocks`, and that `sourceResponseFormat: "auto"` no longer succeeds through those malformed direct-answer paths once later stronger topic surfaces detect leftover authority.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves paragraph-backed topic-summary inference rejects comma-delimited multi-topic paragraphs across explicit `topic_paragraphs`, remaining `inferDecisionTopics`, and `followThrough.inferRemainingAnswers`, while still preserving valid prefixed single-topic paragraphs such as `About pilot scope, ...`.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `sourceResponseFormat: "auto"` no longer falls past malformed explicit question-paragraph authority into weaker `ordered_blocks` or `topic_blocks`, so open-decision capture, remaining decision-topic inference, and remaining planner-answer inference now all fail closed once the question-family probes exhaust on a reply that still contains explicit or canonical question sentences.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves incomplete leading topic predicates like `rollout strategy should.` no longer materialize as direct topic answers, inferred decision topics, or inferred planner answers, and `sourceResponseFormat: "auto"` now fails closed on that malformed explicit topic authority instead of succeeding through dirty topic-family output.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves question-family parsing rejects explicit question answers whose answer text itself carries explicit topic authority for a different summary, so direct decision answers, `inferOpenDecisions`, remaining `inferDecisionTopics`, remaining `followThrough.inferRemainingAnswers`, and `sourceResponseFormat: "auto"` no longer misattribute cross-topic question replies like `What should rollout strategy be? For auth strategy, use Bun-native auth.` to the wrong durable summary.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves deterministic `topic_*` units reject answer text that carries explicit or canonical question authority, and embedded canonical `question_spans` / `question_middle_spans` / `question_closing_spans` now reject answer text with conflicting explicit topic authority, so direct explicit answers, remaining decision-topic inference, remaining planner-answer inference, and `sourceResponseFormat: "auto"` no longer materialize mixed question/topic replies through the wrong surface.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `labeled_sections` and `inline_topics` reject values that carry conflicting explicit topic authority or question authority, so direct explicit label answers, remaining decision-topic inference, open-decision matching, and `sourceResponseFormat: "auto"` no longer accept malformed explicit-label replies like `Auth strategy: Rollout strategy should use a staged rollout.` or `Auth strategy should what should rollout strategy be.` as valid deterministic captures.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves prefixed topic anchors strip narrow leading conjunctions like `and` before explicit topic-summary inference, so later `topic_sentences` no longer fail closed on `And about ...` and later `topic_spans` no longer swallow those later anchored stretches into the previous topic.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves explicit question surfaces normalize stripped later prompts to stable casing across `question_clauses`, `question_blocks`, and `question_spans`, so later `And rollout strategy?` questions no longer persist lowercased prompt/summary text just because they followed earlier punctuation.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts `+` bullets and single-letter alphabetic enumerators like `A.`, `a)`, `(a)`, and `（a）` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those prefixes as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts common Roman outline markers like `II.`, `III)`, `(iv)`, and `（iv）` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those markers as durable label/question/topic text, leaving standalone Roman fragments in sentence parsing, or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts common Unicode bullet glyphs like `○`, `◦`, and `▪` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those glyphs as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts markdown heading prefixes like `#`, `##`, and `###` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those markers as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts symbolic Unicode bullet glyphs like `◆`, `■`, `□`, `▸`, `▹`, `⁃`, and `‣` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those glyphs as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts fullwidth and circled numbering markers like `１．`, `②`, and `（３）` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those numbering markers as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts ideographic-comma and CJK numeral numbering markers like `1、`, `２、`, `一、`, and `（三）` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those numbering markers as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts dash-style Unicode bullet glyphs like `–`, `—`, and `―` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those glyphs as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves shared presentation-marker stripping accepts arrowhead Unicode bullet glyphs like `▶`, `▷`, `►`, and `▻` across `labeled_sections`, `question_*`, `topic_*`, and `auto`, instead of persisting those glyphs as durable label/question/topic text or letting `auto` fall to weaker surfaces.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves ordered-family parsing strips those same presentation-only prefixes from `ordered_items`, from every paragraph of `ordered_blocks`, and from `auto` replies that legitimately fall through to `ordered_items`, instead of persisting those markers into durable ordered answer text.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves generic non-anchor parsing strips those same presentation-only prefixes from `pending_clauses`, `pending_paragraphs`, `pending_sentences`, `pending_conjunctions`, paragraph-level and sentence-level `matching_runs`, and `auto` replies that legitimately fall through to `pending_sentences`, instead of persisting those markers into durable generic answer text or making `matching_runs` match against prefixed non-semantic text.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves embedded `matching_opening_runs`, `matching_closing_runs`, and `matching_middle_runs` strip presentation-only markers that only exist at run boundaries, instead of leaking `▶`-style prefixes into the first embedded run or `▷`-style separator markers into the previous embedded run just because those formats slice text back out of the original source response.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves embedded `question_spans`, `question_middle_spans`, `question_closing_spans`, `topic_spans`, `topic_middle_spans`, and `topic_closing_spans` strip those same presentation-only boundary markers from one-sentence embedded answer slices, instead of leaking `▶`-style prefixes into the current span or `▷`-style separator markers into the neighboring span just because those formats slice answer text directly from the raw source response.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves explicit `single_pending` strips those same presentation-only prefixes from generic whole-reply answers, instead of persisting those markers when the caller explicitly requests single-reply pending capture.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves non-embedded explicit clause/sentence/paragraph parsing strips those same presentation-only prefixes from `topic_clauses`, `topic_sentences`, `topic_paragraphs`, `question_spans`, `question_blocks`, and `question_clauses` answers, instead of persisting those markers when those families materialize normalized units or question-answer suffixes.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves explicit `labeled_sections` and `inline_topics` strip those same presentation-only prefixes from answer-side values like `> Use...`, `- [x] Use...`, or `1. Start...`, instead of only cleaning the label side while persisting marker pollution in the durable answer value itself.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `ordered_blocks` strip those same presentation-only prefixes from continuation paragraphs like `> That keeps...` or `[x] That keeps...`, instead of only cleaning the first paragraph of each ordered block while later paragraphs still leak marker pollution into the durable answer text.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `labeled_sections` and `inline_topics` reject `inferDecisionTopics` requests whose reply contains zero parseable labels, instead of bubbling that explicit label-surface mismatch up as the generic top-level `No decision answers were materialized` failure.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `labeled_sections` and `inline_topics` reject `inferOpenDecisions` requests whose reply either contains zero parseable labels or contains only labels that match none of the current open decisions, instead of bubbling that explicit label-surface mismatch up as the generic top-level `No decision answers were materialized` failure.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves explicit `question_*` and `topic_*` `inferOpenDecisions` surfaces reject replies whose parsed units match none of the current open decisions, instead of bubbling that explicit surface mismatch up as the same generic top-level `No decision answers were materialized` failure.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `sourceResponseFormat: "auto"` no longer falls through from explicit `question_*` or `topic_*` anchor authority to weaker generic surfaces like `ordered_blocks` when that parsed anchor authority belongs to a different open decision than the current call expects; those probes now fail closed with the stronger anchor-family mismatch instead of reinterpreting the reply through weaker fallback.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves `question_middle_spans`, `question_middle_blocks`, `topic_middle_spans`, and `topic_middle_blocks` reject replies with no middle anchor at all, instead of bubbling that explicit-surface mismatch up as a generic top-level "No decision answers were materialized" failure.
- `bun test packages/backend/tests/answerInterpretation.test.ts` now also proves deterministic `topic_blocks` reject source replies that begin with loose prose before the first anchored topic paragraph, instead of silently discarding that unmatched leading paragraph and materializing only the later anchored blocks.
- `cd packages/backend && bun test tests/assistantActionResultDetails.test.ts tests/assistantThreadPresentation.test.ts tests/assistantThreadStore.test.ts` passes.
- `cd packages/backend && bun test tests/assistantActionResultDetails.test.ts tests/assistantThreadPresentation.test.ts` now also proves planning-request inspection surfaces actual request-scoped captured answers and workflow-shared answers instead of only counts.
- `cd packages/backend && bun test tests/assistantActionResultDetails.test.ts tests/assistantThreadPresentation.test.ts` now also proves action-side planning and follow-through payloads surface planner-answer detail lines instead of only planner-answer counts.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves action-side reusable `answerSources` surface full route/key/excerpt detail lines instead of only reusable-source counts.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves `record_answer`, `record_answers`, and `resolve_decision` action inspection surfaces direct decision-answer detail lines instead of only `Explicit answers: N` or scattered decision metadata.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves planning-family action inspection surfaces explicit `blockedBy` detail on root requests, grouped requests, and workflow planning children instead of only update targets plus task/workflow dependency keys.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves planning-family assistant inspection surfaces `description` and `acceptanceCriteria` detail on root planning actions, grouped requests, workflow planning children, and durable planning-request results instead of leaving that authority in raw payload JSON only.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves `planning_batch` workflow children surface nested grouped-request update/blocker detail instead of flattening those children down to request-key lists.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves `move_task` and `create_planning_task` results preserve task snapshot authority and previous status instead of flattening task mutations down to `taskRef` plus summary text.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves assistant thread/run inspection surfaces task-result `description` and `acceptanceCriteria` detail on `move_task` and `create_planning_task` result snapshots instead of leaving that task body authority in raw JSON only.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts packages/backend/tests/assistantThreadStore.test.ts` now also proves assistant preference lifecycle inspection surfaces actual rationale, retirement reason, and updated preference content instead of boolean summary lines, and that those enriched preference result fields survive assistant-thread persistence.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts packages/backend/tests/assistantThreadStore.test.ts` now also proves assistant preference action results preserve structured durable preference-entry snapshots (`preference`, `retiredPreferences`, `preferences`) instead of only scalar keys/reasons, and that those snapshots survive assistant-thread persistence.
- `bun test packages/backend/tests/assistantActionResultDetails.test.ts packages/backend/tests/assistantThreadPresentation.test.ts` now also proves assistant thread/run inspection surfaces durable `resolvedAt` / `resolution` lifecycle detail already present on decision and planning-request result snapshots instead of leaving those fields in raw result JSON only.
- `bun test packages/backend/tests/assistantThreadStore.test.ts` now also proves those same `move_task` and `create_planning_task` task-result fields survive assistant-thread JSON persistence and readback, so the durable store boundary does not drop `previousStatus` or nested `task` snapshots.
- `bun test packages/backend/tests/server.test.ts --test-name-pattern "runs the configured Goal assistant and applies constrained durable actions"` now also proves assistant run HTTP/result/thread readback preserves enriched `record_preference` and `retire_preference` result authority instead of flattening preference results back down to keys only.
- `cd packages/backend && bun test tests/server.test.ts --test-name-pattern "(reuses question middle spans across decisions and planner answers|reuses question closing spans across decisions and planner answers|reuses topic clauses across decisions and planner answers|reuses topic spans across decisions and planner answers|reuses topic middle spans across decisions and planner answers|reuses topic closing spans across decisions and planner answers|infers shared planner answers from remaining question spans|infers shared planner answers from remaining question closing spans)"` passes when run unsandboxed.
- targeted `matching_middle_runs` coverage now also proves `auto` both selects valid middle-anchored generic replies and rejects partial middle-anchor authority before it can fall through to weaker `pending_clauses`.
- targeted unsandboxed `tests/server.test.ts` coverage now also proves assistant thread and assistant run-detail HTTP readback preserve full durable `decision`, `request`, and workflow/follow-through `requests` payloads instead of flattening them back down to keys.
- `cd packages/backend && bun run lint` currently fails on repo-wide Biome formatting and CRLF noise across many untouched files in the current worktree.
- sandboxed `Bun.serve()` listeners fail with `EADDRINUSE` in this environment, so server-backed Bun tests require an unsandboxed run for reliable verification.

## Authoritative Documents

Read these first:

- `README.md`: repo entry point and common commands.
- `docs/agent-handoff.md`: current state, guardrails, and next work.
- `packages/frontend/MIGRATION.md`: current frontend authority and continuation guide for evolving the original frontend codebase in place.
- `docs/hopi-phase-1-authority.md`: canonical Phase 1 schema and runtime boundary.
- `docs/superpowers/plans/2026-05-31-hopi-takeover-stabilization-plan.md`: completed Phase 1 execution plan and rationale.
- `docs/superpowers/specs/README.md`: complete grouped index of all current design-phase authority notes.
- `docs/superpowers/specs/2026-06-01-live-goal-assistant-execution-design.md`: current authority note for explicit Goal assistant execution.
- `docs/superpowers/specs/2026-06-01-goal-assistant-surfacing-and-inspection-design.md`: current authority note for assistant inspection APIs and Bun UI surfacing.
- `docs/superpowers/specs/2026-06-01-assistant-run-bundle-inspection-design.md`: current authority note for exact assistant bundle inspection on the Bun product path.
- `docs/superpowers/specs/2026-06-01-goal-assistant-preferences-and-planning-request-design.md`: current authority note for repo preference editing and safer assistant planning/preference actions.
- `docs/superpowers/specs/2026-06-02-structured-preference-lifecycle-design.md`: current authority note for canonical structured repo preferences with stable keys, active/retired lifecycle, and assistant/API preference mutations.
- `docs/superpowers/specs/2026-06-02-durable-decision-prompt-design.md`: current authority note for preserving exact user-facing decision questions directly on durable decision topics through shared runtime, assistant, API, and Bun UI surfaces.
- `docs/superpowers/specs/2026-06-02-planner-answer-prompt-design.md`: current authority note for preserving exact user-facing planner-answer questions directly on durable planning requests and reusing those prompts as matching authority during answer interpretation.
- `docs/superpowers/specs/2026-06-02-prompt-grounded-question-interpretation-design.md`: current authority note for reusing exact durable decision prompts as the matching authority for question-block and question-span answer interpretation.
- `docs/superpowers/specs/2026-06-02-prompt-core-question-interpretation-design.md`: current authority note for deterministic prompt-core reuse on question-block and question-span answer interpretation when the shared reply question is shorter than the stored durable prompt.
- `docs/superpowers/specs/2026-06-02-prompt-keyword-question-interpretation-design.md`: current authority note for deterministic prompt-keyword anchor reuse on question-block and question-span answer interpretation when the shared reply question keeps the same meaningful prompt words but changes their order.
- `docs/superpowers/specs/2026-06-02-prompt-keyword-topic-interpretation-design.md`: current authority note for deterministic prompt-keyword anchor reuse on `topic_sentences` and `topic_paragraphs` when the answer prose keeps the durable prompt’s meaningful words but omits the explicit topic label.
- `docs/superpowers/specs/2026-06-02-durable-match-hint-design.md`: current authority note for persisting explicit durable `matchHints` on decisions and planner answers, so later answer interpretation can reuse stable product-approved phrases without adding a second alias store or broader parser heuristics.
- `docs/superpowers/specs/2026-06-03-durable-summary-key-design.md`: current authority note for persisting explicit durable `summaryKey` values on decisions and planner answers, so later reusable answer sources can deterministically match known consumers even when visible summaries or prompts are not the stable authority.
- `docs/superpowers/specs/2026-06-03-durable-planner-answer-key-design.md`: current authority note for persisting explicit durable `answerKey` values on planner answers, so later writes can update the same captured planner-answer row by stable identity and reusable answer sources can target that known planner answer without relying on summary wording or old answer text.
- `docs/superpowers/specs/2026-06-03-contiguous-matching-answer-source-merge-design.md`: current authority note for letting `matching_answer_sources` merge adjacent reusable source entries for one already-known decision or planner-answer consumer while still rejecting non-contiguous repeats.
- `docs/superpowers/specs/2026-06-03-contiguous-remaining-matching-answer-source-merge-design.md`: current authority note for letting leftover `matching_answer_sources` entries merge adjacent reusable source entries into one new decision topic or one inferred planner-answer row when they already share explicit key-based authority, while still rejecting non-contiguous repeats.
- `docs/superpowers/specs/2026-06-03-pending-answer-source-key-authority-design.md`: current authority note for letting `pending_answer_sources` honor explicit durable keys on ordered reusable source entries, so adjacent entries can merge into one known consumer and wrong-key or non-contiguous repeats fail closed instead of being silently consumed by order.
- `docs/superpowers/specs/2026-06-03-contiguous-remaining-pending-answer-source-merge-design.md`: current authority note for letting leftover `pending_answer_sources` entries merge adjacent reusable source entries into one new decision topic or one inferred planner-answer row when they already share explicit key-based authority, while still rejecting non-contiguous repeats.
- `docs/superpowers/specs/2026-06-03-mixed-answer-source-route-inference-design.md`: current authority note for allowing one reusable `answerSources` bundle to create both brand-new durable decision topics and brand-new inferred planner-answer rows in the same mutation, but only when the leftover sources are explicitly routed by durable `decisionKey` or durable `answerKey`.
- `docs/superpowers/specs/2026-06-03-answer-source-route-metadata-design.md`: current authority note for lifting mixed answer-source routing onto explicit per-source `route` metadata, so leftovers can choose the decision side or planner side without pre-inventing durable row keys while still failing closed on route/key conflicts or ambiguous grouping.
- `docs/superpowers/specs/2026-06-03-answer-source-route-family-boundary-design.md`: current authority note for making explicit per-source `route` metadata a hard decision/planning family boundary during known consumer matching, so route-scoped reusable sources cannot be silently consumed by the wrong family before leftover inference runs.
- `docs/superpowers/specs/2026-06-03-unreachable-answer-source-route-fail-closed-design.md`: current authority note for failing closed when a decision-only or planning-only mutation leaves an explicitly routed reusable source entry unreachable after materialization, so route-scoped answer sources cannot be silently ignored when the current surface has no valid consumer on that family.
- `docs/superpowers/specs/2026-06-03-repeated-source-excerpt-occurrence-design.md`: current authority note for requiring explicit `sourceOccurrence` when one exact excerpt appears more than once inside one shared `sourceResponse`, so excerpt grounding stays deterministic instead of silently defaulting to the first repeated match.
- `docs/superpowers/specs/2026-06-03-answer-source-group-key-design.md`: current authority note for letting reusable `answerSources` entries carry one explicit `sourceGroupKey`, so one materialized answer can span non-contiguous source fragments without relaxing consumer-selection authority or falling back to weaker parser regrouping.
- `docs/superpowers/specs/2026-06-03-grouped-answer-source-reference-design.md`: current authority note for letting explicit decision/planner answer items consume one already-merged reusable answer-source group through `answerSourceGroupKey`, so grouped reusable sources can be reused directly without overloading single-fragment `answerSourceKey`.
- `docs/superpowers/specs/2026-06-03-auto-source-response-format-design.md`: current authority note for `sourceResponseFormat: "auto"` as a deterministic meta-surface that chooses the strongest successful existing interpretation surface by fixed priority instead of introducing another fuzzy parser family.
- `docs/superpowers/specs/2026-06-03-resolved-source-response-format-surfacing-design.md`: current authority note for surfacing the concrete deterministic interpretation format that runtime actually used through Bun API mutation responses and assistant action results, so `sourceResponseFormat: "auto"` no longer remains opaque after a successful mutation.
- `docs/superpowers/specs/2026-06-03-assistant-run-resolved-format-inspection-design.md`: current authority note for surfacing `resolvedSourceResponseFormat` through assistant-run detail inspection in the Bun UI instead of leaving answer-interpretation provenance visible only in immediate mutation responses or raw bundle files.
- `docs/superpowers/specs/2026-06-03-assistant-run-action-authority-inspection-design.md`: current authority note for surfacing structured assistant `action` authority through Bun assistant-run detail inspection instead of leaving requested mutation shape visible only in raw run JSON.
- `docs/superpowers/specs/2026-06-03-assistant-action-result-authority-metadata-inspection-design.md`: current authority note for surfacing the richer durable metadata already present on structured assistant `action_result` payloads, so thread/context/run inspection no longer collapse those results down to summary plus one narrow provenance field.
- `docs/superpowers/specs/2026-06-04-assistant-decision-result-authority-design.md`: current authority note for preserving the durable decision payload itself on assistant decision-family action results so run/thread inspection can show prompts, hints, capture format, and exact resolved answers instead of only keys plus summary text.
- `docs/superpowers/specs/2026-06-04-assistant-preference-authority-inspection-design.md`: current authority note for preserving and surfacing preference lifecycle rationale/reason/content through assistant result payloads plus shared inspection instead of collapsing preference actions and results down to keys and boolean summary lines.
- `docs/superpowers/specs/2026-06-04-assistant-preference-result-snapshot-design.md`: current authority note for preserving the full resulting durable preference-entry snapshots on assistant preference action results instead of returning only keys plus a few scalar rationale/reason fields.
- `docs/superpowers/specs/2026-06-04-assistant-resolution-lifecycle-inspection-design.md`: current authority note for surfacing `resolvedAt` and durable request `resolution` metadata already present on assistant decision/request result snapshots instead of leaving lifecycle detail in raw result bundles only.
- `docs/superpowers/specs/2026-06-04-assistant-planning-request-result-authority-design.md`: current authority note for preserving the durable planning-request payload itself on assistant planning and decision-follow-through action results so run/thread inspection can show persisted request titles, linked decisions, captured answers, workflow-shared answers, and requested update targets instead of only ids plus summary text.
- `docs/superpowers/specs/2026-06-04-action-decision-answer-detail-inspection-design.md`: current authority note for surfacing the direct decision-answer payload already present on `record_answer`, `record_answers`, and `resolve_decision` assistant actions instead of leaving that action authority split across count-only or key-only inspection lines.
- `docs/superpowers/specs/2026-06-04-action-answer-source-detail-inspection-design.md`: current authority note for surfacing the actual reusable answer-source authority already present on structured assistant actions instead of collapsing root `answerSources` down to count-only inspection lines.
- `docs/superpowers/specs/2026-06-04-planning-action-blocker-detail-inspection-design.md`: current authority note for surfacing planning-family `blockedBy` authority already present on structured assistant actions instead of leaving root requests, grouped requests, and workflow planning children visible only through weaker dependency keys.
- `docs/superpowers/specs/2026-06-04-planning-body-acceptance-inspection-design.md`: current authority note for surfacing planning-family `description` and `acceptanceCriteria` authority already present on assistant planning actions and durable planning-request results instead of leaving that body text visible only in raw JSON payloads.
- `docs/superpowers/specs/2026-06-04-task-result-authority-design.md`: current authority note for preserving the full mutated task snapshot on `move_task` and `create_planning_task` assistant results so thread/run inspection can show current task shape and previous status instead of only `taskRef` plus summary text.
- `docs/superpowers/specs/2026-06-04-task-result-body-inspection-design.md`: current authority note for surfacing `description` and `acceptanceCriteria` already present on durable `move_task` and `create_planning_task` result snapshots instead of leaving that task body detail hidden behind one compact task-summary line.
- `docs/superpowers/specs/2026-06-04-workflow-child-grouped-request-action-detail-design.md`: current authority note for surfacing nested grouped-request update and blocker detail from `planning_batch` workflow children instead of flattening those action payloads down to request-key lists.
- `docs/superpowers/specs/2026-06-04-planning-action-answer-detail-inspection-design.md`: current authority note for surfacing the actual planner-answer slots already present on planning actions and decision follow-through actions instead of collapsing that action authority down to count-only inspection lines.
- `docs/superpowers/specs/2026-06-04-planning-request-answer-detail-inspection-design.md`: current authority note for surfacing the actual captured planner-answer text and workflow-shared planner-answer text through assistant inspection plus Bun planning-request cards instead of collapsing that durable payload down to counts or workflow-only visibility.
- `docs/superpowers/specs/2026-06-04-auto-matching-middle-fail-closed-design.md`: current authority note for stopping `sourceResponseFormat: "auto"` from degrading partially consumed explicit `matching_middle_runs` authority into weaker pending-order reinterpretation like `pending_clauses`.
- `docs/superpowers/specs/2026-06-04-auto-matching-middle-run-fallback-design.md`: current authority note for letting `sourceResponseFormat: "auto"` reach `matching_middle_runs` as a real later generic-run fallback instead of leaving middle-anchored generic replies stranded behind stronger-but-inapplicable topic/question probes.
- `docs/superpowers/specs/2026-06-03-assistant-workflow-child-result-inspection-design.md`: current authority note for surfacing child-level workflow result metadata through shared assistant `action_result` inspection instead of flattening workflow results down to top-level counts and aggregate ids only.
- `docs/superpowers/specs/2026-06-03-assistant-runtime-event-authority-inspection-design.md`: current authority note for surfacing richer structured assistant runtime-event metadata through Bun assistant-run detail inspection instead of flattening transcript/worktree/artifact events down to one summary line.
- `docs/superpowers/specs/2026-06-03-assistant-thread-action-result-authority-design.md`: current authority note for persisting full structured assistant `action_result` data on assistant-thread history and reusing it across Bun thread inspection plus bundled assistant context instead of flattening that history down to summary-only text.
- `docs/superpowers/specs/2026-06-03-assistant-thread-action-authority-design.md`: current authority note for persisting full structured assistant `action` payloads on assistant-thread history and reusing them across Bun thread inspection plus bundled assistant context instead of flattening intent down to summary-only text.
- `docs/superpowers/specs/2026-06-03-assistant-action-authority-metadata-inspection-design.md`: current authority note for surfacing the richer durable metadata already present on structured assistant actions, so thread inspection and bundled assistant context no longer collapse that structured mutation authority down to just titles plus one or two generic fields.
- `docs/superpowers/specs/2026-06-03-durable-interpreted-answer-provenance-design.md`: current authority note for persisting that same concrete deterministic interpretation provenance as durable `captureFormat` metadata on resolved decision rows and materialized planner-answer rows instead of keeping it only in immediate mutation responses.
- `docs/superpowers/specs/2026-06-03-durable-answer-provenance-inspection-design.md`: current authority note for surfacing persisted `captureFormat` provenance through planner context, Bun decision inspection, Bun planning-request inspection, and Bun workflow inspection instead of leaving it trapped in raw durable state only.
- `docs/superpowers/specs/2026-06-03-durable-answer-authority-metadata-inspection-design.md`: current authority note for surfacing the rest of the persisted answer-matching authority (`summaryKey`, `answerKey`, and `matchHints`) through planner context, Bun decision inspection, Bun planning-request inspection, and Bun workflow inspection instead of flattening durable answers to prompt-plus-text.
- `docs/superpowers/specs/2026-06-03-auto-source-response-completeness-design.md`: current authority note for rejecting partially successful unit-based `auto` candidates that leave ordered/question/topic/pending/matching-run units unconsumed, so `auto` can continue searching for a later existing surface that fully captures the reply.
- `docs/superpowers/specs/2026-06-03-auto-label-surface-fail-closed-design.md`: current authority note for failing closed when explicit label authority has already been established by `labeled_sections` or multi-label `inline_topics`, instead of dropping to weaker later reinterpretation surfaces.
- `docs/superpowers/specs/2026-06-03-auto-answer-source-fail-closed-design.md`: current authority note for failing closed when explicit reusable `answerSources` authority remains incomplete after the answer-source family has already been tried, instead of dropping to weaker raw-reply reinterpretation surfaces.
- `docs/superpowers/specs/2026-06-03-auto-anchor-surface-fail-closed-design.md`: current authority note for failing closed when explicit `question_*` or `topic_*` anchor surfaces have already parsed anchored units but still remain incomplete, instead of dropping to weaker generic reinterpretation surfaces like `matching_runs`.
- `docs/superpowers/specs/2026-06-03-auto-anchor-authority-precision-design.md`: current authority note for refining that anchor fail-closed rule so `auto` only stops after `question_*` or `topic_*` surfaces have actually matched at least one consumer-specific unit, while purely generic paragraphs or sentences can still fall through to weaker ordered or pending surfaces.
- `docs/superpowers/specs/2026-06-02-inferred-topic-prompt-synthesis-design.md`: current authority note for synthesizing and persisting canonical prompts on brand-new inferred decision topics and planner answers that came from topic-shaped source responses without explicit question text.
- `docs/superpowers/specs/2026-06-02-summary-prompt-synthesis-design.md`: current authority note for synthesizing canonical prompts on summary-only durable decisions and planner answers, while still letting later explicit prompt text upgrade those synthesized defaults.
- `docs/superpowers/specs/2026-06-02-leading-topic-summary-interpretation-design.md`: current authority note for deterministic topic-summary inference from leading topic phrases like `Pilot scope should ...`.
- `docs/superpowers/specs/2026-06-02-prefixed-topic-summary-interpretation-design.md`: current authority note for deterministic topic-summary inference from prefixed topic phrases like `For auth strategy, ...`.
- `docs/superpowers/specs/2026-06-02-as-topic-summary-interpretation-design.md`: current authority note for deterministic topic-summary inference from `... as the auth strategy` style phrases across topic surfaces.
- `docs/superpowers/specs/2026-06-02-copular-topic-summary-interpretation-design.md`: current authority note for deterministic topic-summary inference from answer-first copular phrases like `Bun-native auth should be the auth strategy.` across topic surfaces.
- `docs/superpowers/specs/2026-06-02-topic-surface-decision-inference-design.md`: current authority note for deterministic brand-new decision-topic inference from remaining `topic_sentences` and `topic_paragraphs` without inline labels, labeled sections, or ordered reply structure.
- `docs/superpowers/specs/2026-06-02-prompt-keyword-topic-block-interpretation-design.md`: current authority note for deterministic prompt-keyword anchor reuse on `topic_blocks` when the block anchor paragraph keeps the durable prompt’s meaningful words but omits the explicit topic label.
- `docs/superpowers/specs/2026-06-02-shared-answer-source-design.md`: current authority note for reusing one less-structured raw user reply across multiple durable decision topics and non-decision follow-through answers.
- `docs/superpowers/specs/2026-06-02-named-answer-source-interpretation-design.md`: current authority note for reusing explicitly extracted topic-specific answer snippets across durable decision topics and non-decision follow-through answers without introducing a second durable store.
- `docs/superpowers/specs/2026-06-02-answer-source-excerpt-grounding-design.md`: current authority note for grounding reusable named answer sources directly in one shared raw reply through exact source excerpts.
- `docs/superpowers/specs/2026-06-02-direct-item-source-excerpt-design.md`: current authority note for grounding one decision answer or planner answer directly in a shared raw reply without first defining a named answer source.
- `docs/superpowers/specs/2026-06-02-labeled-source-response-interpretation-design.md`: current authority note for automatically materializing durable answers from one labeled shared raw reply without per-topic excerpt or mapping fields.
- `docs/superpowers/specs/2026-06-02-open-decision-labeled-answer-inference-design.md`: current authority note for resolving matching current open durable decisions directly from one labeled shared raw reply without repeating those decision topics inside `record_answers`.
- `docs/superpowers/specs/2026-06-02-ordered-source-response-interpretation-design.md`: current authority note for deterministically materializing durable answers from one ordered shared raw reply without labels, excerpts, or per-topic mapping.
- `docs/superpowers/specs/2026-06-02-ordered-block-source-response-design.md`: current authority note for deterministically materializing multi-paragraph durable answers from one ordered shared raw reply without labels or per-topic mapping.
- `docs/superpowers/specs/2026-06-02-single-pending-source-response-design.md`: current authority note for deterministically materializing one less-structured shared reply onto exactly one unresolved pending answer consumer without requiring question/topic anchors or ordered reply structure.
- `docs/superpowers/specs/2026-06-02-pending-clause-source-response-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from one less-structured shared clause reply without repeated anchors or ordered-list markers.
- `docs/superpowers/specs/2026-06-02-pending-paragraph-source-response-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from one less-structured shared paragraph reply without repeated anchors, ordered-list markers, or clause splitting.
- `docs/superpowers/specs/2026-06-02-pending-sentence-source-response-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from one less-structured shared sentence reply without repeated anchors, ordered-list markers, clause splitting, or paragraph splitting.
- `docs/superpowers/specs/2026-06-02-pending-conjunction-source-response-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from one less-structured shared conjunction-linked reply without repeated anchors, ordered-list markers, sentence splitting, clause splitting, or paragraph splitting.
- `docs/superpowers/specs/2026-06-02-pending-answer-source-order-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from ordered reusable `answerSources` without per-topic mapping.
- `docs/superpowers/specs/2026-06-02-matching-answer-source-design.md`: current authority note for deterministically materializing more than one already-known pending answer consumer from reusable `answerSources` matched by source labels and hints instead of current pending order or per-topic mapping.
- `docs/superpowers/specs/2026-06-03-matching-run-source-response-design.md`: current authority note for deterministically materializing repeated stretches for already-known decision or planner-answer consumers into one merged answer run without widening into brand-new topic or remaining-planner inference.
- `docs/superpowers/specs/2026-06-03-matching-run-orphan-prose-design.md`: current authority note for keeping `matching_runs` narrow by failing closed on unmatched prose before the first run, between different consumers, or after the last run instead of silently absorbing orphan prose into the nearest known consumer.
- `docs/superpowers/specs/2026-06-03-matching-closing-run-embedded-anchor-design.md`: current authority note for extending `matching_closing_runs` onto one continuous sentence when explicit known-consumer anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-03-matching-middle-run-embedded-anchor-design.md`: current authority note for extending `matching_middle_runs` onto one continuous sentence when explicit known-consumer anchors still appear once in the middle of each answer stretch.
- `docs/superpowers/specs/2026-06-03-matching-opening-run-embedded-anchor-design.md`: current authority note for extending `matching_opening_runs` onto one continuous sentence when explicit known-consumer anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-03-matching-run-assistant-exposure-design.md`: current authority note for exposing anchored matching-run formats clearly in assistant action guidance and aligned `inferOpenDecisions` validation text.
- `docs/superpowers/specs/2026-06-03-matching-opening-run-source-response-design.md`: current authority note for deterministically materializing less-structured answer stretches whose already-known consumer anchor sits at the start, with later continuation prose attached to that same consumer.
- `docs/superpowers/specs/2026-06-03-matching-closing-run-source-response-design.md`: current authority note for deterministically materializing less-structured answer stretches whose already-known consumer anchor sits at the end, with earlier continuation prose attached to that same consumer.
- `docs/superpowers/specs/2026-06-03-auto-generic-run-fallback-design.md`: current authority note for letting `sourceResponseFormat: "auto"` keep probing deterministic generic run surfaces when earlier topic-family probes only matched by durable consumer keywords and never established explicit topic authority.
- `docs/superpowers/specs/2026-06-03-matching-middle-run-source-response-design.md`: current authority note for deterministically materializing less-structured answer stretches whose already-known consumer anchor sits in the middle, with leading and trailing continuation prose attached to that same consumer.
- `docs/superpowers/specs/2026-06-04-question-middle-span-embedded-anchor-design.md`: current authority note for extending `question_middle_spans` onto one continuous sentence when explicit known question anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-04-question-closing-span-embedded-anchor-design.md`: current authority note for extending `question_closing_spans` onto one continuous sentence when explicit known question anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-04-question-span-embedded-anchor-design.md`: current authority note for extending `question_spans` onto one continuous sentence when explicit known question anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-04-question-canonical-embedded-anchor-inference-design.md`: current authority note for extending embedded `question_spans` / `question_middle_spans` / `question_closing_spans` onto one continuous sentence even before a question is already known, but only when the anchor fits the narrow canonical `What should <summary> be` form.
- `docs/superpowers/specs/2026-06-04-topic-clause-conjunction-design.md`: current authority note for extending `topic_clauses` onto one continuous sentence when explicit conjunction-linked segments still each carry exactly one explicit or inferable topic.
- `docs/superpowers/specs/2026-06-04-topic-span-embedded-anchor-design.md`: current authority note for extending `topic_spans` onto one continuous sentence when explicit inferable or already-known opening topic anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-04-topic-middle-span-embedded-anchor-design.md`: current authority note for extending `topic_middle_spans` onto one continuous sentence when explicit inferable or already-known middle topic anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-04-topic-closing-span-embedded-anchor-design.md`: current authority note for extending `topic_closing_spans` onto one continuous sentence when explicit inferable or already-known closing topic anchors still appear once per answer stretch.
- `docs/superpowers/specs/2026-06-02-answer-source-remaining-inference-design.md`: current authority note for letting leftover ordered or matching reusable `answerSources` entries directly materialize brand-new decision topics or planner answers when those sources already carry explicit `summary` authority.
- `docs/superpowers/specs/2026-06-02-question-block-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from question-and-answer style shared replies whose answer blocks no longer repeat the topic name.
- `docs/superpowers/specs/2026-06-02-question-clause-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from more than one self-contained natural question-and-answer clause inside one longer sentence without requiring sentence or paragraph boundaries.
- `docs/superpowers/specs/2026-06-02-question-closing-block-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from answer-first question blocks whose final paragraph is the question, without requiring the question paragraph to appear first.
- `docs/superpowers/specs/2026-06-02-question-closing-span-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from answer-first question spans whose final sentence is the question, without requiring the question sentence to appear first.
- `docs/superpowers/specs/2026-06-02-question-span-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from inline question-and-answer shared replies without requiring separate question paragraphs.
- `docs/superpowers/specs/2026-06-02-labeled-decision-topic-inference-design.md`: current authority note for turning remaining labeled shared-reply sections into new durable decision topics without existing open decisions or explicit `answers[]` mapping.
- `docs/superpowers/specs/2026-06-02-inline-topic-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from natural-language inline topic clauses without line-based labeled sections or ordered-list structure.
- `docs/superpowers/specs/2026-06-02-topic-clause-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from more than one self-contained natural topic clause inside one longer sentence without requiring sentence or paragraph boundaries.
- `docs/superpowers/specs/2026-06-02-topic-sentence-source-response-design.md`: current authority note for deterministically materializing durable answers from less-structured topic-mentioned sentences without front-loaded inline labels or ordered-list structure.
- `docs/superpowers/specs/2026-06-02-topic-span-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from anchored multi-sentence topic spans without requiring blank-line paragraph or block boundaries.
- `docs/superpowers/specs/2026-06-02-topic-middle-anchor-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics when a topic-bearing sentence or paragraph sits in the middle of each answer stretch, with leading and trailing continuation units on both sides.
- `docs/superpowers/specs/2026-06-02-question-middle-anchor-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics when a question sentence or paragraph sits in the middle of each answer stretch, with leading and trailing answer units on both sides.
- `docs/superpowers/specs/2026-06-02-topic-closing-block-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from multi-paragraph topic blocks whose final paragraph names the topic, without requiring the topic-bearing paragraph to appear first.
- `docs/superpowers/specs/2026-06-02-topic-closing-span-source-response-design.md`: current authority note for deterministically materializing durable answers and inferred decision topics from multi-sentence topic spans whose final sentence names the topic, without requiring the topic-bearing sentence to appear first.
- `docs/superpowers/specs/2026-06-02-topic-paragraph-source-response-design.md`: current authority note for deterministically materializing durable answers from multi-sentence topic paragraphs without requiring topic mentions in every sentence.
- `docs/superpowers/specs/2026-06-02-topic-block-source-response-design.md`: current authority note for deterministically materializing durable answers from anchored multi-paragraph topic blocks without requiring topic mentions in every continuation paragraph.
- `docs/superpowers/specs/2026-06-02-topic-block-decision-inference-design.md`: current authority note for deterministic brand-new decision-topic inference from remaining anchored `topic_blocks` without requiring every paragraph to restate the topic.
- `docs/superpowers/specs/2026-06-02-follow-through-inferred-planner-answer-design.md`: current authority note for turning the remaining unclaimed question/topic reply items into shared planner captured answers through root `followThrough.inferRemainingAnswers`.
- `docs/superpowers/specs/2026-06-02-workflow-follow-through-inferred-planner-answer-design.md`: current authority note for extending root `followThrough.inferRemainingAnswers` onto decision-backed and answer-backed `workflow_batch`, so remaining unclaimed structured reply items can become workflow-root shared planner answers after child explicit answers consume their own items.
- `docs/superpowers/specs/2026-06-02-inferred-question-prompt-persistence-design.md`: current authority note for preserving original question text as durable `prompt` authority when `question_blocks` / `question_spans` automatically materialize planner answers or brand-new decision topics.
- `docs/superpowers/specs/2026-06-02-decision-workflow-explicit-surface-reuse-design.md`: current authority note for explicit `reuseTaskRef` / `reuseGroupKey` parity on decision-backed and answer-backed `workflow_batch` follow-through.
- `docs/superpowers/specs/2026-06-02-planning-workflow-inspection-design.md`: current authority note for independent workflow-graph read surfaces on Bun API and UI, including workflow-root shared context and child request detail.
- `docs/superpowers/specs/2026-06-01-goal-assistant-decision-requests-and-management-design.md`: current authority note for assistant decision requests and direct decision management.
- `docs/superpowers/specs/2026-06-01-decision-resolution-follow-through-and-reconcile-controls-design.md`: current authority note for immediate decision-unblock follow-through and explicit reconcile controls.
- `docs/superpowers/specs/2026-06-01-write-trace-aware-review-and-merge-policy-design.md`: current authority note for trace-aware reviewer/merger prompt policy.
- `docs/superpowers/specs/2026-06-01-goal-docs-inspection-and-planner-doc-status-design.md`: current authority note for Goal doc inspection and planner durable doc-status policy.
- `docs/superpowers/specs/2026-06-01-durable-planning-requests-and-planner-follow-through-design.md`: current authority note for durable planning requests and deterministic planner follow-through.
- `docs/superpowers/specs/2026-06-01-decision-linked-planning-follow-through-design.md`: current authority note for decision-linked planning requests and explicit `design.md` / `todo.yml` follow-through targets.
- `docs/superpowers/specs/2026-06-01-planning-update-coverage-validation-design.md`: current authority note for requested-update coverage surfacing and scheduler hard guards on planning follow-through.
- `docs/superpowers/specs/2026-06-01-decision-driven-planning-request-enrichment-design.md`: current authority note for enriching open planning requests when visible decision blockers are opened for planning tasks.
- `docs/superpowers/specs/2026-06-01-decision-resolution-planner-follow-through-design.md`: current authority note for routing resolved engineering decisions through visible planner follow-through before engineering resumes.
- `docs/superpowers/specs/2026-06-01-goal-doc-planning-update-targets-design.md`: current authority note for using `goal.md` as a first-class planning follow-through target.
- `docs/superpowers/specs/2026-06-01-goal-doc-planning-update-paths-design.md`: current authority note for generalized Goal-local planning requested-update paths beyond the built-in core files.
- `docs/superpowers/specs/2026-06-01-decision-resolution-explicit-planner-workflows-design.md`: current authority note for carrying explicit single or grouped planner follow-through on engineering-linked decision resolution.
- `docs/superpowers/specs/2026-06-01-planning-linked-decision-follow-through-design.md`: current authority note for reusing the current planning surface when linked planning decisions resolve into explicit durable follow-through.
- `docs/superpowers/specs/2026-06-01-standalone-decision-follow-through-design.md`: current authority note for creating visible planner follow-through from an explicit answered decision before there is a blocker or reusable planning surface.
- `docs/superpowers/specs/2026-06-01-multi-workflow-answer-follow-through-design.md`: current authority note for fanning one durable answer out into multiple independent planner workflows under the same decision answer.
- `docs/superpowers/specs/2026-06-01-multi-decision-answer-follow-through-design.md`: current authority note for resolving multiple durable decision topics from one answer bundle and routing them through shared planner follow-through.
- `docs/superpowers/specs/2026-06-01-answer-backed-planning-requests-design.md`: current authority note for capturing non-decision user answers directly on durable planning requests.
- `docs/superpowers/specs/2026-06-02-direct-planning-answer-interpretation-design.md`: current authority note for giving direct planning request, grouped planning, and direct workflow surfaces the same interpreted planner-answer authority as decision-backed follow-through.
- `docs/superpowers/specs/2026-06-01-mixed-answer-follow-through-design.md`: current authority note for atomically combining decision-backed answers with non-decision captured answers on the same planner follow-through.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-batch-design.md`: current authority note for atomically opening more than one independent durable planning workflow directly on the planning surface.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-reuse-design.md`: current authority note for reusing one existing planning surface as the first child in a direct higher-order planning workflow batch.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-blocker-propagation-design.md`: current authority note for fanning engineering blockers out across every sink in a reused direct planning workflow batch.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-extension-design.md`: current authority note for giving direct higher-order planning workflows a stable top-level identity and durable extension path.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-child-key-design.md`: current authority note for giving standalone children inside direct higher-order planning workflows a stable durable child identity.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-child-dependency-design.md`: current authority note for durable child-to-child dependencies inside direct higher-order planning workflows.
- `docs/superpowers/specs/2026-06-01-direct-planning-workflow-shared-context-design.md`: current authority note for carrying shared decision lineage and captured answers across an entire direct higher-order planning workflow graph.
- `docs/superpowers/specs/2026-06-01-decision-workflow-graph-follow-through-design.md`: current authority note for reusing the same direct-workflow graph surface on decision-backed and answer-backed `workflow_batch` follow-through.
- `docs/superpowers/specs/2026-06-01-decision-workflow-shared-answer-context-design.md`: current authority note for carrying shared non-decision captured answers across an entire decision-backed or answer-backed workflow graph.
- `docs/superpowers/specs/2026-06-01-workflow-shared-context-persistence-design.md`: current authority note for persisting workflow-root shared context across later `workflowKey` extensions without introducing a second workflow store.
- `docs/superpowers/specs/2026-06-01-grouped-planning-workflow-reuse-design.md`: current authority note for reusing one already-open grouped planning surface as the first child in direct or decision-backed workflow graphs.
- `docs/superpowers/specs/2026-06-01-generated-workflow-key-design.md`: current authority note for making higher-order workflow graphs durable by default through runtime-generated `W-*` workflow keys.
- `docs/superpowers/specs/2026-06-01-decision-api-follow-through-result-design.md`: current authority note for surfacing full shared decision-runtime results, including generated workflow keys, on Bun decision answer/resolve APIs.
- `docs/superpowers/specs/2026-06-01-assistant-decision-follow-through-result-design.md`: current authority note for surfacing the same shared decision-runtime follow-through structure on assistant action results, persisted run detail, and Bun UI run inspection.
- `docs/superpowers/specs/2026-06-02-transcript-tool-correlation-design.md`: current authority note for persisting stable tool invocation keys on normalized transcript events and correlating tool call/result evidence across run detail and reviewer/merger context.
- `docs/superpowers/specs/2026-06-02-transcript-tool-target-detail-design.md`: current authority note for extracting stable shell-command and file-target detail from normalized tool-call transcript events so review/merge evidence can show what a correlated invocation actually did.
- `docs/superpowers/specs/2026-06-01-grouped-planning-follow-through-design.md`: current authority note for grouped planning follow-through across more than one visible planning task.
- `docs/superpowers/specs/2026-06-01-grouped-planning-decision-enrichment-design.md`: current authority note for propagating decision lineage across grouped planning follow-through.
- `docs/superpowers/specs/2026-06-01-incremental-grouped-planning-extension-design.md`: current authority note for durable grouped task keys and later grouped planning extension.
- `docs/superpowers/specs/2026-06-01-grouped-planning-blocker-propagation-design.md`: current authority note for keeping engineering blocked on the current open grouped-planning leaves.
- `docs/superpowers/specs/2026-06-01-run-history-and-artifact-aware-review-merge-policy-design.md`: current authority note for run-history and artifact-aware reviewer/merger policy.
- `docs/superpowers/specs/2026-06-01-planning-follow-through-review-merge-policy-design.md`: current authority note for planning follow-through reviewer/merger policy.

Historical reference only:

- `docs/hopi-goal-kanban-assistant-unified-design.md`
- `docs/hopi-multi-agent-architecture.md`
- `docs/hopi-multi-agent-implementation-plan.md`

Those historical docs contain old prototype concepts. Do not implement from them unless a newer authority doc explicitly reintroduces a concept.

## Hard Constraints

- Use Bun by default.
- Use `Bun.serve()` for backend APIs.
- Do not add Express, CORS middleware, execa, Vite backend coupling, `todo.mjs`, or a project-local kanban CLI.
- Keep the design simple. Prefer one deterministic source of truth over duplicated state.
- Do not add short-term compatibility layers for deleted prototype fields.
- Keep commits small and verified.

Phase 1 task schema does not include:

- `candidate`
- `blocked` as a task status
- `dependencyTaskList`
- durable historical blockers in `todo.yml`

## Data Model

Goal board path:

```text
.hopi/docs/goals/<goalKey>/todo.yml
```

Audit event path:

```text
.hopi/docs/goals/<goalKey>/events.jsonl
```

Write trace path:

```text
.hopi/docs/goals/<goalKey>/write-trace.jsonl
```

Goal decision path:

```text
.hopi/docs/goals/<goalKey>/decisions.yml
```

Planning request path:

```text
.hopi/docs/goals/<goalKey>/planning-requests.yml
```

Repo preference path:

```text
.hopi/preference.md
```

Runtime overlay path:

```text
.hopi/runtime/**
```

Runtime files are ignored and may be regenerated.

Goal assistant thread path:

```text
.hopi/runtime/goals/<goalKey>/assistant-thread.json
```

Goal assistant run path:

```text
.hopi/runtime/goals/<goalKey>/assistant/runs/<assistantRunId>/
```

Canonical task shape:

```yaml
version: 1
goal:
  goalKey: example
  title: Example Goal
items:
  - ref: T-1
    kind: engineering
    status: planned
    title: Implement a backend task
    description: Make the behavior work.
    acceptanceCriteria:
      - The behavior is covered by tests.
    blockedBy: []
```

Task kinds:

- `planning`
- `engineering`

Task statuses:

- `planned`
- `in_progress`
- `in_review`
- `merging`
- `done`

Blocker kinds:

- `task`
- `decision`
- `merge_conflict`
- `intervention`

Failure kinds:

- `agent_failed`
- `reviewer_rejected`
- `merge_conflict`
- `timeout`

`blockedBy` contains only current unresolved blockers. When a task blocker references a task that is now `done`, the scheduler removes that blocker and writes a `task_blocker_resolved` event.

When a decision blocker references a `decisionKey` that is resolved in `decisions.yml`, the scheduler removes that blocker and writes a `decision_blocker_resolved` event.

## Backend Modules

Current backend source:

- `packages/backend/src/domain/board.ts`: canonical task, blocker, status, failure, and event types.
- `packages/backend/src/domain/validation.ts`: YAML parsing, schema normalization, duplicate ref checks, missing task blocker checks, and task blocker cycle checks.
- `packages/backend/src/storage/paths.ts`: `.hopi` path construction.
- `packages/backend/src/storage/lock.ts`: file lock with same-process queue and stale lock handling.
- `packages/backend/src/storage/boardStore.ts`: atomic board reads, mutations, and event appends.
- `packages/backend/src/storage/decisionStore.ts`: durable Goal decision storage in `decisions.yml`, including optional exact question prompts.
- `packages/backend/src/storage/preferenceStore.ts`: canonical structured repo preference storage with stable keys, active/retired lifecycle, and file-native migration/validation for `.hopi/preference.md`.
- `packages/backend/src/storage/planningRequestStore.ts`: durable Goal planning-request storage in `planning-requests.yml`.
- `packages/backend/src/runtime/assistantThreadStore.ts`: Goal-scoped assistant conversation overlay under `.hopi/runtime/**`.
- `packages/backend/src/runtime/decisionRequest.ts`: shared control-path helper for decision requests plus resolution-side visible blocker cleanup.
- `packages/backend/src/runtime/answerInterpretation.ts`: explicit answer materialization and validation for shared raw replies plus reusable named extracted answer sources, including exact-excerpt grounding both through named sources and direct item-level excerpts plus deterministic labeled-section extraction.
- `packages/backend/src/runtime/planningRequest.ts`: shared control-path helper for durable planning requests plus planning-task follow-through resolution.
- `packages/backend/src/assistant/goalAssistantContext.ts`: Goal-scoped assistant context and prompt bundle generation.
- `packages/backend/src/assistant/assistantRun.ts`: assistant run record types and validation.
- `packages/backend/src/assistant/assistantRunStore.ts`: read-side assistant run inspection store.
- `packages/backend/src/assistant/GoalAssistantRuntime.ts`: explicit Goal assistant execution runtime and constrained durable action application, including structured planning requests, decision requests, and structured preference record/retire lifecycle.
- `packages/backend/src/runtime/attemptStore.ts`: ignored runtime attempt budget overlay.
- `packages/backend/src/runtime/runHistory.ts`: runtime run, step, message, and summary types.
- `packages/backend/src/runtime/runHistoryStore.ts`: Goal-scoped run history persistence under `.hopi/runtime/goals/<goalKey>/run-history.json`.
- `packages/backend/src/runtime/goalDocsStore.ts`: deterministic bootstrap plus inspectable `goal.md` / `design.md` content and `bootstrapped` versus `curated` status.
- `packages/backend/src/runtime/roleProcessContext.ts`: per-step `context.md` / `prompt.md` bundle generation with role-specific boundaries, planner durable-input plumbing for `todo.yml`, `decisions.yml`, `planning-requests.yml`, structured `.hopi/preference.md`, Goal doc status, and reviewer/merger evidence correlation across run history, artifact refs, transcript summaries, write traces, and planning follow-through requests.
- `packages/backend/src/runtime/worktreeManager.ts`: run-scoped git worktree preparation and cleanup.
- `packages/backend/src/runtime/gitMergeExecutor.ts`: deterministic git merge completion and settled-run cleanup for merger success paths.
- `packages/backend/src/runtime/writeTrace.ts`: durable write-trace types.
- `packages/backend/src/runtime/writeTraceStore.ts`: Goal-scoped append-only `write-trace.jsonl` storage.
- `packages/backend/src/runtime/writeTraceRecorder.ts`: process-focused file-change snapshot recorder for compact durable traces.
- `packages/backend/src/agent/AgentRunner.ts`: event-streaming execution adapter contract and scripted `MockAgentRunner`.
- `packages/backend/src/agent/ProcessAgentRunner.ts`: process-backed runner that can execute in the repo root or a prepared worktree and stream runtime evidence.
- `packages/backend/src/agent/ConfiguredRoleProcessRunner.ts`: repo-local role adapter config, placeholder substitution, context bundle wiring, and typed outcome ingestion.
- `packages/backend/src/agent/vendorTransport.ts`: built-in Codex / Claude / OpenCode transport config parsing and command resolution.
- `packages/backend/src/agent/vendorTranscript.ts`: built-in vendor stream normalization into structured transcript events.
- `packages/backend/src/scheduler/reconcileOnce.ts`: deterministic one-step scheduler.
- `packages/backend/src/server.ts`: Bun API, SSE endpoint, and temporary backend-served UI shell.
- `packages/backend/src/index.ts`: public exports.

Current backend tests:

- `packages/backend/tests/validation.test.ts`
- `packages/backend/tests/boardStore.test.ts`
- `packages/backend/tests/decisionStore.test.ts`
- `packages/backend/tests/answerInterpretation.test.ts`
- `packages/backend/tests/attemptStore.test.ts`
- `packages/backend/tests/preferenceStore.test.ts`
- `packages/backend/tests/assistantThreadStore.test.ts`
- `packages/backend/tests/assistantRunStore.test.ts`
- `packages/backend/tests/runHistoryStore.test.ts`
- `packages/backend/tests/goalDocsStore.test.ts`
- `packages/backend/tests/planningRequestStore.test.ts`
- `packages/backend/tests/roleProcessContext.test.ts`
- `packages/backend/tests/agentRunner.test.ts`
- `packages/backend/tests/configuredRoleProcessRunner.test.ts`
- `packages/backend/tests/gitMergeExecutor.test.ts`
- `packages/backend/tests/processAgentRunner.test.ts`
- `packages/backend/tests/reconcileOnce.test.ts`
- `packages/backend/tests/server.test.ts`
- `packages/backend/tests/sampleGoals.test.ts`
- `packages/backend/tests/writeTraceStore.test.ts`
- `packages/backend/tests/worktreeManager.test.ts`

## Scheduler Rules

`reconcileOnce` performs at most one deterministic action per call.

Before dispatching work, it removes resolved task blockers:

```text
blockedBy.kind == task and referenced task status == done
```

Then it selects the first unblocked dispatchable task and applies:

```text
planning/planned       -> planner   -> success: in_review
planning/in_review     -> reviewer  -> success: merging, reject: planned
planning/merging       -> merger    -> success: done
engineering/planned    -> generator -> success: in_review
engineering/in_review  -> reviewer  -> success: merging, reject: planned
engineering/merging    -> merger    -> success: done, merge_conflict: planned until budget exhausted
```

During a runner call, the task is temporarily persisted as `in_progress`. After the runner returns, the scheduler persists the final status.

Failure attempt budgets are stored in `.hopi/runtime/attempts.json` with keys like:

```json
{
  "T-1:merge_conflict": 2
}
```

When a failure kind reaches the max attempt budget:

- `merge_conflict` writes `blockedBy: [{ kind: "merge_conflict", ref: artifactRef }]`.
- Other failure kinds write `blockedBy: [{ kind: "intervention", ref: "<taskRef>:<failureKind>" }]`.

System errors are not task failures. Adapter, route, schema, or storage errors should be reported as system errors and must not become task blockers.

Run history is stored in:

```text
.hopi/runtime/goals/<goalKey>/run-history.json
```

Model rules:

- a run starts when a task leaves `planned`
- a step records one `planner` / `generator` / `reviewer` / `merger` dispatch
- a run stays `active` through successful review/merge progression
- a run ends as `retryable`, `blocked`, `completed`, or `system_error`
- step messages are runtime overlay only; they do not mutate workflow truth
- step transcripts carry normalized vendor execution semantics for built-in transports
- step execution evidence may include worktree metadata and artifact references

Runtime adapter events currently supported:

- `message`
- `transcript`
- `worktree_prepared`
- `artifact`

Planner context bundles now also receive these durable inputs:

- `goal.md`
- `design.md`
- current `todo.yml` content
- `decisions.yml`
- `.hopi/preference.md`
- relevant write traces

Configured role adapters live at:

```text
.hopi/runtime/agent-adapters.json
```

Context bundles live at:

```text
.hopi/runtime/goals/<goalKey>/runs/<runId>/<stepId>/
```

Model rules:

- `context.md` carries task, Goal, and boundary context into role processes
- `prompt.md` is the transport-facing execution prompt built from the current context bundle
- `outcome.json` lets reviewer and merger return typed outcomes on exit `0`
- `goal.md` and `design.md` are bootstrapped if missing before configured role execution

Built-in vendor transports now supported in `.hopi/runtime/agent-adapters.json`:

- `process`
- `codex`
- `claude`
- `opencode`

`agent-adapters.json` may now also include a top-level `assistant` transport config alongside `roles`.

Model rules:

- built-in vendor transports keep `outcome.json` as the deterministic workflow contract
- Codex and Claude transports pass `prompt.md` through stdin
- OpenCode transports pass prompt content as the final non-interactive message argument
- built-in vendor stdout is normalized into compact transcript entries before it reaches run history
- raw `process` transports remain available for repo-local custom adapters

Merger success paths now have deterministic backend post-processing:

- engineering merger success performs a real git merge from the run branch into the root repo
- merge conflicts abort the root merge and flow through the existing retry/budget path
- settled success paths clean the run worktree and disposable branch
- planning merger success can complete without a run branch

Durable write traces are stored in:

```text
.hopi/docs/goals/<goalKey>/write-trace.jsonl
```

Model rules:

- entries are compact append-only JSON lines
- entries summarize changed repo-relative paths without file contents
- write traces do not alter scheduler decisions or workflow truth
- the current recorder is process-backed and works for both root and worktree execution

## API

Start the backend:

```sh
cd packages/backend
bun run start
```

Expected startup line:

```text
[API] Server listening on http://localhost:3000
```

Routes:

```text
GET  /api/preferences
POST /api/preferences
POST /api/preferences/record
POST /api/preferences/retire
GET  /api/goals/:goalKey/board
GET  /api/goals/:goalKey/docs
GET  /api/goals/:goalKey/planning-requests
POST /api/goals/:goalKey/planning-requests
GET  /api/goals/:goalKey/decisions
POST /api/goals/:goalKey/decisions
POST /api/goals/:goalKey/decisions/:decisionKey/resolve
GET  /api/goals/:goalKey/runs
GET  /api/goals/:goalKey/runs/:runId
GET  /api/goals/:goalKey/write-traces
GET  /api/goals/:goalKey/assistant/thread
GET  /api/goals/:goalKey/assistant/runs
GET  /api/goals/:goalKey/assistant/runs/:assistantRunId
GET  /api/goals/:goalKey/assistant/runs/:assistantRunId/bundle
POST /api/goals/:goalKey/assistant/messages
POST /api/goals/:goalKey/assistant/run
POST /api/goals/:goalKey/tasks
POST /api/goals/:goalKey/tasks/:taskRef/move
POST /api/goals/:goalKey/reconcile
GET  /api/events
GET  /
```

Default bootstrap note:

- `createServer()` now prefers `ConfiguredRoleProcessRunner` when `.hopi/runtime/agent-adapters.json` exists.
- If adapter config is absent, it still falls back to `MockAgentRunner`.

Create task request:

```json
{
  "ref": "T-1",
  "kind": "engineering",
  "title": "Implement atomic writes",
  "description": "Make writes safe.",
  "acceptanceCriteria": ["Concurrent writes are safe."],
  "blockedBy": []
}
```

Manual move request:

```json
{
  "status": "in_review",
  "reason": "manual transition"
}
```

## Frontend State

Frontend ownership has been restored to `packages/frontend`.

The original React/Vite frontend baseline in `packages/frontend` has been restored and is the codebase that should keep evolving. The backend-served UI under `packages/backend/src/ui` is still available at `/` as a compatibility/reference surface, but it is not the product frontend path.

Do not create a new frontend tree. Continue frontend work in `packages/frontend`, following `packages/frontend/MIGRATION.md`.

Verified on 2026-06-06:

- `cd packages/frontend && bun run check` passes
- `bun run check:frontend` from repo root passes
- `bun run check` from repo root still fails in the existing backend/root Biome formatting pass on pre-existing CRLF/style drift outside `packages/frontend`; the current Track C frontend changes are not the source of that failure
- the restored frontend baseline now typechecks and builds again
- interactive dev still depends on local port availability in the operator environment

Current restored frontend baseline in `packages/frontend`:

- routed app shell via `App.tsx` plus `Layout.tsx`
- board-oriented view via `BoardView.tsx`, now wired to the live board, docs, decisions, planning-requests, workflows, preferences, reconcile, and `/api/events` surfaces, now also able to create tasks and manually move task status through the current board mutation routes, now also surfacing richer durable decision/planning/workflow inspection metadata like summary keys, match hints, capture formats, workflow-shared context, captured answers, workflow dependency detail, linked planning-task status, linked planning-task blocker refs, request/workflow resolution detail, and direct mutation-result authority like resolved source-response formats plus created/updated durable refs and statuses, now also exposing structured planning/workflow answer and reusable-answer-source editors plus reusable planning-answer suggestions and reusable source suggestions lifted from currently visible durable decision/planning answers, structured answer-bundle decision-entry editors plus reusable open-decision suggestions lifted from currently visible durable open decisions, structured decision-ref editors plus reusable decision suggestions lifted from currently visible durable decisions, structured blocker editors plus reusable blocker suggestions lifted from the currently visible unresolved tasks and open decisions, structured workflow-dependency editors plus reusable workflow-key suggestions lifted from the currently visible durable workflow graphs, reusable current workflow key/task/group suggestions for direct and answer-driven `workflow_batch` reuse fields, grouped batch-request / decision follow-through answer editors, structured list editors for acceptance criteria / decision refs / requested updates / workflow dependency keys / grouped task dependency keys / shared-answer match hints / preference supersedes across the current planner-workflow and durable matching-authority paths, and structured advanced workflow-child editors for both direct workflow batches and decision-follow-through workflow batches so current shared-answer planner authoring is no longer only raw JSON textarea or comma/newline text editing, and now also able to open decisions, resolve decisions with direct `planning` / `planning_batch` / `workflow_batch` follow-through authoring, source-excerpt / `answerSources[]` / shared-source-response resolve inputs, direct durable matching authority like `summaryKey` / `matchHints` / prompt-upgrade fields on create, single-answer, and direct-resolve flows, and explicit follow-through `answers[]`, capture answer-first decision bundles through the current `/decisions/answer` and `/decisions/answers` Bun routes, including source-excerpt / source-occurrence / source-key direct capture in single mode, carry reusable `answerSources[]` across those decision/planning/workflow shared-answer surfaces, drive answer-bundle follow-through with `followThrough.answers[]` and `followThrough.inferRemainingAnswers`, write shared-answer planner context directly onto planning requests and workflow batches through the current planning routes, author explicit planning-task blocker refs on direct planning requests, direct workflow planning children, and grouped batch requests, open planning requests, open direct workflow batches, extend existing workflow keys, reuse visible workflow surfaces, inspect one selected durable workflow graph through the current `/planning-requests/workflows/:workflowKey` API, and write durable preferences through the current Bun API
- those same reusable structured string-list editors now also support one-click bulk reuse of their current durable suggestions, so higher-order decision refs, workflow dependencies, and grouped task dependency surfaces no longer depend on re-clicking each individual current durable value
- those same structured blocker editors now also support one-click bulk reuse of all currently reusable blocker suggestions, so task, planning, workflow, and grouped-request blocker authoring no longer depends on re-clicking each current blocker ref one by one
- that same `BoardView` docs surface now also invalidates and refetches on current board / decision / planning mutations and on the matching live event-stream updates, so in-app `goal.md` / `design.md` inspection no longer stays stale after the frontend itself changes durable goal state
- those `goal.md` / `design.md` cards now also expand to full content in place instead of staying capped at a preview clamp, so longer durable Goal docs are fully inspectable inside the preserved frontend shell
- board-oriented view via `BoardView.tsx`, now wired to the live board, docs, decisions, planning-requests, workflows, preferences, reconcile, and `/api/events` surfaces, now also able to create tasks and manually move task status through the current board mutation routes, now also surfacing richer durable decision/planning/workflow inspection metadata like summary keys, match hints, capture formats, workflow-shared context, captured answers, workflow dependency detail, linked planning-task status, linked planning-task blocker refs, request/workflow resolution detail, and direct mutation-result authority like resolved source-response formats plus created/updated durable refs and statuses, now also exposing structured planning/workflow answer and reusable-answer-source editors plus reusable planning-answer suggestions and reusable source suggestions lifted from currently visible durable decision/planning answers, structured answer-bundle decision-entry editors plus reusable open-decision suggestions lifted from currently visible durable open decisions, structured decision-ref editors plus reusable decision suggestions lifted from currently visible durable decisions, structured blocker editors plus reusable blocker suggestions lifted from the currently visible unresolved tasks and open decisions, structured workflow-dependency editors plus reusable workflow-key suggestions lifted from the currently visible durable workflow graphs, reusable current workflow key/task/group suggestions for direct and answer-driven `workflow_batch` reuse fields, reusable current child `workflowTaskKey` plus child/group `groupKey` suggestions for direct and answer-driven workflow-child reuse fields, grouped batch-request / decision follow-through answer editors, structured list editors for acceptance criteria / decision refs / requested updates / workflow dependency keys / grouped task dependency keys / shared-answer match hints / preference supersedes across the current planner-workflow and durable matching-authority paths, and structured advanced workflow-child editors for both direct workflow batches and decision-follow-through workflow batches so current shared-answer planner authoring is no longer only raw JSON textarea or comma/newline text editing, and now also able to open decisions, resolve decisions with direct `planning` / `planning_batch` / `workflow_batch` follow-through authoring, source-excerpt / `answerSources[]` / shared-source-response resolve inputs, direct durable matching authority like `summaryKey` / `matchHints` / prompt-upgrade fields on create, single-answer, and direct-resolve flows, and explicit follow-through `answers[]`, capture answer-first decision bundles through the current `/decisions/answer` and `/decisions/answers` Bun routes, including source-excerpt / source-occurrence / source-key direct capture in single mode, carry reusable `answerSources[]` across those decision/planning/workflow shared-answer surfaces, drive answer-bundle follow-through with `followThrough.answers[]` and `followThrough.inferRemainingAnswers`, write shared-answer planner context directly onto planning requests and workflow batches through the current planning routes, author explicit planning-task blocker refs on direct planning requests, direct workflow planning children, and grouped batch requests, open planning requests, open direct workflow batches, extend existing workflow keys, reuse visible workflow surfaces, inspect one selected durable workflow graph through the current `/planning-requests/workflows/:workflowKey` API, and write durable preferences through the current Bun API
- board-oriented view via `BoardView.tsx`, now also exposes reusable current planning request keys, planning group keys, and grouped task keys lifted from the currently visible durable planning requests across direct planning request authoring, grouped batch-request editors, and nested direct/answer-driven workflow child batch-request editors, so grouped-planning reuse paths no longer depend on hand-typing stable request/group/task refs
- board-oriented view via `BoardView.tsx`, now also exposes reusable current grouped planning-request prefills inside grouped batch-request editors, so higher-order `planning_batch` authoring can lift current request title, description, acceptance criteria, requested updates, blockers, and stable grouped request identity without hand-copying an existing grouped sink back into the form
- board-oriented view via `BoardView.tsx`, now also exposes reusable current grouped planning-sink bundle prefills inside grouped batch-request editors, so answer-driven `planning_batch` follow-through, direct workflow `planning_batch` authoring, and advanced direct/answer-driven `planning_batch` child editors can lift one existing durable group key plus its grouped requests and workflow dependencies in one step instead of re-adding each request individually
- board-oriented view via `BoardView.tsx`, now also exposes reusable current planning-request prefills across direct planning request authoring plus direct and answer-driven planning-kind workflow-child authoring, so current title, description, acceptance criteria, decision refs, captured answers, requested updates, blockers, and stable request/group/group-task/workflow-child identity can be reused without hand-copying an existing durable planning request back into the form
- board-oriented view via `BoardView.tsx`, now also exposes reusable current planning-request prefills across answer-driven `planning` follow-through authoring, so current title, description, acceptance criteria, captured answers, and requested updates can be lifted from an existing durable planning request directly into `resolve decision` and `Answer Bundles` follow-through instead of being recopied by hand
- those same grouped batch-request editors now also support one-click bulk reuse of all currently reusable grouped planning requests, so preserved frontend no longer depends on re-clicking every grouped request suggestion when seeding a higher-order `planning_batch` authoring surface from current durable planning sinks
- those same reusable current grouped planning-request and grouped planning-sink prefills now also filter down to the real open visible grouped surfaces that runtime can actually reuse, so resolved grouped requests and done-task group members no longer leak stale request or sink suggestions into higher-order `planning_batch` authoring
- those same current planning group-key and grouped-task-key suggestion surfaces now also filter down to real open visible planning requests, so resolved grouped requests and done-task grouped rows no longer leak stale stable-key suggestions back into direct planning and grouped-request authoring
- those same reusable current planning-request prefills and current planning-request-key suggestion surfaces now also filter down to real open visible planning requests, so resolved requests and done-task planning rows no longer leak stale request payloads or stable request keys back into direct planning and workflow-child authoring
- those same reusable grouped planning-request suggestions now also keep bulk-reuse and already-added identity aligned with the full grouped-request payload that current structured editors can really materialize, including description, acceptance criteria, requested updates, and blocker payload, so preserved frontend no longer treats two materially different durable grouped requests as the same reusable request just because they share a task key or title
- those same reusable current grouped planning-sink previews no longer silently stop after the first three grouped request titles, so higher-order `planning_batch` reuse can preview the full current grouped sink body instead of truncating request-title authority before the existing preview summarizer runs
- those answer-driven workflow-child grouped batch-request editors now also reuse the same current blocker suggestions as the direct grouped-planning surfaces, so decision-backed grouped planning authoring no longer drops blocker-ref reuse
- board-oriented view via `BoardView.tsx`, now also exposes `decisionRefs` on direct advanced workflow-child `planning_batch` entries in the structured editor, so child-level decision context for that workflow path now matches the current backend contract instead of staying behind raw JSON fallback
- board-oriented view via `BoardView.tsx`, now also exposes stable `requestKey` on direct simple workflow-child `planning` authoring plus reusable current planning request key suggestions, so carrying that durable child request ref no longer requires dropping to advanced JSON
- board-oriented view via `BoardView.tsx`, now also gates direct and answer-driven advanced workflow-child identity fields by the real backend child contract, so `requestKey` / `workflowTaskKey` / `groupKey` no longer appear on child kinds that would otherwise be silently dropped on submit
- board-oriented view via `BoardView.tsx`, now also gates the direct simple workflow-child surface by that same backend contract, keeps `groupKey` required on the simple `planning_batch` path, and removes planning-only fields like `workflowTaskKey`, `title`, `description`, `acceptanceCriteria`, and `requestedUpdates` from `planning_batch` children where the backend would otherwise just ignore them
- board-oriented view via `BoardView.tsx`, now also gates answer-driven follow-through submit paths by that same live backend schema, so `planning` follow-through still requires title/description/acceptance criteria, root `planning_batch` follow-through requires `groupKey` plus at least one grouped request, and root `workflow_batch` follow-through requires at least one workflow child before `resolve decision` or `record_answer(s)`-style frontend mutations can submit
- board-oriented view via `BoardView.tsx`, now also lets direct simple `planning_batch` workflow children omit grouped requests when they only need to adopt an existing grouped planning sink into a workflow graph, while still validating any non-empty grouped-request payload before submit
- board-oriented view via `BoardView.tsx`, now also gates direct planning-request submit on at least one acceptance criterion, matching the live backend schema instead of leaving that invalid payload to mutation-time rejection
- board-oriented view via `BoardView.tsx`, now also stops over-constraining backend-supported empty descriptions on task/planning/workflow planning surfaces and stops over-constraining task create with a required acceptance-criteria list, so those frontend mutations now match the live Bun schema instead of forcing a narrower subset than the backend accepts
- board-oriented view via `BoardView.tsx`, now also gates the advanced workflow-children override on at least one materialized child, matching the live backend `workflows.min(1)` schema instead of letting an empty override submit and fail only at mutation time
- board-oriented view via `BoardView.tsx`, now also front-loads optional structured JSON validation for blockers, planning answers, reusable answer sources, batch decision answers, simple workflow-child answers, and answer-driven follow-through answer payloads, so those non-empty-but-invalid payloads no longer wait until mutation-time parser rejection
- board-oriented view via `BoardView.tsx`, now also front-loads the direct `sourceOccurrence` semantic guard on single-answer and direct-resolve capture paths, so occurrence indices still require `sourceExcerpt` and malformed values no longer wait until mutation-time rejection
- board-oriented view via `BoardView.tsx`, now also front-loads the live durable preference-key regex on record/retire submit paths, so optional `preferenceKey`, `supersedes`, and `supersededBy` values no longer wait until mutation-time rejection when they fall outside the Bun contract
- board-oriented view via `BoardView.tsx`, now also reuses the current durable decision summary and matching authority by default on direct resolve, so shared-source resolve flows no longer degrade to the route's synthetic `Decision: <key>` fallback when the frontend already knows the real summary, summary key, prompt, match hints, and linked task ref
- board-oriented and runtime inspection views via `BoardView.tsx` and `SessionView.tsx` now also front the backend durable write-trace `role` and `limit` filters, so task-scoped and run-scoped trace inspection can be narrowed to planner/generator/reviewer/merger writes, expanded beyond the old silent 20-entry cap, and inspected with the current `callId` / `cwd` authority without leaving the preserved frontend shell
- board-oriented view via `BoardView.tsx`, now also carries `sourceOccurrence` on structured batch decision-answer entries through the live frontend contract, with the same `sourceExcerpt` requirement as the backend schema, so shared-reply capture no longer drops that authority at submit time
- board-oriented view via `BoardView.tsx`, now also shows fuller selected workflow-graph inspection authority in the existing `Workflow Graphs` card, including workflow-root request/task/group/blocker refs, child-level blocker task refs, and grouped child request detail like workflow-shared decisions, grouped task keys, acceptance criteria, and resolution timestamps
- board-oriented view via `BoardView.tsx`, now also uses the structured batch-request and structured workflow-child editors as the primary entrypoint for the deepest direct and answer-driven workflow-child mutation surfaces, so those planner/workflow paths no longer keep parallel raw advanced JSON textareas beside the real authoring controls
- board-oriented view via `BoardView.tsx`, now also uses the structured blocker, planner-answer, decision-answer, and answer-source editors as the primary entrypoint across the current top-level task, resolve-decision, answer-bundle, planning-request, direct workflow, answer-driven follow-through, nested grouped batch-request, and advanced workflow-child mutation forms, so those live product surfaces no longer keep parallel raw JSON textareas beside the real authoring controls
- board-oriented view via `BoardView.tsx`, now also honors answer-source-only interpretation formats on the current resolve-decision, answer-bundle, planning-request, and direct workflow surfaces without requiring a shared source response, and preserved-frontend submit gating now mirrors backend format compatibility for `inferOpenDecisions`, `inferDecisionTopics`, and `inferRemainingAnswers` instead of leaving those format mismatches to mutation-time rejection
- board-oriented view via `BoardView.tsx`, now also surfaces inline `sourceResponseFormat` guidance across the current resolve-decision, answer-bundle, planning-request, and direct workflow forms, including human-readable labels, deterministic input-authority hints, visible inference compatibility for `inferOpenDecisions`, `inferDecisionTopics`, and `inferRemainingAnswers`, plus active incompatibility reasons when the current format/input authority cannot support the selected shared-answer inference path, so those advanced deterministic reply surfaces no longer require hopping into backend docs or reverse-engineering a disabled submit button before a user can choose the right format
- board-oriented view via `BoardView.tsx`, and specifically `Answer Bundles`, now also front-loads the mixed answer-source routing rule for `inferDecisionTopics + followThrough.inferRemainingAnswers`, so non answer-source-family formats and remaining answer-source entries without explicit or non-conflicting routing are blocked before submit instead of failing only after backend shared-answer interpretation
- those same structured answer-source editors inside `BoardView.tsx` now also expose reusable current-consumer routing suggestions across resolve-decision, answer-bundle, planning-request, and direct workflow authoring, including the current single-decision answer-bundle draft and resolve-decision matching-authority overrides, so preserved frontend can append `route` / `decisionKey` / `answerKey` seeded answer-source stubs for the real current decision or planner consumers instead of hand-authoring every mixed remaining-answer route
- those same structured answer-source editors inside `BoardView.tsx` now also support bulk-prefilling all current consumer routes or all reusable durable answer sources in one step, so higher-order shared-answer authoring no longer depends on re-clicking every individual routing or source suggestion across the current resolve-decision, answer-bundle, planning-request, and direct workflow surfaces
- those same shared structured answer-source editors inside `BoardView.tsx` now also expose one-click current answer-source setup inside the editor itself, combining current consumer routing with reusable durable answers so nested advanced workflow-child and other higher-order shared-answer surfaces no longer depend on running those two bulk reuse flows separately when no top-level guidance surface is present
- the preserved `Preferences` surface inside `BoardView.tsx` now also exposes reusable current preference-key suggestions for stable-key, `supersedes`, and `supersededBy` authoring, and its live preference cards plus record/retire result cards now surface full durable preference authority like rationale, retired reason, superseded-by links, and reverse supersedes instead of summary-only rows
- those same shared structured planning-answer and batch decision-answer editors inside `BoardView.tsx` now also support bulk-prefilling current durable answers, current open decisions, and current reusable answer-source-backed answer patches in one step, so higher-order planner/workflow/shared-answer authoring no longer depends on re-clicking every individual durable answer or open-decision suggestion across the current follow-through, planning-request, workflow-batch, and advanced workflow-child surfaces
- those same shared structured planning-answer and batch decision-answer editors inside `BoardView.tsx` now also expose one-click current-answer setup that combines reusable durable/open-decision answer authority with reusable answer-source-backed answer patches, so higher-order planner/workflow/shared-answer authoring no longer depends on running those two bulk reuse flows separately before current answer context is usable
- board-oriented view via `BoardView.tsx`, now also exposes format-aware starter templates for shared source responses across the current resolve-decision, answer-bundle, planning-request, and direct workflow forms, so preserved frontend can insert a deterministic skeleton for the selected `sourceResponseFormat` instead of making the user author each reply shape from scratch
- those same current-context starter templates now also extend across the remaining `matching_*` and `topic_* middle/closing` deterministic families, so higher-order shared-answer guidance no longer falls back to prose-only consumer-order hints on those formats when preserved frontend already knows the current durable consumer authority
- those same interpretation-heavy board forms now also expose current-consumer-aware source-response prefills, and that consumer aggregation now includes higher-order answer-driven follow-through plus workflow-child answer consumers instead of only outer decision/root answer fields, so preserved frontend can seed deterministic shared-reply skeletons from the real current durable answer authority already in play
- those same interpretation-heavy resolve-decision, answer-bundle, planning-request, and direct workflow forms now also let one current-context setup action seed both the deterministic shared-reply template and the current consumer routing entries together, so higher-order shared-answer setup no longer depends on separately inserting a template and then bulk-routing those same current consumers back into structured answer sources
- that same current-consumer guidance now also stays live on answer-source-only formats like `pending_answer_sources` and `matching_answer_sources`, so preserved frontend can still route current consumers directly from the guidance surface even when no shared source-response textarea or deterministic reply template is in play
- that same current-consumer guidance now also stays visible on `sourceResponseFormat: "auto"`, so preserved frontend no longer hides current consumer order or routing actions just because the user has not chosen a concrete deterministic format yet
- that same `sourceResponseFormat: "auto"` guidance now also exposes a one-click current-context handoff into one recommended deterministic reply surface, so preserved frontend can switch straight from `auto` into a seeded `question_blocks` or `labeled_sections` reply template instead of manually changing formats before current-context template setup starts
- that same `sourceResponseFormat: "auto"` guidance now also exposes a one-click current-context setup handoff into the recommended deterministic reply surface, so preserved frontend can switch formats, seed the reply template, and route current consumers into structured answer sources in one step instead of repeating the current-context setup flow after leaving `auto`
- those same interpretation-heavy resolve-decision, answer-bundle, planning-request, and direct workflow surfaces now also expose a one-click current answer-source setup path that combines current consumer routing with reusable durable answers, so preserved frontend no longer depends on separately bulk-routing current consumers and then bulk-reusing current durable answers before answer-source-only or mixed shared-answer authoring is usable
- those same current-context deterministic starter templates now also cover `single_pending`, and `sourceResponseFormat: "auto"` now recommends that lighter single-consumer surface whenever only one current consumer is in play, so single-consumer shared-answer setup no longer has to route through heavier labeled or question-block formats just to seed one answer
- the direct `Planning Requests` shared-answer surface now also exposes one-click current-answer-context setup that combines the deterministic shared-reply template, current answer-source routing/source reuse, and the shared structured planning-answer editor setup in one step, so that preserved-frontend planning route no longer depends on running those three setup flows separately before current answer context is ready to submit
- the direct `Workflow Graphs` root shared-answer surface now also exposes one-click current-answer-context setup that combines the deterministic shared-reply template, current answer-source routing/source reuse, and the root shared planning-answer editor setup in one step, so that preserved-frontend workflow route no longer depends on running those three setup flows separately before current shared answer context is ready to submit
- the `Answer Bundles` batch shared-reply surface now also exposes one-click current-answer-context setup that combines the deterministic shared-reply template, current answer-source routing/source reuse, and the structured batch decision-answer editor setup in one step, so that preserved-frontend answer-bundle route no longer depends on running those three setup flows separately before current bundle answer context is ready to submit
- direct `resolve decision` plus `Answer Bundles` single-decision shared-answer setup now also expose the same one-click current-answer-context flow for their single-decision routes, combining the deterministic shared-reply template with current answer-source routing/source reuse in one step, so preserved frontend no longer depends on manually chaining template setup and answer-source setup before current single-decision answer context is ready to submit
- those same current-answer setup flows now also follow the real materialized child-authoring path on those surfaces: when no advanced workflow-child override is present they seed the simple workflow-child answer editors, and when a structured advanced child override is present they treat the simple child draft as inactive and seed only the workflow root plus advanced child editors, so `resolve decision`, `Answer Bundles`, and direct `Workflow Graphs` no longer mix outer/root answer setup with child-authoring state that would not actually materialize
- board-oriented view via `BoardView.tsx`, now also exposes reusable current workflow root-context prefills across direct `Workflow Graphs` authoring and answer-driven `workflow_batch` follow-through, so one existing durable workflow's `workflowKey`, shared decision refs, shared answers, and reuse task/group identity can be lifted into higher-order workflow authoring instead of being recopied by hand
- those same direct `Workflow Graphs` and answer-driven `workflow_batch` surfaces now also expose reusable current workflow-graph prefills, so one existing durable workflow's current child structure can be lifted back into the structured advanced child editors instead of being rebuilt by hand
- direct `Workflow Graphs` create plus answer-driven workflow create/follow-through success paths now also auto-focus the returned durable workflow graph only after the live workflow list reflects that key, so newly created or reused workflow inspection no longer snaps back to an older stale selection before the current workflow graph is visible
- those same structured advanced workflow-child editors now also expose reusable current workflow-child prefills, so one existing durable workflow child can be appended directly into direct or answer-driven advanced child authoring instead of being rebuilt field by field
- those same reusable current workflow-child suggestions now also keep preview and append/select-all identity aligned with the child authority that each surface can really materialize, including current planner answers, workflow dependencies, grouped-request payloads, and direct-workflow child decision refs, so preserved frontend no longer treats two materially different reusable children as the same suggestion or hides that richer child context behind identical preview rows
- those same reusable current grouped planning-sink prefills now also carry grouped shared planner answers back into direct, answer-driven, and advanced `planning_batch` authoring, so preserved frontend no longer reuses one existing grouped sink's requests and workflow dependencies while silently dropping that sink's current answer authority
- those same reusable current grouped planning-sink prefills now also carry grouped shared `decisionRefs` back into direct `Workflow Graphs` `planning_batch` children, so direct higher-order workflow reuse no longer drops one existing grouped sink's durable decision context while lifting its grouped requests and shared answers
- those same structured grouped-request and workflow-child reuse flows now also canonicalize `blockedByJson`, `answersJson`, and `batchRequestsJson` before they compare reusable selections, so semantically identical higher-order planner/workflow payloads no longer drift into duplicate `Reuse` / `Append` candidates just because their JSON formatting differs
- answer-driven `workflow_batch` child authoring intentionally does not front child-level `decisionRefs`; the direct decision follow-through route injects resolved decision lineage automatically, so preserved frontend now mirrors that live contract instead of implying that `resolve decision` / `Answer Bundles` can submit direct-workflow-style child decision context
- answer-driven current workflow-child prefills likewise no longer treat child-level `decisionRefs` as part of their preview or identity authority, so append/select-all flows on that surface now mirror the real direct follow-through contract instead of surfacing non-submittable child decision context
- those same structured advanced workflow-child editors now also support one-click append of all currently reusable durable workflow children, so preserved frontend no longer depends on re-clicking each current child suggestion when seeding a higher-order direct or answer-driven workflow override from the current graph
- the direct simple workflow-child form inside `Workflow Graphs` now also exposes reusable current workflow-child prefills, so one existing durable child can be lifted straight back into the simple child authoring path without switching over to the advanced-child override first
- board-oriented view via `BoardView.tsx`, now also exposes a simple workflow-child authoring path inside answer-driven `workflow_batch` follow-through, including reusable current workflow-child and planning-request prefills plus the same simple-child fallback semantics that direct `Workflow Graphs` already uses whenever no structured advanced child override is present
- board-oriented view via `BoardView.tsx`, now also lets single-decision `Answer Bundles` reuse current open decision authority as a prefill source, so one existing durable decision's summary, key, summary key, prompt, match hints, linked task, and answer-source key identity can be lifted into single-answer capture without retyping them by hand
- board-oriented view via `BoardView.tsx`, now also exposes reusable current visible task refs across `Decision Topics`, direct `resolve decision`, single-decision `Answer Bundles`, and the structured batch decision-answer editor, so those decision and answer-capture surfaces no longer depend on hand-typing current task identity
- board-oriented view via `BoardView.tsx`, now also reuses current answer-source content authority across direct `resolve decision`, single-decision `Answer Bundles`, and the shared structured planning-answer / decision-answer editors, so those higher-order shared-answer surfaces can prefill current summary/answer/source/matching fields while still carrying existing `answerSourceKey` / `answerSourceGroupKey` identity instead of hand-typing or copy-pasting them
- those same current-context deterministic starter templates no longer silently stop after the first 12 normalized consumers, so larger direct resolve, answer-bundle, planning-request, and direct workflow shared-answer surfaces can now seed the full current durable consumer set instead of truncating current-context reply setup partway through
- top-level planning-request cards and selected workflow child-request cards now also show full durable request descriptions, request statuses, grouped-task keys, created timestamps, and grouped child workflow dependencies instead of truncating that request authority down to summary-only lines
- those selected workflow child-request cards and the matching child-request cards inside `Task Authority -> Workflow Membership` now also reuse one shared request-authority detail surface, so workflow/task/group child metadata, shared-answer context, requested updates, resolution detail, and resolved timestamps no longer drift between the existing workflow and task inspection panels
- the top-level `Planning Requests` cards, `Task Authority -> Linked Planning Requests`, and direct planning-request success card now also reuse that same shared request-authority detail surface, and it now includes `resolvedSourceResponseFormat`, so returned planning authority no longer drifts between the live planning list, task/workflow request inspection, and the direct create-success surface
- that same direct planning-request success card now also preserves the route's real `201/200` create-vs-reuse authority, so preserved frontend no longer treats every successful planning mutation as a fresh create when the backend actually returned an already-open or already-resolved durable request
- that same direct planning-request success card now also preserves whether the current visible board had to allocate a fresh planning task ref for the returned request, so preserved frontend no longer hides task reuse when a durable planning request was created on top of an existing visible planning task
- top-level `Decision Topics`, `Task Authority -> Linked Decisions`, and decision mutation success cards now also reuse one shared decision-authority detail surface, so prompt, answer, capture format, linked task ref, and created/resolved timestamps no longer drift between the live decision list, task inspection, and direct mutation-result surfaces
- board-oriented view via `BoardView.tsx`, no longer hard-truncates the visible durable decision, planning-request, workflow, workflow-root ref, or preference lists to the first few items, so current Goal inspection is no longer silently capped at six decisions, six planning requests, four workflows, or six preferences
- answer-driven success cards in `Decision Topics` and `Answer Bundles` now also expand the returned durable decision authority and follow-through refs, including status/answer/prompt/match/capture/task metadata, blocker-removal outcome, created-in-bundle markers, and follow-through request/task/blocker refs instead of stopping at one-line summaries
- those same answer-driven success cards now also surface the nested direct follow-through runtime result that Bun already returns, including follow-through request detail plus `workflow_batch` child structure, so resolve/answer mutations no longer flatten richer follow-through authority back to top-level key arrays
- direct workflow-batch success cards now also surface the nested direct workflow runtime result that Bun already returns, including workflow-child create detail plus workflow request detail, so direct workflow mutations no longer flatten richer create authority back to top-level key arrays
- board-oriented view via `BoardView.tsx`, now also exposes a dedicated `Task Authority` inspector driven from board task selection, so task inspection includes full description, acceptance criteria, blockers, linked decisions, linked planning requests, linked workflows, and current dependent tasks instead of stopping at card-summary level
- that `Task Authority` inspector now also expands linked decision and linked planning-request cards to show the rest of their current durable authority, including decision `prompt` / `captureFormat`, planning-request description, workflow dependencies, requested updates, captured answers, workflow-shared answers, and created timestamps instead of stopping at key/status summaries
- that `Task Authority` inspector now also expands workflow-membership cards to show the selected task's matching child request authority inside each workflow graph, including durable statuses, the selected task's current status/blockers, grouped task keys, linked and workflow-shared decision refs, acceptance criteria, workflow dependencies, blocker task refs, shared answers, and timestamps instead of stopping at workflow keys and membership counts
- that `Task Authority` inspector now also reads task-scoped durable write traces through the current Goal write-trace API, so task-level tool/step/file-change authority is visible directly inside `BoardView` instead of only through run-level runtime inspection
- that `Task Authority` inspector now also reads the current Goal run list and filters it by selected task, so task-level workflow-run status, step counts, terminal outcomes, and final task status are visible directly inside `BoardView` instead of only through runtime history
- that `Task Authority` inspector now also lets the user select one task-scoped workflow run and inspect its durable step-level runtime detail in place, including per-step status transitions, transcript/message counts, artifact counts, worktree paths, and terminal outcome authority without leaving `BoardView`
- that `Task Authority` inspector now also lets the user drill write traces down to the selected task run and one selected step through the current `runId` / `stepId` write-trace filters, so step-scoped file-write authority stays attached to selected-run inspection instead of getting lost in the broader task trace history
- that `Task Authority` inspector now also expands the selected step's transcript entries, step messages, artifact refs, and worktree branch/base-branch authority in place, so task-scoped run inspection is no longer limited to count-only step summaries before leaving `BoardView`
- session-oriented view via `SessionView.tsx`, now wired to the live Goal run, durable write-trace, and assistant-run inspection APIs, including exact assistant bundle inspection for `context.md`, `prompt.md`, `outcome.json`, and `result.json`
- `SessionView.tsx` now also exposes a dedicated run-step inspector for the selected workflow run and can drill durable write traces down through the existing `runId` / `stepId` filters, so step-scoped file-write authority is visible in the dedicated runtime page instead of only through `BoardView`'s task inspector
- that selected-step inspector now also surfaces transcript timestamps, transcript tool/vendor metadata, step-message timestamps, and worktree branch/base-branch authority instead of flattening those runtime fields into summary-only step detail
- that same `SessionView.tsx` runtime timeline now also shows transcript tool/vendor metadata and worktree branch/base-branch authority for workflow runs and assistant-run events, so those current runtime fields no longer disappear from the scrolling activity log while only surviving in secondary inspectors
- `SessionView.tsx` now also surfaces richer structured assistant action/action-result inspection from those live APIs, including request/task/workflow ids, follow-through metadata, resolved source-response formats, durable decision payloads, durable planning-request payloads, preference mutation detail, and workflow-child result detail without opening raw bundle files
- those `SessionView` assistant run and bundle surfaces now also refetch on live `assistant_changed`, so the selected assistant run detail does not stay stale after a real assistant run completes
- assistant side panel via `AssistantPanel.tsx`, now wired to the live assistant thread, thread-only message append, and assistant run APIs, now also showing structured persisted thread action/action-result detail instead of flattening that thread history down to summary-only text, and now also refetching on live `assistant_changed` so an already-open thread stays current after a real assistant run appends new entries
- that preserved `AssistantPanel` thread surface now also fronts persisted entry timestamps and visible entry counts, so user/assistant/action/result history no longer hides durable thread chronology behind bubble order alone
- that preserved `AssistantPanel` side panel now also fronts recent assistant runs plus one selected run's request/reply/error/count authority and compact structured action/result/event detail, so board-side assistant inspection is no longer limited to thread history alone
- that same preserved `AssistantPanel` selected-run surface now also fronts exact assistant bundle file inspection for `context.md`, `prompt.md`, `outcome.json`, and `result.json`, so board-side assistant inspection no longer needs a `SessionView` hop just to read the current run bundle authority
- when `AssistantPanel` starts a new assistant run, the selected-run inspector now also jumps to that fresh run immediately and seeds its cached run detail before the refetch completes, so board-side inspection no longer stays pinned to the previously selected run after the user launches a new one
- those preserved assistant inspection surfaces now also front the current assistant action capture-authority contract instead of dropping it out of the frontend type layer, so direct answer/resolve actions surface `status`, `summaryKey`, `matchHints`, `answer`, `sourceExcerpt`, `sourceOccurrence`, `answerSourceKey`, `answerSourceGroupKey`, and preference `supersedes`, while shared assistant result formatting now also exposes durable decision `createdAt` plus planning-request `createdAt` and `blockedByWorkflowKeys` in both `SessionView` and `AssistantPanel`
- those same preserved assistant inspection surfaces now also front root planning-workflow reuse authority on assistant actions, so `request_planning_workflows` entries no longer drop `reuseTaskRef` / `reuseGroupKey` while workflow follow-through inspection already shows those reuse refs
- those same preserved assistant inspection surfaces now also front direct planning-workflow child `requestKey` authority on assistant actions, so `request_planning_workflows` entries no longer drop the stable planning-child request identity that backend already accepts for in-place workflow-child reuse
- those same preserved assistant inspection surfaces now also preserve root shared planner-answer semantics on direct grouped-planning and planning-workflow actions plus grouped/workflow follow-through, so assistant run/thread inspection no longer relabels shared answer authority as generic planner answers where the backend contract is explicitly shared across multiple sinks
- those same preserved assistant inspection surfaces now also keep the root planning-family action labels aligned with the live backend inspection contract, so direct planning, grouped planning, and planning-workflow actions now surface `Planning title/description/acceptance`, `Initial/Planning blockers`, `Requested durable updates`, `Grouped requests`, `Workflow count`, and `Action source-response format` instead of one overly generic label set
- those same preserved assistant inspection surfaces now also distinguish reused request/task refs from newly created refs on `request_planning_batch` and `request_planning_workflows` action results, so assistant run/thread inspection no longer collapses planning/workflow result authority down to created arrays while hiding which current sinks were reused
- those same preserved assistant inspection surfaces now also keep decision-family action semantics aligned with the live backend inspection contract, so `request_decision` / `record_answer` / `record_answers` / `resolve_decision` entries now surface `Decision prompt`, direct `Decision answer detail`, explicit multi-answer detail, `Infer open decisions` / `Infer decision topics`, and decision-family `Action source-response format` instead of flattening `record_answers.answers` into planner-answer semantics or weaker generic labels
- those same preserved assistant inspection surfaces now also keep assistant action summaries and follow-through planner-answer labels aligned with the live backend inspection contract, so dedicated runtime logs no longer fall back to shorthand summaries for task/planning/workflow/preference actions and non-shared planning follow-through answers now surface as captured planner answers instead of a weaker generic planner-answer label
- those same preserved assistant inspection surfaces now also derive root workflow shared decision/answer context for answer-driven `workflow_batch` follow-through results from the returned durable planning requests, so `SessionView` and `AssistantPanel` no longer leave that shared workflow authority buried only inside child request detail after the root follow-through graph is already visible
- those same preserved assistant inspection surfaces now also derive root workflow shared decision/answer context for direct `request_planning_workflows` results from the returned durable planning requests, and that shared derivation now also falls back to nested child requests when top-level workflow request arrays are absent, so assistant inspection no longer drops root workflow shared context on direct workflow results or on workflow follow-through variants that only expose child-level requests
- those same preserved assistant inspection surfaces now also keep answer-driven `workflow_batch` child detail wording aligned with the live backend inspection contract, so follow-through child title/description/dependency/grouped-request/planner-answer authority now stays under explicit `Follow-through workflow child ...` labels instead of drifting back to the generic direct-workflow child wording
- `SessionView` now also exposes a selected assistant-run authority card above the runtime log stream, so full terminal `message`, `error`, `requestContent`, and run-level action/event counts no longer stay buried in a clamped list row or the tail end of the log timeline
- top-level `StructuredAnswerSourcesEditor` setup/reuse gating now also dedupes on a fuller shared answer-source identity, using stable `answerSourceKey + sourceGroupKey` when present and falling back to semantic source fields when they are not, so preserved-frontend reusable answer-source flows no longer silently collapse grouped or unkeyed source variants down to one weak source key path
- those same shared structured planning-answer and batch decision-answer editors now also dedupe unkeyed reusable entries on a fuller semantic identity instead of falling back to bare `summary`, so higher-order current-answer setup and bulk reusable-answer flows no longer silently collapse distinct unkeyed answers that happen to share the same summary text
- those same grouped planning-sink and advanced workflow-child reusable flows now also canonicalize structured string-list fields like workflow dependencies, decision refs, acceptance criteria, requested updates, and grouped blocker task keys before running `Selected` / `Added` / append-all dedupe checks, so preserved-frontend higher-order planner/workflow reuse no longer treats `a,b` and `a\nb` as different payloads when the durable semantics are the same
- the shared `StructuredStringListEditor` substrate now also enforces one trimmed duplicate-free identity across manual add, per-item edit, and bulk reuse flows, so decision refs, workflow dependencies, grouped task dependencies, requested updates, acceptance criteria, match hints, and superseded preference keys no longer admit duplicate durable values through the structured editor on one path while rejecting them on another
- direct workflow-batch success cards now also surface one root workflow-authority block for the returned direct workflow result, including workflow key, group keys, request/task refs, created-vs-reused refs, and blocker task refs instead of keeping that create/reuse authority only on child rows
- those same direct workflow-batch success cards now also surface the returned root workflow shared context, including `workflowSharedDecisionRefs` and `workflowSharedAnswers`, so preserved-frontend direct workflow-create inspection no longer drops shared decision/answer authority that is already present on the live backend result
- `Workflow Graphs` list cards now also surface root workflow shared decisions/answers, grouped children, current tail blockers, and compact child summaries directly on each visible workflow card instead of keeping that workflow authority only behind the selected-detail panel
- those same `Workflow Graphs` list cards now also surface child request status, grouped request status, child-level decision refs, captured planner answers, and standalone planning-child group keys directly inside the existing compact child summaries, so visible workflow-card inspection no longer hides that durable child authority until the user opens selected detail
- those same workflow list/detail surfaces now also refetch on board-only task changes and other local board-affecting mutations like direct planning, task create/move, reconcile, or decision-create, so current tail blockers, child tail summaries, and selected workflow detail no longer stay stale after `board_changed`-only updates
- `AssistantPanel` now also mirrors the backend assistant-run refresh contract on local run success and closes the local `retire_preference` stale-state gap, so board-side assistant-triggered decision/planning/workflow/doc/runtime/preference cache refresh no longer waits entirely on SSE before the preserved frontend updates
- direct decision and direct workflow success surfaces now also preserve the Bun route's explicit create-vs-reuse authority, so `Decision Topics` distinguishes reused open topics from newly opened ones and direct `Workflow Graphs` now distinguishes `Created workflow graph`, `Created workflow requests`, and `Created workflow tasks`, while also deriving root and child group-key create-vs-reuse authority instead of conflating route-level `201/200` with request creation or leaving group reuse implicit
- direct `Workflow Graphs` and answer-driven `workflow_batch` root reuse suggestions now also target the real current open planning surfaces instead of a workflow-only subset, so `reuseTaskRef` can reuse any visible open planning task, `reuseGroupKey` can reuse any visible open grouped planning sink, and resolved planning requests no longer leak stale group keys into those workflow-root suggestion lists
- those same root workflow reuse suggestion lists now also prioritize planning-surface previews over workflow-only previews when one current task or group belongs to both, so `reuseTaskRef` / `reuseGroupKey` no longer show misleading workflow-only subtitles for values that runtime actually reuses as current planning surfaces
- workflow-child `workflowTaskKey` and `blockedByWorkflowKeys` surfaces now also scope reusable child identity suggestions to the current durable workflow graph selected by explicit `workflowKey`, so preserved-frontend workflow-child authoring no longer fronts cross-workflow standalone child keys or dependency keys that the backend would reject or misinterpret outside the current workflow; `workflowTaskKey` reuse now comes only from the current graph's standalone children, dependency reuse now still derives child sink identities from that same graph with `workflowTaskKey` for `planning` children and `groupKey` for `planning_batch` children, and the advanced workflow-child editors now also expose same-draft earlier sibling child keys as dependency suggestions so later draft children no longer require hand-typing stable dependency keys that backend already accepts within one submission
- direct `Workflow Graphs` and answer-driven `workflow_batch` follow-through now also front-load backend workflow authoring validation, so submit gating and inline notices catch invalid root reuse combinations like `reuseTaskRef + reuseGroupKey`, root reuse that points at a current planning task or grouped sink that no longer exists, grouped reuse whose first child is not the matching `planning_batch`, and children that try to use `blockedByWorkflowKeys` without a stable `workflowTaskKey` / `groupKey`, depend on missing current-graph or earlier-sibling child keys, depend on themselves, or introduce workflow dependency cycles instead of waiting for runtime mutation failure
- those same workflow authoring notices now also front-load the grouped-sink reuse rule for empty `planning_batch` children, so direct and answer-driven workflow mutations no longer allow an empty grouped-request payload unless the first `planning_batch` child is actually consuming the root `reuseGroupKey` surface that backend will reuse
- the shared `planning_batch` request parser/editor substrate now also front-loads backend batch-level dependency validation, so structured batch-request authoring rejects duplicate `taskKey` values, self-dependencies, and intra-batch dependency cycles before direct planning-batch, workflow-child, or answer-driven grouped-request mutations hit runtime rejection
- those same shared batch-request surfaces now also front-load backend task-blocker existence on grouped requests, so structured grouped-request authoring rejects missing task blocker refs across the batch editor, direct workflow `planning_batch`, and answer-driven grouped-request follow-through instead of waiting for runtime `Task blocker not found`
- the shared decision-answer parser/editor substrate now also front-loads backend answer-batch identity validation, so structured decision-answer authoring rejects duplicate stable `decisionKey` rows before `record_answers` bundle mutations hit runtime `Duplicate decision key in answer batch` rejection
- the batch `Answer Bundles` shared-reply surface now also front-loads backend `inferOpenDecisions` decision-key authority, so explicit decision-answer rows without stable `decisionKey` are blocked in the preserved frontend instead of failing only when runtime tries to match them back onto current open decisions
- direct `resolve decision` and single-answer `Answer Bundles` now also accept explicit answer authority that comes only from `answerSourceKey` or `answerSourceGroupKey`, while front-loading the backend rule that those two reusable-source authorities are mutually exclusive on one explicit answer, so preserved frontend no longer rejects source-backed single-answer flows that runtime already accepts
- the shared planning-answer parser/editor substrate now also front-loads backend planning-answer merge-conflict validation, so structured planner-answer authoring rejects answerKey summary conflicts, same-row summaryKey/answerKey conflicts, and answerKey-vs-value identity mismatches before planning-request, workflow, or follow-through mutations hit runtime merge rejection
- those same shared planning-answer and decision-answer parsers now also allow explicit rows that are backed only by `answerSourceKey` or `answerSourceGroupKey`, while front-loading the backend rule that those two reusable-source authorities are mutually exclusive on one answer row, so preserved frontend no longer rejects source-backed explicit planner/decision answers that the runtime already accepts
- those same decision/planning/workflow shared-answer surfaces now also front-load backend answer-source reference existence, so explicit answer rows that rely only on `answerSourceKey` or `answerSourceGroupKey` are blocked when the current structured `answerSources[]` payload does not define that key/group instead of failing only inside runtime `resolveAnswerContent(...)`
- the direct `Planning Requests`, direct `Workflow Graphs`, and batch `Answer Bundles` infer-topic surfaces now also front-load remaining answer-source summary authority on the exact answer-source-only paths where all current sources are provably still remaining, so planner-answer and inferred decision-topic mutations no longer wait for runtime to reject leftover source entries that lack any visible summary authority like `summary`, `prompt`, `decisionKey`, `summaryKey`, one stable `matchHint`, or a stable suffixed `answerSourceKey`
- those same exact remaining answer-source paths now also front-load backend non-contiguous repeat rejection, so answer-source-only `inferRemainingAnswers` and exact `inferDecisionTopics` routes now block repeated `decisionKey` / `answerKey` / `summaryKey` / fallback `answerSourceKey` groups that reappear after another remaining source block instead of waiting for runtime `Non-contiguous ... answerSources repeated ...` failures
- those same exact remaining answer-source paths now also front-load contiguous-group merge conflicts, so answer-source-only `inferRemainingAnswers` and exact `inferDecisionTopics` routes now reject repeated remaining source groups whose merged `route`, `decisionKey`, `answerKey`, `summaryKey`, normalized `summary`, `prompt`, or `sourceGroupKey` metadata disagree instead of waiting for runtime `Conflicting ... values in answerSources ...` failures
- batch `Answer Bundles` exact infer-topic answer-source paths now also front-load known-decision summary ambiguity when the frontend can directly infer the remaining topic summary from stable source authority, so those bundle routes no longer wait for runtime `Multiple existing decisions match inferred answerSource summary ...` failures when current durable decisions already contain multiple matches for the same inferred remaining summary
- the shared answer-source parser/editor substrate now also front-loads backend reusable-source batch validation, so structured `answerSources[]` authoring rejects duplicate stable `answerSourceKey` rows plus conflicting grouped `sourceGroupKey` route/decisionKey/answerKey/summaryKey/summary/prompt metadata before shared-answer mutations hit runtime answer-source merge rejection
- answer-driven follow-through success cards now also derive create-vs-reuse authority from the current durable board/planning/workflow snapshot, so direct `resolve decision` and `Answer Bundles` no longer flatten `planning` / `planning_batch` / `workflow_batch` follow-through results down to raw refs while hiding which requests, tasks, groups, and workflow roots were newly created versus reused
- those same answer-driven `workflow_batch` follow-through success cards now also derive and surface root workflow shared context from the returned durable planning requests, so preserved-frontend answer-driven workflow inspection no longer leaves shared decision/answer authority buried only inside child request cards after root create/reuse authority is already visible
- that selected assistant-run authority card now also fronts structured `Assistant Actions` and `Assistant Results` lists in place, so run-scoped assistant inspection no longer depends on scanning the monolithic runtime log stream just to read the action/action-result authority for one selected assistant run
- that selected assistant-run authority card now also fronts a structured `Assistant Events` list in place, so run-scoped assistant inspection no longer depends on scanning the monolithic runtime log stream just to read one selected run's message/transcript/worktree/artifact event authority
- those same preserved assistant run summaries and selected-run panels now also keep assistant-run wording aligned with the live backend inspection contract, so `actionCount` is no longer mislabeled as action results and the dedicated runtime-side run detail now uses backend-consistent `Request` / `Reply` / `Actions` / `Action Results` / `Runtime Events` labels instead of drifting to a separate frontend-only vocabulary
- `Save Preference Document` now also surfaces the full returned preference-entry authority instead of only a document count summary, so direct preference-document saves no longer hide the updated durable `summary` / `status` / `rationale` / supersession state that the backend already returns
- board-oriented success cards for direct workflow batches and answer-driven workflow follow-through now also preserve child-level request grouping inside each returned workflow child, including full request description, acceptance criteria, shared-answer context, workflow dependencies, and timestamps instead of flattening that richer workflow result back into one request list
- those same direct workflow-batch success cards now also distinguish reused request/task refs from newly created refs at both the workflow root and each returned child, so the returned durable workflow graph no longer collapses down to created counts while hiding which current request/task sinks were reused
- board-oriented success cards for direct planning-request mutations now also reuse the full request-authority renderer, including request description, acceptance criteria, workflow-shared answer context, workflow dependencies, and timestamps instead of only surfacing a selective subset of the returned durable request fields
- original styling/theme direction preserved as the current UI baseline

Frontend work still missing inside that baseline:

- surface the remaining current backend product capabilities, especially the remaining interpretation-backed planner/workflow authoring paths and higher-order shared-answer flows, without copying backend UI wholesale

Current non-UI Goal assistant substrate:

- durable Goal decisions in `decisions.yml`
- durable decision-prompt backfill that lets stronger later request/answer/resolve surfaces persist a missing canonical question onto an existing decision without overwriting an already-recorded prompt
- durable Goal planning requests in `planning-requests.yml`
- decision-linked planning request metadata for explicit `design.md` / `todo.yml` reshape intent
- shared decision-request flows that backfill planning request lineage and default requested updates when a planning task becomes visibly blocked by one decision
- engineering-linked decision resolution with explicit single or grouped planner follow-through on the shared planning-request path
- planning-linked decision resolution with explicit single or grouped follow-through that can reuse the current planning surface instead of creating a wrapper task
- standalone answered decision follow-through that can create visible planner work even before there is a blocker or reusable planning surface
- answer-first durable decision capture that lets assistant or API record one explicit answer, auto-create or reuse the durable decision topic, and route single or grouped planner follow-through through the same shared resolution helper
- higher-order answer follow-through that lets one durable decision answer fan out into multiple independent planner workflows without introducing another durable workflow store
- multi-decision answer capture that lets one user answer resolve several durable decision topics and attach one shared planner follow-through with combined decision lineage
- answer-backed planning requests that let assistant or API capture non-decision user answers directly on durable planning follow-through without inventing synthetic decision topics
- mixed answer follow-through that lets one durable decision-answer action also carry non-decision captured answers directly onto the same planner follow-through
- direct higher-order planning workflows that let assistant or API atomically open multiple independent durable planning workflows without routing through a decision-answer action first
- direct higher-order planning reuse that lets assistant or API expand one current planning surface into the first child of a richer workflow batch instead of spawning a wrapper task
- direct higher-order planning blocker propagation that keeps engineering blocked on every current sink when a reused planning blocker expands into a workflow batch
- direct higher-order planning extension that lets assistant or API add new child workflows onto one durable `workflowKey` and reconstruct the full current workflow state
- direct higher-order planning child reuse that lets assistant or API update one standalone child inside a durable direct workflow through stable `workflowTaskKey` instead of low-level request ids or title collisions
- direct higher-order planning child dependencies that let assistant or API wire one durable workflow child onto another child’s current sink through stable `blockedByWorkflowKeys`
- direct higher-order planning shared context that lets assistant or API carry one workflow-root set of shared `decisionRefs` and captured `answers` across every child in a durable direct workflow graph
- decision-backed workflow-graph follow-through that lets resolve_decision, record_answer, and record_answers reuse that same durable direct-workflow graph authority instead of looping through weaker answer-local multi-workflow logic
- decision-backed workflow shared-answer context that lets resolve_decision, record_answer, and record_answers carry one workflow-root set of shared non-decision captured answers across every child in an answer-driven workflow graph
- durable workflow-root shared-context persistence that lets both direct and answer-driven workflow graphs extend one `workflowKey` later without restating the same shared decision lineage or captured answers
- independent workflow-graph inspection that lets Bun API and UI read one current workflow graph by `workflowKey` with root shared context plus child request detail, instead of only exposing raw planning requests or earlier mutation results
- decision answer/resolve API surfacing that returns the full shared runtime result, including `blockerRemoved`, creation metadata, and generated workflow-graph keys, instead of trimming authority down to decision-only bodies
- assistant decision action-result surfacing that returns the same shared runtime follow-through structure, including generated workflow keys, instead of flattening decision follow-through into lossy summary arrays
- leading topic-summary interpretation that lets `topic_sentences`, `topic_paragraphs`, and `topic_blocks` infer durable decision-topic summaries and planner-answer summaries from leading phrases like `Pilot scope should ...`, instead of only from narrower trailing `... for pilot scope` forms
- prefixed topic-summary interpretation that lets those same topic surfaces infer durable summaries from phrases like `For auth strategy, ...` or `About pilot scope, ...`, instead of only from subject-leading or trailing-topic forms
- `as <topic>` summary interpretation that lets those same topic surfaces infer durable summaries from phrases like `Use Bun-native auth as the auth strategy.`, instead of only from leading, prefixed, or trailing-topic forms
- topic-span interpretation that lets one anchored topic sentence carry later continuation sentences without forcing blank-line paragraph or block boundaries
- question-closing-span interpretation that lets one later question sentence close a multi-sentence explanation without forcing the question sentence to appear first
- topic-closing-span interpretation that lets one later topic-bearing sentence close a multi-sentence explanation without forcing the topic-bearing sentence to appear first
- topic-closing-block interpretation that lets one later topic-bearing paragraph close a multi-paragraph explanation without forcing the topic-bearing paragraph to appear first
- transcript tool-correlation evidence that persists stable tool invocation keys and stable tool target details, letting reviewer/merger context see real tool interactions instead of only flat transcript summaries
- durable structured repo preferences in `.hopi/preference.md`
- Goal-scoped assistant thread storage under `.hopi/runtime/**`
- Goal-scoped assistant thread storage now also persists structured assistant `action` and `action_result` payloads instead of only lossy summary entries, and shared inspection helpers now surface the richer durable metadata already embedded in those actions
- assistant `request_decision` result authority that preserves and surfaces structured `created`, `blockerAdded`, and `decisionStatus` metadata instead of forcing assistant run/thread inspection to recover that state from free-text summaries
- assistant planning-result creation authority that preserves and surfaces structured `created` / `taskCreated` and `createdRequestKeys` / `createdTaskRefs` metadata for planning, grouped-planning, and planning-workflow actions instead of forcing inspection surfaces to infer “what this mutation created” from summary text
- deterministic Goal doc bootstrap plus status inspection for `goal.md` and `design.md`
- planner context wiring for Goal docs, decisions, planning requests, and preferences
- explicit Goal assistant execution with constrained durable actions, including grouped planning requests, `request_decision`, decision resolution with embedded planner follow-through, and durable preferences
- reviewer/merger context correlation across prior run history, artifact refs, transcript summaries, and write traces
- planning reviewer/merger follow-through policy grounded in planning requests and durable planning write traces
- scheduler hard guards that retry planning review/merge work when explicit requested updates still lack durable trace coverage
- grouped-planning blocker propagation that keeps engineering waiting on the current open grouped leaves instead of stale earlier grouped tasks
- contiguous explicit anchor-run merging that lets `question_*` and `topic_*` answer surfaces merge one known consumer across adjacent repeated matching units while still failing closed on non-contiguous repeats
- reusable answer-source decision-key authority that lets `matching_answer_sources` target an existing durable decision by explicit `decisionKey`, and lets remaining reusable source entries materialize a brand-new durable decision topic from that same explicit durable key without restating summary text
- contiguous matching answer-source merging that lets `matching_answer_sources` merge one known decision or planner-answer consumer across adjacent reusable source entries while still failing closed on non-contiguous repeats
- contiguous remaining matching answer-source merging that lets leftover `matching_answer_sources` entries merge one brand-new durable decision topic or one inferred planner-answer row across adjacent reusable source entries when those leftovers already share explicit key-based authority, while still failing closed on non-contiguous repeats
- explicit pending answer-source key authority that lets `pending_answer_sources` stop blindly consuming ordered reusable source entries once those entries already carry explicit durable keys, so adjacent entries can merge into one known decision or planner-answer consumer and wrong-key or non-contiguous repeats fail closed instead of being silently misassigned by order
- contiguous remaining pending answer-source merging that lets leftover `pending_answer_sources` entries merge one brand-new durable decision topic or one inferred planner-answer row across adjacent reusable source entries when those leftovers already share explicit key-based authority, while still failing closed on non-contiguous repeats
- mixed answer-source route inference that lets one reusable `answerSources` bundle simultaneously create brand-new durable decision topics and brand-new inferred planner-answer rows, when the leftover sources are explicitly routed either by per-source `route` metadata or by durable `decisionKey` / `answerKey` authority, so runtime still never has to guess the side
- answer-source route family boundaries that make explicit per-source `route` metadata a real hard boundary during known consumer matching too, so route-scoped reusable sources cannot be silently consumed by the wrong decision/planning family before leftover inference even starts
- unreachable answer-source route fail-closed semantics that reject decision-only or planning-only mutations when an explicitly routed reusable source entry remains unused after materialization, so route-scoped answer sources cannot be silently ignored just because the current surface lacks a valid consumer on that family
- repeated source-excerpt occurrence authority that makes direct item excerpts and reusable answer-source excerpts fail closed unless a repeated exact substring is disambiguated by explicit `sourceOccurrence`, so excerpt grounding no longer silently picks the first repeated match
- explicit answer-source group-key authority that lets reusable `answerSources` merge one materialized decision or planner answer across non-contiguous source entries through stable `sourceGroupKey`, while keeping consumer selection on existing `decisionKey`, `answerKey`, `summaryKey`, prompt, hint, or route authority instead of weaker regrouping heuristics
- explicit grouped answer-source references that let one decision answer or planner answer consume that already-merged grouped reusable answer through stable `answerSourceGroupKey`, without overloading single-fragment `answerSourceKey` semantics
- opening-anchored matching-run interpretation that lets one less-structured shared reply materialize explicit decision answers, explicit planner answers, and open decisions when each already-known consumer appears once at the start of its answer stretch, with at least one trailing continuation unit after that anchor
- closing-anchored matching-run interpretation that lets one less-structured shared reply materialize explicit decision answers, explicit planner answers, and open decisions when each already-known consumer appears once at the end of its answer stretch, with at least one leading continuation unit before that anchor
- middle-anchored matching-run interpretation that lets one less-structured shared reply materialize explicit decision answers, explicit planner answers, and open decisions when each already-known consumer appears once in the middle of its answer stretch, with at least one leading and trailing continuation unit around that anchor
- auto generic-run fallback precision that lets `sourceResponseFormat: "auto"` keep probing deterministic opening/closing generic-run surfaces when earlier topic-family probes only matched by durable prompt keywords and never established explicit topic authority, while still fail-closing on real explicit topic authority
- topic-unit ambiguity fail-closed semantics that keep `topic_clauses`, `topic_sentences`, `topic_paragraphs`, and the explicit `topic_*` anchor families from silently swallowing a second inferable topic summary once one decision or planner consumer has already matched the same reply unit, so leftover `inferDecisionTopics`, `inferRemainingAnswers`, and `auto` no longer succeed through over-consumed topic units
- canonical question-clause interpretation that lets `question_clauses` keep explicit clause-separated question authority on narrow `What should <summary> be` clauses even without trailing `?`, while normalizing those prompts to the same canonical durable question used by embedded span inference
- canonical question-sentence interpretation that lets `question_spans`, `question_middle_spans`, and `question_closing_spans` keep explicit sentence-bounded question authority on narrow `What should <summary> be` sentences even without trailing `?`, while normalizing those prompts to the same canonical durable question used by embedded span inference
- canonical question-paragraph interpretation that lets `question_blocks`, `question_middle_blocks`, and `question_closing_blocks` keep explicit blank-line question authority on narrow `What should <summary> be` paragraphs even without trailing `?`, while normalizing those prompts to the same canonical durable question used by embedded span inference
- canonical stable-prompt interpretation that lets reusable `answerSources` infer summaries from narrow `What should <summary> be` prompts even when those prompts end with `.`, `。`, `!`, `！`, or no punctuation, instead of only recognizing canonical stable prompts that ended with `?` or `？`
- fullwidth punctuation interpretation that lets explicit sentence- and clause-bounded surfaces keep deterministic authority when an otherwise ordinary reply uses fullwidth punctuation like `？`, `。`, `，`, or `；`, instead of only partially recognizing those marks on question detection and then failing later in the shared splitters or trailing-summary parsers
- fullwidth explicit-label separator interpretation that lets `labeled_sections` and `inline_topics` keep deterministic label/value authority when an otherwise ordinary reply uses non-ASCII separators like `：`, `＝`, `－`, `—`, `–`, or `→`, instead of skipping the intended explicit parser and leaking those separator characters into weaker inline verbal summaries
- explicit-label list-marker prefix interpretation that lets `labeled_sections` and `inline_topics` strip leading list markers like `・`, `●`, or `→` before durable label inference, instead of successfully parsing those explicit surfaces while silently persisting the presentation marker itself as part of the semantic summary
- question/topic list-marker prefix interpretation that lets explicit `question_*` and `topic_*` anchor surfaces strip the same leading list markers like `・`, `●`, or `→` before deterministic question/topic normalization, instead of letting presentation-only markers block explicit question authority or leak into durable prompts and summaries
- numbered list-marker prefix interpretation that lets explicit `labeled_sections`, `question_*`, and `topic_*` surfaces treat narrow prefixes like `1.` or `2)` as presentation-only numbering, and lets the shared sentence splitter discard standalone numbering tokens before explicit anchor parsing, instead of leaking those numbers into durable summaries/prompts or blocking the stronger explicit surface entirely
- parenthesized numbered list-marker interpretation that extends that same presentation-only numbering boundary to prefixes like `(1)` or `（2）`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat parenthesized numbering as semantic label/question/topic text or miss the stronger explicit surface because of it
- markdown presentation-prefix interpretation that extends that same presentation-only boundary to blockquotes and checkboxes like `>`, `- [x]`, or `[ ]`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those markdown affordances as semantic label/question/topic text or miss the stronger explicit surface because of them
- plus-bullet and alphabetic-enumerator prefix interpretation that extends that same presentation-only boundary to `+`, `A.`, `a)`, `(a)`, and `（a）`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those outline markers as semantic label/question/topic text or miss the stronger explicit surface because of them
- common Roman-outline prefix interpretation that extends that same presentation-only boundary to markers like `II.`, `III)`, `(iv)`, and `（iv）`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those outline markers as semantic label/question/topic text, leave standalone Roman fragments behind in sentence parsing, or miss the stronger explicit surface because of them
- common Unicode-bullet prefix interpretation that extends that same presentation-only boundary to glyphs like `○`, `◦`, and `▪`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those bullet glyphs as semantic label/question/topic text or miss the stronger explicit surface because of them
- markdown-heading prefix interpretation that extends that same presentation-only boundary to heading markers like `#`, `##`, and `###`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those heading markers as semantic label/question/topic text or miss the stronger explicit surface because of them
- symbolic Unicode-bullet prefix interpretation that extends that same presentation-only boundary to glyphs like `◆`, `■`, `□`, `▸`, `▹`, `⁃`, and `‣`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those bullet glyphs as semantic label/question/topic text or miss the stronger explicit surface because of them
- fullwidth-and-circled-number prefix interpretation that extends that same presentation-only boundary to numbering markers like `１．`, `②`, and `（３）`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those numbering markers as semantic label/question/topic text or miss the stronger explicit surface because of them
- ideographic-comma and CJK-number prefix interpretation that extends that same presentation-only boundary to numbering markers like `1、`, `２、`, `一、`, and `（三）`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those numbering markers as semantic label/question/topic text or miss the stronger explicit surface because of them
- dash-style Unicode-bullet prefix interpretation that extends that same presentation-only boundary to glyphs like `–`, `—`, and `―`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those dash glyphs as semantic label/question/topic text or miss the stronger explicit surface because of them
- arrowhead Unicode-bullet prefix interpretation that extends that same presentation-only boundary to glyphs like `▶`, `▷`, `►`, and `▻`, so explicit `labeled_sections`, `question_*`, `topic_*`, and `auto` no longer treat those arrowhead glyphs as semantic label/question/topic text or miss the stronger explicit surface because of them
- ordered-family presentation-prefix alignment that extends that same shared presentation-only boundary onto `ordered_items` and every paragraph of `ordered_blocks`, so sequence-based answer capture and `auto` fallback no longer persist those markers into durable ordered answer text just because the reply used order authority instead of explicit question/topic anchors
- generic pending/matching presentation-prefix cleanup that extends that same shared presentation-only boundary onto `pending_clauses`, `pending_paragraphs`, `pending_sentences`, `pending_conjunctions`, and `matching_runs`, so generic pending-order or repeated-consumer capture no longer persists those markers into durable answer text or makes known-consumer run matching depend on prefixed non-semantic text
- embedded matching-run presentation-boundary cleanup that extends that same presentation-only boundary onto embedded `matching_opening_runs`, `matching_closing_runs`, and `matching_middle_runs`, so one-sentence embedded generic-run capture no longer leaks leading boundary markers into the current run or separator-only markers into the previous run just because those variants slice answer text directly from the raw source response
- embedded question/topic span presentation-boundary cleanup that extends that same presentation-only boundary onto embedded `question_spans`, `question_middle_spans`, `question_closing_spans`, `topic_spans`, `topic_middle_spans`, and `topic_closing_spans`, so one-sentence embedded question/topic capture no longer leaks leading boundary markers into the current span or separator-only markers into the neighboring span just because those variants slice answer text directly from the raw source response
- single-pending presentation-prefix cleanup that extends that same shared presentation-only boundary onto explicit `single_pending`, so one-reply generic pending capture no longer persists presentation-only prefixes into durable whole-answer text just because the caller requested the weakest single-reply surface
- explicit non-embedded unit presentation-prefix cleanup that extends that same shared presentation-only boundary onto non-embedded `topic_clauses`, `topic_sentences`, `topic_paragraphs`, the sentence- and paragraph-backed explicit `topic_*` and `question_*` families, plus `question_blocks` and `question_clauses` answer text, so deterministic explicit capture no longer persists presentation-only prefixes just because those variants materialize normalized units or question-answer suffixes instead of embedded raw slices
- explicit label-answer presentation-prefix cleanup that extends that same shared presentation-only boundary onto `labeled_sections` and `inline_topics` answer-side values, so explicit label/value capture no longer persists presentation-only prefixes just because earlier cleanup only sanitized the label side before extracting the durable answer text
- ordered-block continuation presentation-prefix cleanup that extends that same ordered-family boundary onto every paragraph inside `ordered_blocks`, so deterministic ordered block capture no longer persists presentation-only prefixes just because earlier ordered cleanup only sanitized the first paragraph of each block
- label-family empty-inference fail-closed semantics that keep `labeled_sections` and `inline_topics` from degrading into the generic top-level no-op when `inferDecisionTopics` is enabled but the reply contains no parseable labels at all, so an explicitly chosen label surface now reports that zero-item mismatch directly
- label-family open-decision fail-closed semantics that keep `labeled_sections` and `inline_topics` from degrading into the generic top-level no-op when `inferOpenDecisions` is enabled but the reply contains zero parseable labels or only unmatched labels, so an explicitly chosen label surface now reports that zero-item or zero-match mismatch directly
- open-decision no-match fail-closed semantics that keep explicit `question_*` and `topic_*` `inferOpenDecisions` surfaces from degrading into the generic top-level no-op when the reply parses but none of the current open decisions match that parsed authority, so the already-chosen deterministic surface now reports its own mismatch directly
- open-decision topic-unit ambiguity fail-closed semantics that keep `inferOpenDecisions` from letting `topic_clauses`, `topic_sentences`, or `topic_paragraphs` swallow a second explicit topic summary inside one matched unit, so one open decision can no longer consume another still-unmatched topic just because both appeared in the same sentence or paragraph and `auto` can no longer succeed through that over-consumed unit either
- auto anchor no-match fail-closed semantics that keep `sourceResponseFormat: "auto"` from dropping explicit `question_*`, embedded `topic_spans` / `topic_middle_spans` / `topic_closing_spans`, or already-established multi-clause `topic_clauses` authority to weaker generic fallback once those families have already parsed real anchor-bearing units for the wrong open decision, while still allowing coarse inferred-label topic surfaces like `topic_sentences`, `topic_paragraphs`, `topic_blocks`, and `topic_closing_blocks` to fall through when they only inferred one broad label from raw text and a stronger later span surface has not yet attached
- explicit-topic versus inline-topic auto-selection semantics that keep `sourceResponseFormat: "auto"` from stopping at weaker `inline_topics` label/value parsing when a reply is already carrying stronger leading, trailing, or copular explicit topic authority, so decision-bundle and planning-input auto capture now preserve full topic-anchored answer text and no longer let copular topic sentences fail closed through the wrong inline label
- middle-anchor anchorless-reply fail-closed semantics that keep `question_middle_spans`, `question_middle_blocks`, `topic_middle_spans`, and `topic_middle_blocks` from degrading into generic top-level no-op failures when the reply contains no middle anchor at all, so an explicitly chosen strong middle-anchor surface now rejects at the parser boundary instead of pretending to parse zero stretches
- topic-block leading-prose fail-closed semantics that keep deterministic `topic_blocks` from silently dropping loose paragraphs before the first anchored topic paragraph, so explicit anchored multi-paragraph capture now follows the same “start with real anchor or reject” rule as other stronger anchor families
- fullwidth prefixed-topic anchor interpretation that lets explicit topic surfaces keep inferring deterministic summaries from narrow `For <summary>： ...` or `About <summary>， ...` anchors, instead of recognizing the surrounding fullwidth sentence punctuation but still rejecting those anchors because prefixed-topic summary extraction only accepted ASCII separators
- topic-summary noun-phrase punctuation interpretation that lets explicit topic surfaces keep inferring deterministic summaries from noun phrases like `team's auth strategy` or `rollout/rollback strategy`, instead of failing closed on prefixed, trailing, or `as`-topic anchors or misparsing copular topic sentences as answer-first labels because those shared extractors only accepted bare alphanumerics plus spaces, underscores, and hyphens
- question-clause leading-conjunction interpretation that lets later explicit `question_clauses` keep deterministic question authority after narrow conjunctions like `and` or `but`, instead of leaking those conjunctions into inferred prompt/summary text or failing canonical `What should <summary> be` clause parsing whenever the later clause did not start at the first token
- question-clause additional-anchor fail-closed semantics that keep deterministic `question_clauses` from silently swallowing a second explicit or canonical question anchor into the first clause answer text, so explicit decision answers, `inferOpenDecisions`, and planner follow-through now reject over-consumed clause replies instead of materializing dirty durable answers
- labeled-section nested-sentence fail-closed semantics that keep deterministic `labeled_sections` from silently swallowing a later explicit label/value sentence into the first line’s value text, so explicit decision answers, `inferOpenDecisions`, planner follow-through, and `sourceResponseFormat: "auto"` now reject malformed explicit-label lines instead of materializing dirty durable values or falling through to weaker surfaces
- labeled-section same-summary nested-label fail-closed semantics that keep deterministic `labeled_sections` from treating value text like `Auth strategy: Auth strategy: ...` as a dirty mixed-authority value, so direct explicit answers, `inferDecisionTopics`, and `sourceResponseFormat: "auto"` now stop at the tighter parser boundary where the value immediately started another same-summary explicit label/value pair
- explicit topic-answer ambiguity fail-closed semantics that keep direct explicit decision answers and direct explicit planner answers from silently swallowing another topic summary inside one matched `topic_sentences`, `topic_paragraphs`, `topic_blocks`, `topic_middle_blocks`, or `topic_closing_blocks` unit, so `sourceResponseFormat: "auto"` no longer succeeds through those over-consumed direct-answer paths either
- comma-delimited paragraph topic ambiguity fail-closed semantics that make paragraph-backed topic-summary inference combine whole-paragraph and clause-local topic extraction, so `topic_paragraphs`, `topic_blocks`, `topic_closing_blocks`, remaining decision-topic inference, and remaining planner-answer inference no longer flatten one comma-delimited multi-topic paragraph into one durable topic while still preserving valid single-topic prefixed paragraphs like `About pilot scope, ...`
- question-family auto terminal fail-closed semantics that make the final `question_clauses` auto probe treat any remaining explicit or canonical question-sentence authority as terminal, so malformed question-paragraph replies can no longer fall through from failed `question_blocks` / `question_closing_blocks` / `question_middle_blocks` probing into weaker `ordered_blocks` or `topic_blocks` interpretations
- incomplete topic-predicate fail-closed semantics that keep leading explicit topic authority from materializing when it stops at the predicate without real answer content, so direct `topic_*` capture, remaining decision-topic inference, remaining planner-answer inference, and `sourceResponseFormat: "auto"` no longer accept dirty fragments like `rollout strategy should.` as valid topic answers
- question-answer topic-authority mismatch fail-closed semantics that keep explicit `question_clauses`, `question_blocks`, `question_spans`, `question_middle_spans`, `question_closing_spans`, `question_middle_blocks`, and `question_closing_blocks` from materializing answers whose answer text itself establishes explicit topic authority for a different summary, so question-family direct answers, open-decision matching, remaining decision-topic inference, remaining planner-answer inference, and `sourceResponseFormat: "auto"` no longer misattribute cross-topic replies like `What should rollout strategy be? For auth strategy, use Bun-native auth.`
- question-answer same-summary topic-authority fail-closed semantics that keep question-family surfaces from materializing answers whose answer text itself re-establishes explicit topic authority for the same summary, so direct question-family answers plus decision/planning `sourceResponseFormat: "auto"` no longer persist mixed-authority replies like `What should auth strategy be? Use Bun-native auth for auth strategy.`
- question/topic cross-authority fail-closed semantics that keep explicit `topic_clauses`, `topic_spans`, `topic_paragraphs`, `topic_blocks`, `topic_middle_blocks`, and `topic_closing_blocks` from materializing answer text that already carries explicit or canonical question authority, and that also wire the same conflicting-topic guard onto embedded canonical `question_spans`, `question_middle_spans`, and `question_closing_spans`, so direct explicit answers, remaining decision-topic inference, remaining planner-answer inference, and `sourceResponseFormat: "auto"` no longer materialize mixed question/topic replies through the wrong deterministic surface
- label-family cross-authority fail-closed semantics that keep `labeled_sections` and `inline_topics` from materializing answers whose value text itself carries explicit topic authority for another summary or still contains explicit/canonical question authority, so direct explicit label answers, remaining decision-topic inference, open-decision matching, and `sourceResponseFormat: "auto"` no longer accept malformed mixed-authority label replies through the wrong deterministic surface
- label-family label-authority fail-closed semantics that keep `labeled_sections` and separator-style `inline_topics` from treating label text that already carries question or topic authority as a valid durable summary, so direct label-family inference plus decision/planning `sourceResponseFormat: "auto"` no longer create dirty summaries like `What should auth strategy be` or `Auth strategy should rollout strategy`
- labeled-section standalone-question auto-terminal semantics that keep decision-bundle `sourceResponseFormat: "auto"` from stopping at `labeled_sections` when standalone explicit or canonical question authority still exists on non-section lines before or after those sections, so auto no longer persists only the labeled-section subset of replies like `What should auth strategy be?` + `Auth strategy: ...`
- direct labeled-section standalone-authority fail-closed semantics that keep explicit `sourceResponseFormat: "labeled_sections"` from materializing only the parsed section subset when standalone question or topic authority still exists on non-section lines, so explicit decision answers, `inferDecisionTopics`, `inferOpenDecisions`, and explicit planning answers now reject partial labeled-section interpretations instead of silently ignoring the leftover authority; malformed question-span leftovers split across consecutive non-section lines like `What should rollout strategy be?` plus `For auth approach, ...` now also surface the concrete `Question span ...` parser-boundary error instead of flattening back to a generic standalone-question leftover, and standalone inline-topic leftovers split across consecutive non-section lines like `Rollout strategy should` plus `what should auth approach be.` or `Rollout strategy -` plus `Use a staged rollout.` now likewise surface either the concrete inline-topic parser-boundary error or the clean standalone topic summary instead of being flattened or silently ignored
- direct inline-topic standalone-authority fail-closed semantics that keep explicit `sourceResponseFormat: "inline_topics"` from materializing only the consumed inline-clause subset when question or topic authority still exists on other source clauses outside semantically valid inline-topic clauses, so explicit decision answers, `inferDecisionTopics`, `inferOpenDecisions`, and explicit planning answers now reject partial inline-topic interpretations instead of silently ignoring leftover authority on non-inline clauses; split inline-topic continuations like `Rollout strategy should` + `what should auth approach be.` or `Rollout strategy should` + `auth approach should use a staged rollout.` now also surface the concrete inline-topic parser-boundary error instead of letting the later line masquerade as a new clause, while valid separator-style continuations like `Rollout strategy -` + `Use a staged rollout.` now materialize as one inline-topic clause
- direct label-family completeness fail-closed semantics that keep explicit `sourceResponseFormat: "labeled_sections"` or `sourceResponseFormat: "inline_topics"` from materializing only the consumed subset when the same reply still contains additional parseable labeled sections or inline-topic clauses, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through now reject partial label-family captures with concrete unconsumed-unit errors like `left 1 unconsumed labeled sections` or `left 1 unconsumed inline topic clauses`; the same completeness guard now also keeps `inline_topics` auto probing terminal once explicit inline-topic authority has already been established, instead of letting auto consume one inline-topic clause and keep falling through
- direct ordered-family completeness fail-closed semantics that keep explicit `sourceResponseFormat: "ordered_items"` or `sourceResponseFormat: "ordered_blocks"` from materializing only the consumed prefix when the same reply still contains additional parseable ordered items or ordered blocks, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through now reject partial ordered captures with concrete unconsumed-unit errors like `left 1 unconsumed ordered items` or `left 1 unconsumed ordered blocks`
- ordered-family auto terminal and block-boundary semantics that keep `ordered_blocks` from collapsing multiple marked ordered-item lines into one block, and keep decision/planning `sourceResponseFormat: "auto"` from then falling through that weaker ordered-block surface once stronger ordered-item authority is already present, so replies like `1. ...` plus `2. ...` with only one requested answer now fail closed instead of swallowing the later ordered item into the first durable answer
- direct question/topic indexed-surface completeness fail-closed semantics that keep direct indexed `question_*` and `topic_*` formats from materializing only the consumed subset when the same reply still contains additional parseable question or topic units, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through now reject partial captures with concrete unconsumed-unit errors like `left 1 unconsumed question blocks` or `left 1 unconsumed topic paragraphs`
- direct pending-family completeness fail-closed semantics that keep direct generic `pending_clauses`, `pending_paragraphs`, `pending_sentences`, and `pending_conjunctions` from materializing only the consumed prefix when the same reply still contains additional parseable pending units, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through now reject partial generic captures with concrete unconsumed-unit errors like `left 1 unconsumed pending sentences`
- direct generic-unit cross-authority fail-closed semantics that keep `pending_clauses`, `pending_paragraphs`, `pending_sentences`, `pending_conjunctions`, `ordered_items`, and `ordered_blocks` from materializing one consumed unit whose text already carries explicit question or topic authority for a different consumer, by extending the shared direct answer-side guard to inspect real clause/conjunction splits inside one unit without treating untouched whole-run text as a fake topic predicate
- direct answer-source-family completeness fail-closed semantics that keep direct `pending_answer_sources` and `matching_answer_sources` from materializing only the consumed prefix when the same reply still contains additional parseable answerSource units, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through now reject partial answer-source captures with concrete unconsumed-unit errors like `left 1 unconsumed pending answer sources` or `left 1 unconsumed matching answer sources`
- direct matching-run-family completeness fail-closed semantics that keep top-level direct `matching_runs`, `matching_opening_runs`, `matching_closing_runs`, and `matching_middle_runs` from materializing only the consumed subset when the same reply still contains additional parseable matching runs for other already-registered consumers, so bundle/planning/follow-through surfaces no longer persist only the first matched run while silently leaving a later registered run unconsumed
- non-punctuated question-authority fail-closed semantics that let shared authority guards recognize narrow interrogative sentences like `Should rollout happen in stages or all at once.` even without `?`, so direct `topic_paragraphs` can no longer persist those paragraphs as topic answers and `sourceResponseFormat: "auto"` now fail-closes on the stronger question-family boundary instead of degrading into weaker topic fallback
- explicit question-family non-punctuated surface semantics that reuse that same shared detector on explicit question sentences and paragraphs, so `question_spans`, `question_blocks`, `question_middle_spans`, `question_closing_spans`, `question_middle_blocks`, `question_closing_blocks`, and `question_clauses` with a question-first sentence plus later answer sentences now keep strong question authority even when the question sentence ends with `.` instead of `?`; replies like `How should rollout happen. Use a staged rollout.`, `Should pilot scope start with enterprise users. Start with five enterprise customers.`, and `How should rollout happen.` paragraph blocks now stay on question-family surfaces, infer stable question-shaped summaries/prompts like `How should rollout happen?`, and let `sourceResponseFormat: "auto"` prefer `question_spans` over weaker fallback instead of rejecting those same explicit question anchors as non-question prose
- safe auxiliary-led and contracted-negative question-authority fail-closed semantics that extend that same shared non-punctuated detector to safe sentence-initial interrogatives like `What ...`, auxiliaries and modals like `Does ...`, `Did ...`, `Has ...`, `Had ...`, `Was ...`, `Must ...`, `May ...`, `Might ...`, `Shall ...`, plus contracted negatives like `Shouldn't ...` or `Doesn't ...`, and also to narrow subject-bearing bare forms like `Do we ...`, `Have we ...`, `Am I ...`, `Do rollout teams need ...`, or `Have blockers surfaced ...`, while still excluding imperative-ambiguous bare `do` / `have`; that same sentence-leading auxiliary substrate now also covers contracted perfect-have openings like `Haven't rollout blockers surfaced yet.` and contracted modal openings like `Mayn't rollout happen in stages or all at once.`, so hidden questions no longer slip past the fail-closed path just because the leading auxiliary is contracted; the same detector now also avoids misclassifying declarative wh-clause answers like `When rollout starts, use a staged rollout.`, `What matters is staging first.`, `What we need is staging first.`, `Which path we pick is staging first.`, `Whose signoff matters depends on budget.`, `Which rollout owner matters depends on budget.`, `Whose signoff is required depends on budget.`, `Whose signoff remains required depends on budget.`, `What to do is stage rollout first.`, `Which path to take depends on rollout risk.`, `How many stages we use depends on rollout risk.`, `Which rollout path we choose depends on budget.`, `How soon rollout starts depends on dependencies.`, `How often rollout happens depends on incident load.`, `How long rollout lasts depends on budget.`, or `How far rollout gets depends on support load.` as hidden question authority, and now also keeps copular and `as`-topic summary extraction from treating declarative wh-clause complements like `Which path to take is the staged rollout path.` or `Which path to take serves as the staged rollout path.` as explicit topic authority, so direct `topic_paragraphs`, `sourceResponseFormat: "auto"`, and shared answer-side guards no longer wrong-success on hidden questions or false-positive on non-question wh-clause answers just because `?` was omitted
- the same safe sentence-leading modal substrate now also covers `Ought ...`, `Need ...`, and the contracted negative `Oughtn't ...`, so hidden-question replies like `Ought rollout happen in stages or all at once.`, `Oughtn't rollout happen in stages or all at once.`, and `Need rollout happen in stages or all at once.` now fail close through direct `single_pending` and weaker topic-family fallback instead of leaking through as durable answers
- adverb-fragment and wh noun-phrase non-punctuated question-authority semantics that keep trailing-adjunct fragments like `When rollout starts on Tuesday.` or `How rollout works in Europe.` and noun-phrase fragments like `What rollout blockers remain.` on the fail-closed side, while still allowing the corresponding declarative continuations `When rollout starts on Tuesday depends on regulation.` and `How rollout works in Europe depends on regulation.` through the same shared detector
- how-quantifier embedded-clause semantics that also allow declarative `How much ... depends ...` forms like `How much budget we have depends on policy.` or `How much rollout budget remains depends on policy.` through that same shared detector, while still fail-closing true fragments like `How much budget we have.`
- existential wh-clause semantics that make embedded `there is/are` forms declarative only when they continue into a later outer predicate, so `How much budget there is depends on policy.` and `Which rollout blockers there are depends on scope.` now pass while bare fragments like `How much budget there is.` or `Which rollout blockers there are.` still fail closed
- singular existential wh noun-phrase semantics that extend that same `there is/are` rule to heads like `What cost there is depends on scope.`, `Which cost there is depends on scope.`, and `Whose approval there is depends on scope.`, while `What cost there is.`, `Which cost there is.`, and `Whose approval there is.` still fail close
- existential how-quantifier semantics that extend the same rule to `How many ... there are`, `How few ... there are`, and `How little ... there is`, so `How many rollout stages there are depends on policy.`, `How few rollout stages there are likely not to be depends on policy.`, and `How little rollout runway there is likely to be depends on policy.` now pass while `How many rollout stages there are.`, `How few rollout stages there are.`, and `How little rollout runway there need to be.` still fail close; `few` and `little` now ride the same shared detector on both sides of that boundary, so they reach the declarative embedded-clause path when a real outer predicate exists but still trigger the bare-question fail-closed guard for fragments like `How few rollout stages there are likely to be.` or `How little rollout runway there is much more likely to be.` instead of being misread as singular noun-phrase embedded subject clauses
- existential modal/future semantics that extend the same rule to chains like `there need(s) to be`, `there ought to be`, `there will be`, and `there is/was/are going to be`, so `How many rollout stages there need to be depends on policy.`, `What issue there ought to be depends on scope.`, `What rollout blockers there will be depends on scope.`, `How many rollout stages there are going to be depends on policy.`, and `What issue there is going to be depends on scope.` now pass while the corresponding bare fragments still fail close
- existential auxiliary-filler semantics that extend that same rule to modal existential chains with one narrow filler adverb or particle before the terminal copula, such as `there should probably be`, `there could perhaps be`, `there would possibly be`, `there might certainly be`, `there must surely be`, `there can presumably be`, `there will conceivably be`, `there should only be`, `there could even be`, and multi-token filler phrases like `there should at least be`, `there should at the least be`, `there should at the very least be`, `there could at most be`, `there should more or less be`, `there should more and more be`, `there should kind of be`, `there should sort of be`, `there should just about be`, `there should just plain be`, `there should by now be`, `there should no longer be`, `there should no more be`, `there should any more be`, or `there should at all be`; the shared predicate-filler substrate now skips those fillers consistently across generic wh and `how + quantifier` declarative scans, and recognized filler phrases can extend the existential scan window far enough to still reach the terminal copula instead of stopping at the original fixed lookahead, so `What issue there should probably be depends on scope.`, `What issue there should only be depends on scope.`, `What issue there should at least be depends on scope.`, `What issue there should at the very least be depends on scope.`, `What issue there should just plain be depends on scope.`, `What issue there should by now be depends on scope.`, `What issue there should more or less be depends on scope.`, `What issue there should more and more be depends on scope.`, `What issue there should no longer be depends on scope.`, `What issue there should at all be depends on scope.`, `How many rollout stages there could perhaps be depends on policy.`, and `How many rollout stages there could even be depends on policy.` now pass while the corresponding bare fragments still fail close
- common auxiliary-filler semantics now also cover additional adverbs like `definitely`, `clearly`, `obviously`, `plainly`, `simply`, `merely`, `actually`, `basically`, `virtually`, `effectively`, `essentially`, and `generally`, so openings like `What issue there should definitely be depends on scope.` and `How many rollout stages there could definitely be depends on policy.` now pass while the corresponding bare fragments still fail close instead of leaking through `single_pending`, `topic_paragraphs`, or weaker `sourceResponseFormat: "auto"` topic-family fallback
- existential support-chain semantics that extend that same rule to `there seem(s/ed) to be` and `there appear(s/ed) to be`, so `What issues there seem to be depends on scope.` and `What issues there appear to be depends on scope.` now pass while the corresponding bare fragments still fail close
- nested existential support-chain semantics that extend that same rule to support-verb plus copular-support shapes like `there seem(s/ed) likely to be`, `there seem(s/ed) very likely to be`, and `there appear(s/ed) highly likely not to be`; support-chain internal tokens are now excluded from the generic delayed-clause and `how + quantifier` declarative scans too, so `What issue there seems likely to be depends on scope.`, `What issue there appears highly likely not to be depends on scope.`, and `How many rollout stages there seem likely to be depends on policy.` now pass while the corresponding bare fragments still fail close
- broader lexical existential support-chain semantics that extend the same rule to `there used/happens/tends/continues/remains/proves/grows to be`, so `What issue there used to be depends on scope.` and `What issue there remains to be depends on scope.` now pass while `What issue there used to be.` and `What issue there remains to be.` still fail close
- that same lexical existential support-chain rule now also covers `there comes/gets to be`, so `What issue there comes to be depends on scope.` and `What issue there gets to be depends on scope.` pass while `What issue there comes to be.` and `What issue there gets to be.` still fail close
- phrasal existential support-chain semantics that extend the same rule to `there turned out to be`, so openings like `What issue there turned out to be depends on scope.` and `How many rollout stages there turned out to be depends on scope.` now pass while bare fragments like `What issue there turned out to be.` and `How many rollout stages there turned out to be.` still fail close
- narrow existential `being`-chain semantics that extend the same rule to shapes like `there ends up being` and `there keeps being`, so openings like `What issue there ends up being depends on scope.`, `What issue there keeps being depends on scope.`, `Which rollout blockers there ends up being depends on scope.`, and `Who there keeps being depends on scope.` now pass while bare fragments like `What issue there ends up being.`, `What issue there keeps being.`, `Which rollout blockers there ends up being.`, and `Who there keeps being.` still fail close
- negative existential-chain semantics that extend the same rule to `there should not be`, `there shouldn't be`, `there mayn't be`, `there mightn't be`, `there cannot be`, and `there ought not to be`, so declaratives like `What issue there should not be depends on scope.`, `What issue there shouldn't be depends on scope.`, `What issue there mayn't be depends on scope.`, `What issue there probably mightn't be depends on scope.`, `What issue there ought not to be depends on scope.`, `How many rollout stages there definitely mightn't be depends on policy.`, and `How many rollout stages there cannot be depends on policy.` now pass while the corresponding bare fragments still fail close instead of leaking through `single_pending`, `topic_paragraphs`, or `sourceResponseFormat: "auto"`
- copular existential support-chain semantics that extend the same rule to `there is/was/are supposed/expected/likely/meant/certain/ready/set to be`, including narrow modifier stacks before the support token like `very likely`, `much more likely`, `much less likely`, common degree-adverb prefixes like `quite/rather/too/almost/nearly likely`, filler-particle stacks like `even more likely`, repeated-comparative stacks like `less and less likely`, multi-token filler stacks like `at least likely`, `all too likely`, `a bit more likely`, `a little more likely`, `a little bit more likely`, `a fair bit more likely`, `a good deal more likely`, `a great deal more likely`, `a lot more likely`, `a whole lot more likely`, `way more likely`, `way less likely`, `more or less likely`, `kind of likely`, `sort of likely`, `no longer likely`, `no more likely`, `any more likely`, or `at all likely`, degree-adverb prefixes like `highly/extremely/fairly/somewhat likely`, and additional unlisted `-ly` prefixes like `especially/relatively/increasingly likely`; the shared prefix matcher now treats those fillers and `-ly` tokens as support prefixes without consuming `likely/unlikely` itself, prefers the longest overlapping filler phrase when multiple sequences match the same start, and extends the internal lookback far enough to keep generic delayed-clause scans and embedded-subject-clause scans from reinterpreting support-chain internal tokens as free predicates or fresh clause subjects, so `What issue there is supposed to be depends on scope.`, `What issue there is very likely to be depends on scope.`, `How many rollout stages there are likely to be depends on policy.`, `How many rollout stages there are much more likely to be depends on policy.`, `What issue there is much less likely to be depends on scope.`, `What issue there is even more likely to be depends on scope.`, `What issue there is less and less likely to be depends on scope.`, `What issue there is at least likely to be depends on scope.`, `What issue there is all too likely to be depends on scope.`, `What issue there is a bit more likely to be depends on scope.`, `What issue there is a little bit more likely to be depends on scope.`, `What issue there is a fair bit more likely to be depends on scope.`, `What issue there is a good deal more likely to be depends on scope.`, `What issue there is a great deal more likely not to be depends on scope.`, `What issue there is a whole lot more likely to be depends on scope.`, `What issue there is no longer likely to be depends on scope.`, `What issue there is at all likely to be depends on scope.`, `What issue there is almost certain to be depends on scope.`, `What issue there is highly likely to be depends on scope.`, `What issue there is especially likely to be depends on scope.`, `What issue there is relatively likely to be depends on scope.`, `How many rollout stages there are increasingly likely to be depends on policy.`, `Which blockers there are a whole lot more likely to be depends on scope.`, and `What issue there is ready to be depends on scope.` now pass while the corresponding bare fragments still fail close, including `What issue there is set to be.`, `What issue there is a little bit more likely to be.`, and `Which blockers there are a whole lot more likely to be.`
- negative copular existential support-chain semantics that extend the same rule to `there is/was/are not expected to be`, `there is/was/are expected not to be`, `there is/was/are not likely to be`, `there is/was/are likely not to be`, longer modifier stacks like `there is/was/are much more likely not to be`, degree-adverb prefixes like `there is/was/are highly likely not to be`, additional unlisted `-ly` prefixes like `there is/was/are particularly likely not to be`, `there is/was/are unlikely to be`, and contracted negatives like `there isn't/aren't/wasn't/weren't likely to be`, so `What issue there is not expected to be depends on scope.`, `What issue there is expected not to be depends on scope.`, `What issue there is not likely to be depends on scope.`, `How many rollout stages there are likely not to be depends on policy.`, `What issue there is much more likely not to be depends on scope.`, `What issue there is highly likely not to be depends on scope.`, `What issue there is particularly likely not to be depends on scope.`, `What issue there isn't likely to be depends on scope.`, `Who there is not expected to be depends on scope.`, and `How many rollout stages there are unlikely to be depends on policy.` now pass while the corresponding bare fragments still fail close instead of leaking through direct `single_pending`, `topic_paragraphs`, or weaker `sourceResponseFormat: "auto"` topic-family fallback
- contracted-negative auxiliary semantics now also preserve `mustn't` and `needn't` through embedded tokenization, so hidden-question replies like `Mustn't rollout happen in stages or all at once.` and `Needn't rollout happen in stages or all at once.` now fail close through the shared non-punctuated detector instead of leaking through direct `single_pending` or weaker topic-family fallback
- contracted-negative existential semantics now also preserve `oughtn't be` and `needn't be` inside embedded existential wh-clauses, so declaratives like `What issue there oughtn't be depends on scope.` and `What issue there needn't be depends on scope.` now pass while the corresponding bare fragments still fail close instead of being flattened into hidden question authority
- generic wh existential modal semantics that extend that same rule beyond `how-many` / `what` to shapes like `Which rollout blockers there need to be depends on scope.` and `Who there needs to be depends on scope.`, while `Which rollout blockers there need to be.` and `Who there needs to be.` still fail close
- delayed-copula existential wh noun-phrase semantics that stop single plural/possessive heads like `What costs there are.` or `Whose approvals there need to be.` from being misread as declarative just because `costs` or `approvals` look predicate-like, while still allowing the real outer-predicate continuations `What costs there are depends on scope.` and `Whose approvals there need to be depends on scope.`
- declarative wh-clause inline-topic and embedded-anchor fail-closed semantics that keep `Which path to take is the staged rollout path.` style answer text from being reinterpreted as separator-free verbal `inline_topics` label/value authority or as a fake embedded leading topic anchor like `Path to take`, so direct `inline_topics` now reject those replies when `inferDecisionTopics` is enabled and `sourceResponseFormat: "auto"` no longer wrong-success through embedded `topic_middle_spans` after inline-topic fallback is removed
- direct answer-source cross-authority fail-closed semantics that keep direct `pending_answer_sources` and `matching_answer_sources` from materializing answer text that already carries explicit topic or question authority for a different consumer, so explicit decision answers, `inferOpenDecisions`, explicit planning answers, and direct exported planner follow-through no longer accept a single mixed-authority answerSource like `Use Bun-native auth. Rollout strategy should use a staged rollout.` as a valid `Auth strategy` answer
- direct answer-side question-authority fail-closed semantics that keep `single_pending`, direct generic pending/ordered formats, direct matching-run formats, named answerSource resolution, and direct answer-source-backed formats from materializing text that is still just a question, even when that question appears to belong to the current requested summary; replies like `Should rollout happen in stages or all at once?`, `Which auth provider should we adopt for the Bun-first product path?`, or `What if rollout needs stages.` no longer persist as durable answers
- direct answer-side nested incomplete-topic fail-closed semantics that keep `single_pending`, direct generic pending/ordered formats, named answerSource-backed direct capture, and direct pending answer-source capture from materializing malformed repeated same-summary predicate chains like `Rollout strategy should rollout strategy should.` or `Rollout strategy should rollout strategy should use stages.`, so direct answer-side guards no longer stop at the first `should ...` and mistake the remainder for substantive answer content just because the nested chain eventually contains real words
- direct resolved-answer cross-authority fail-closed semantics that keep `single_pending` and named `answerSourceKey` / `answerSourceGroupKey` resolution from materializing answer text that already carries explicit topic or question authority for a different consumer, so a single shared reply like `Use Bun-native auth. Rollout strategy should use a staged rollout.` or a named answerSource with that same mixed text no longer succeeds as a valid `Auth strategy` answer
- direct matching-run cross-authority fail-closed semantics that keep direct `matching_runs`, `matching_opening_runs`, `matching_closing_runs`, and `matching_middle_runs` from materializing answer text that already carries explicit topic or question authority for a different consumer, while still allowing same-consumer aliases like `Rollout` versus `Rollout strategy`; this closes the real wrong-success path where direct `matching_opening_runs` or `matching_middle_runs` could otherwise swallow later rollout text into an auth answer when only one direct answer or one open decision had been requested
- inline-topic standalone-question auto-terminal semantics that keep decision/planning `sourceResponseFormat: "auto"` from falling through into `question_*` probes after any semantically valid inline-topic clause has already been established and another clause still carries standalone question authority, so replies like `Auth strategy - ...` plus `What should rollout strategy be?` or `Auth strategy should use Bun-native auth.` plus `What should rollout strategy be?` now stop at the inline-topic boundary with the real leftover-authority error instead of surfacing a later question-family mismatch; the same now holds when the malformed inline-topic/question mix is split across adjacent clauses like `Rollout strategy should` + `what should auth approach be.`
- inline-topic standalone-topic auto-terminal semantics that keep decision-bundle `sourceResponseFormat: "auto"` from falling through into weaker `topic_*` probes after separator-style inline-topic authority has already been established and another clause still carries standalone explicit topic authority, so replies like `Auth strategy - ...` plus `For rollout strategy, ...` now stop at the inline-topic boundary with the real leftover-authority error instead of surfacing a later topic-family mismatch; valid separator-style continuations like `Rollout strategy -` + `Use a staged rollout.` now stay inside `inline_topics` instead of being flattened into leftover topic authority
- labeled-section standalone-topic auto-terminal semantics that keep decision-bundle `sourceResponseFormat: "auto"` from stopping at `labeled_sections` when standalone explicit topic authority still exists on non-section lines before or after those sections, so auto no longer persists only the labeled-section subset of replies like `For rollout strategy, ...` + `Auth strategy: ...`
- inline-topic auto terminal fail-closed semantics that keep malformed one-clause `inline_topics` replies from falling through into `question_*` or `topic_*` auto fallbacks after inline-topic parsing has already established explicit label/value authority, so mixed-authority clauses like `Auth strategy should what should rollout strategy be.`, `Auth strategy should rollout strategy should use a staged rollout.`, or `Auth strategy should auth strategy should.` now stop at the inline-topic boundary instead of materializing dirty durable answers through `question_clauses`, `question_closing_spans`, or `topic_closing_blocks`
- inline-topic question-surface parser rejection that keeps explicit `sourceResponseFormat: "inline_topics"` from treating question-first text like `What should auth strategy be?` or `What should auth strategy be? Use Bun-native auth.` as a fake inline-topic clause labeled `What`, so direct `inferDecisionTopics` and `inferOpenDecisions` now correctly fail closed with “requires at least one inline topic clause” instead of leaking parser junk through the verbal inline-topic path
- inline-topic question-label auto terminal semantics that keep malformed separator-style inline topic labels like `What should auth strategy be - ...` from falling through into `question_spans`, so decision-bundle auto and planning auto now stop at the explicit inline-topic boundary once that non-colon separator label has already established invalid question authority
- question leading-text-before-canonical-authority fail-closed semantics that keep question-family surfaces from materializing dirty prompts like `Auth strategy should what should auth strategy be?`, so direct question-family inference plus decision/planning `sourceResponseFormat: "auto"` now reject canonical question anchors that only appear after non-empty leading text
- question incomplete-topic-label fail-closed semantics that keep question-family surfaces from materializing dirty prompts like `Auth strategy should?`, so direct question-family inference plus decision/planning `sourceResponseFormat: "auto"` now reject question text that only contains an incomplete leading topic predicate
- planning-auto label-family fail-closed semantics that keep `inferRemainingAnswers` auto resolution from falling through explicit `labeled_sections` or separator-style `inline_topics` authority into `topic_*` or `question_*` planning surfaces, so planning auto no longer persists dirty summaries like `Auth strategy:` or `Auth strategy -` when the reply had already committed to an unsupported explicit label/value family; malformed labeled-section values like `Auth strategy: What should rollout strategy be?`, `Auth strategy: Rollout strategy should ...`, or `Auth strategy: Auth strategy should.` now also surface their real labeled-section parser-boundary error, leftover standalone question/topic authority outside labeled-section lines now surfaces the real labeled-section leftover-authority error, separator-style inline-topic lines outside labeled sections now report a clean leftover topic summary like `Rollout strategy` instead of leaking `Rollout strategy -`, malformed separator-style inline-topic lines outside labeled sections now surface their concrete inline-topic parser-boundary error instead of a flattened leftover-authority message, malformed verbal inline-topic lines outside labeled sections like `Rollout strategy should what should auth approach be.` or `Rollout strategy should auth approach should ...` now likewise surface their concrete inline-topic answer-side parser-boundary error instead of a flattened leftover-authority message, the same now holds when those standalone inline-topic leftovers are split across consecutive non-section lines like `Rollout strategy should` plus `what should auth approach be.` or `Rollout strategy -` plus `Use a staged rollout.`, malformed question-span lines outside labeled sections like `What should rollout strategy be? For auth approach, ...` or `What should rollout strategy be? Rollout strategy should.` now surface their concrete `Question span ...` parser-boundary error instead of a flattened leftover standalone-question message, the same now holds when that malformed question span is split across consecutive non-section lines like `What should rollout strategy be?` plus `For auth approach, ...`, unsupported separator-style inline-topic replies like `Auth strategy - ...` plus `What should rollout strategy be?` or `For rollout strategy, ...` now likewise surface the real inline-topic leftover-authority error instead of collapsing into the generic unsupported-format message, and mixed separator-style plus verbal inline-topic replies like `Auth strategy - ...` plus `Rollout strategy should ...` now surface a concrete mixed inline-topic authority error instead of the same generic fallback
- planning-auto label-family fail-closed semantics that keep `inferRemainingAnswers` auto resolution from falling through explicit `labeled_sections` or separator-style `inline_topics` authority into `topic_*` or `question_*` planning surfaces, so planning auto no longer persists dirty summaries like `Auth strategy:` or `Auth strategy -` when the reply had already committed to an unsupported explicit label/value family; malformed labeled-section values like `Auth strategy: What should rollout strategy be?`, `Auth strategy: Rollout strategy should ...`, or `Auth strategy: Auth strategy should.` now also surface their real labeled-section parser-boundary error, leftover standalone question/topic authority outside labeled-section lines now surfaces the real labeled-section leftover-authority error, separator-style inline-topic lines outside labeled sections now report a clean leftover topic summary like `Rollout strategy` instead of leaking `Rollout strategy -`, malformed separator-style inline-topic lines outside labeled sections now surface their concrete inline-topic parser-boundary error instead of a flattened leftover-authority message, malformed verbal inline-topic lines outside labeled sections like `Rollout strategy should what should auth approach be.` or `Rollout strategy should auth approach should ...` now likewise surface their concrete inline-topic answer-side parser-boundary error instead of a flattened leftover-authority message, malformed question-span lines outside labeled sections like `What should rollout strategy be? For auth approach, ...` or `What should rollout strategy be? Rollout strategy should.` now surface their concrete `Question span ...` parser-boundary error instead of a flattened leftover standalone-question message, unsupported separator-style inline-topic replies like `Auth strategy - ...` plus `What should rollout strategy be?` or `For rollout strategy, ...` now likewise surface the real inline-topic leftover-authority error instead of collapsing into the generic unsupported-format message, malformed question-span clauses outside consumed inline-topic clauses like `What should rollout strategy be? For auth approach, ...` or `What should rollout strategy be? Rollout strategy should.` now likewise surface their concrete `Question span ...` parser-boundary error instead of a flattened leftover standalone-question message, and mixed separator-style plus verbal inline-topic replies like `Auth strategy - ...` plus `Rollout strategy should ...` now surface a concrete mixed inline-topic authority error instead of the same generic fallback
- question-answer incomplete-topic-authority fail-closed semantics that keep question-family surfaces from materializing bare fragments like `Auth strategy should` or `Rollout strategy should` as answers when the answer text only contains an incomplete leading topic predicate, so direct `question_closing_spans` capture plus decision/planning auto no longer succeed through malformed question replies that stopped before any substantive topic answer content
- planning-auto inline-topic parser-boundary precedence that keeps malformed verbal `inline_topics` clauses from yielding to topic-family fallback once inline-topic parsing has already established explicit label/value authority, so planning auto no longer lets replies like `Auth strategy should rollout strategy should.` succeed through `topic_closing_blocks` after the inline-topic guard already knows the clause still carries conflicting topic authority; split verbal continuations like `Rollout strategy should` + `auth approach should use a staged rollout.` or `Rollout strategy should` + `what should auth approach be.` now also fail at the inline-topic parser boundary instead of being reinterpreted as a later clause or flattened leftover
- label-family incomplete-topic-authority fail-closed semantics that keep `labeled_sections` and `inline_topics` from materializing bare fragments like `Auth strategy should.` just because the malformed predicate reused the same summary as the label, so direct label capture, remaining decision-topic inference, decision-bundle auto, and planning-auto verbal inline-topic preflight no longer accept same-summary incomplete topic authority as if it were a real durable answer
- prefixed-topic leading-conjunction interpretation that lets later explicit topic anchors like `And about rollout strategy, ...` keep deterministic prefixed-topic authority, instead of failing sentence-level topic inference or causing continuation-based topic surfaces to over-consume that later anchored stretch into the previous topic because the prefixed anchor was no longer at the first token
- prefixed-topic versus inline-topic auto disambiguation that keeps `sourceResponseFormat: "auto"` from stopping at weaker `inline_topics` parses when a reply already establishes explicit prefixed topic authority like `For auth strategy, ...` or `About rollout strategy, ...`, so deterministic topic inference no longer persists dirty summaries like `For auth strategy,`
- question leading-conjunction prompt normalization that lets later explicit question surfaces like `And rollout strategy?` persist stable `Rollout strategy?` prompts and `Rollout strategy` summaries across clauses, sentence spans, and question blocks, instead of keeping deterministic authority but leaking sentence-position lowercase text into the durable prompt/summary substrate
- contracted existential starter/copula question-authority semantics that merge `there'd`, `there'll`, `there's`, and `there're` into the shared non-punctuated existential substrate, so declaratives like `What issue there'd be depends on scope.` / `What issue there'll be depends on scope.` / `What issue there'd have to be depends on scope.` / `What issue there'd likely be depends on scope.` / `What issue there'll likely be depends on scope.` / `What issue there'd very likely be depends on scope.` / `What issue there's likely to be depends on scope.` / `What issue there's going to be depends on scope.` / `What issues there're likely to be depends on scope.` no longer false-positive as hidden questions, while bare fragments like `What issue there'd be.` / `What issue there'd likely be.` / `What issue there's likely to be.` still fail closed and paragraph-backed topic-family plus `auto` fallback still stop at the stronger question-authority boundary
- present-perfect existential question-authority semantics that merge `there've` and recognize shared perfect existential predicate chains like `there've been`, `there've got to be`, `there've had to be`, and `there have likely been`, so declaratives like `What issues there've been depends on scope.` / `What issues there've got to be depends on scope.` / `What issues there've had to be depends on scope.` / `How many rollout stages there have likely been depends on policy.` no longer wrong-success or false-positive as hidden questions, while the corresponding bare fragments still fail closed and paragraph-backed topic-family plus `auto` fallback no longer persist them as durable answers
- contracted existential perfect-copula question-authority semantics that treat `there's` as either `there is` or `there has` through the same shared existential substrate, so declaratives like `What issue there's been depends on scope.` / `What issue there's likely been depends on scope.` / `How many rollout stages there's likely been depends on policy.` no longer false-positive as hidden questions, while the corresponding bare fragments still fail closed and paragraph-backed topic-family plus `auto` fallback no longer persist them as durable answers
- contracted existential starter perfect question-authority semantics that recognize perfect continuations after `there'd` / `there'll` support stacks like `there'd likely have been` and `there'll likely have been`, so declaratives like `What issues there'd likely have been depends on scope.` / `What issues there'll likely have been depends on scope.` / `How many rollout stages there'd likely have been depends on policy.` no longer wrong-success as durable answers, while the corresponding bare fragments still fail closed and paragraph-backed topic-family plus `auto` fallback no longer persist them as durable answers
- negative modal perfect existential question-authority semantics that recognize auxiliary-plus-support-plus-perfect chains like `there wouldn't likely have been` and `there can't likely have been`, so declaratives like `What issues there wouldn't likely have been depends on scope.` / `What issues there can't likely have been depends on scope.` / `How many rollout stages there wouldn't likely have been depends on policy.` no longer wrong-success as durable answers, while the corresponding bare fragments still fail closed and paragraph-backed topic-family plus `auto` fallback no longer persist them as durable answers
- existential support-to-perfect question-authority semantics that recognize support-chain continuations like `there seems likely to have been` and `there is likely to have been`, so declaratives like `What issue there seems likely to have been depends on scope.` / `What issue there is likely to have been depends on scope.` / `How many rollout stages there are likely to have been depends on policy.` no longer wrong-success as durable answers, while the corresponding bare fragments still fail closed and paragraph-backed topic-family plus `auto` fallback no longer persist them as durable answers
- auxiliary-led existential question-authority semantics that keep sentence-leading modal questions like `Should there be rollout blockers for the Bun-first product path.` and `Should there have been rollout blockers for the Bun-first product path.` from being misread as declaratives just because their existential complement ends in a plural noun phrase, so direct `single_pending`, paragraph-backed topic-family parsing, and `auto` fallback all now fail closed on those hidden questions instead of persisting them as durable answers
- bare-`need` question-authority semantics that route sentence-leading `Need ...` through the contextual leading-word gate instead of the shared auxiliary substrate, so ordinary hidden questions like `Need rollout happen in stages or all at once.` and existential hidden questions like `Need there be rollout blockers for the Bun-first product path.` / `Need there have been rollout blockers for the Bun-first product path.` no longer wrong-success as durable answers, while declaratives like `What we need is staging first.` stay materializable and direct `single_pending`, paragraph-backed topic-family parsing, plus `auto` fallback all now stop at the stronger question-authority boundary
- contextual bare-`have` existential question-authority semantics that admit sentence-leading `Have there ...` into the existing shared existential helper path, so hidden questions like `Have there been rollout blockers for the Bun-first product path.` and `Have there likely been rollout blockers for the Bun-first product path.` no longer wrong-success as durable answers, while already-supported bare `Have we ...` questions and declaratives like `What we have seen depends on rollout scope.` keep their existing behavior and direct `single_pending`, paragraph-backed topic-family parsing, plus `auto` fallback all now stop at the stronger question-authority boundary
- contextual bare-`do` existential question-authority semantics that admit sentence-leading `Do there ...` into the existing shared existential helper path, so hidden questions like `Do there need to be rollout blockers for the Bun-first product path.` and `Do there have to be rollout blockers for the Bun-first product path.` no longer wrong-success as durable answers, while already-supported bare `Do rollout teams need ...` questions and declaratives like `What we do there depends on rollout scope.` keep their existing behavior and direct `single_pending`, paragraph-backed topic-family parsing, plus `auto` fallback all now stop at the stronger question-authority boundary
- contracted bare-`do` question-authority semantics that merge sentence-leading `don't` into the shared embedded token stream and route it through the same contextual bare-`do` subject/existential gate, so hidden questions like `Don't we need a staged rollout.` / `Don't there need to be rollout blockers for the Bun-first product path.` / `Don't there have to be rollout blockers for the Bun-first product path.` no longer wrong-success as durable answers, while imperative surfaces like `Don't rollout now.` and declaratives like `What we don't need is staging first.` keep their existing behavior and direct `single_pending`, paragraph-backed topic-family parsing, plus `auto` fallback all now stop at the stronger question-authority boundary
- adverb-led existential plural-complement question-authority semantics that keep hidden questions like `Why there are rollout blockers.` / `Why there need to be rollout blockers.` / `Where there have been rollout blockers.` / `When there would be rollout blockers.` from being misread as declaratives just because the existential clause ends in a plural complement head, so direct `single_pending`, paragraph-backed topic-family parsing, and `auto` fallback now fail closed on those fragments while corresponding declaratives like `Why there are rollout blockers depends on scope.` still materialize as durable answers
- whether-clause leading-topic rejection semantics that stop declaratives like `Whether there are rollout blockers depends on scope.` and `Whether we use Bun depends on support load.` from synthesizing fake topic authority such as `Whether there` or `Whether we`, so direct `single_pending` now materializes those clauses as durable answers while `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries from those subordinate-clause openers
- subordinate-clause fragment authority semantics that treat bare sentence-leading `if` / `whether` clauses like `If rollout happens in stages.`, `If there are rollout blockers.`, `Whether rollout happens in stages.`, and `Whether there are rollout blockers.` as fail-closed answer authority unless they continue into a real outer predicate or comma continuation, so direct `single_pending`, paragraph-backed topic-family parsing, and `sourceResponseFormat: "auto"` no longer persist those fragments as durable answers, while continuations like `If rollout happens in stages, use feature flags.` and declaratives like `Whether there are rollout blockers depends on scope.` still materialize normally
- subordinate-clause leading-topic rejection semantics that extend the same shared clause-starter substrate across `after`, `because`, `before`, `if`, `since`, `unless`, `whether`, and `while`, so bare fragments like `Because rollout happens in stages.` or `Since rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, non-question continuations like `Since we use Bun, support load stays low.` or `Unless we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Since we` / `Unless we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Because we`, `If there`, `Since we`, or `Unless we`
- multi-token subordinate-clause authority semantics that extend the same shared token-sequence substrate across `even if`, `even though`, `as long as`, `so long as`, `as soon as`, `now that`, and `once`, so bare fragments like `Even though rollout happens in stages.` or `As long as rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, declarative continuations like `Even though we use Bun, support load stays low.` or `As soon as we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Even though we` or `As soon as we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Even if we`, `As long as we`, `Now that we`, or `Once we`
- additional subordinate-clause opener authority semantics that extend the same shared token-sequence substrate across `assuming`, `given`, `given that`, `in case`, `in order to`, `provided`, `provided that`, and `so that`, so bare fragments like `So that rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, declarative continuations like `Provided we use Bun, support load stays low.` or `So that we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Provided we` or `So that we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Given that we`, `In case we`, or `So that we`
- second-wave subordinate-clause opener authority semantics that extend the same shared token-sequence substrate across `although`, `though`, `until`, `whenever`, `whereas`, `wherever`, `even when`, `only if`, `only when`, `as if`, `as though`, and `just as`, so bare fragments like `Although rollout happens in stages.` or `Whenever rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, declarative continuations like `Although we use Bun, support load stays low.` or `As though we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Although we` or `As though we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Even when we`, `Only if we`, `Whenever we`, or `Whereas we`
- third-wave subordinate-clause opener authority semantics that extend the same shared token-sequence substrate across `as far as`, `as much as`, `even as`, `except if`, `except when`, `for fear that`, `granted`, `inasmuch as`, `insofar as`, `lest`, `save that`, `seeing`, `seeing that`, `so far as`, `supposing`, and `supposing that`, so bare fragments like `Even as rollout happens in stages.` or `Supposing rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, declarative continuations like `As far as we use Bun, support load stays low.` or `Seeing that we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `As far as we` or `Seeing that we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Even as we`, `Except when we`, `Insofar as we`, or `Supposing that we`
- free-choice subordinate-clause opener authority semantics that extend the same shared token-sequence substrate across `independent of whether`, `irrespective of whether`, `no matter how`, `no matter if`, `no matter when`, `no matter whether`, `regardless of whether`, and `whether or not`, so bare fragments like `No matter when rollout happens in stages.` or `Whether or not rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, declarative continuations like `No matter whether we use Bun, support load stays low.` or `Irrespective of whether we use Bun, support load stays low.` no longer escape the shared subordinate-clause guard, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `No matter when we`, `Regardless of whether we`, `Independent of whether we`, or `No matter how we`
- temporal article-stripped subordinate opener authority semantics that extend the same shared token-sequence substrate across `by the time`, `the moment`, `the minute`, and `the instant`, while also recognizing article-stripped aliases like `moment`, `minute`, and `instant`, so bare fragments like `By the time rollout happens in stages.` or `The moment rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `By the time we use Bun, support load stays low.` and `The moment we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `By the time we` or `Moment we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `By the time we`, `Moment we`, `Minute we`, or `Instant we`
- repeating and deictic time subordinate opener authority semantics that extend the same shared token-sequence substrate across `every time`, `each time`, `any time`, `next time`, `last time`, `the first time`, `the next time`, and `the last time`, while also recognizing article-stripped aliases like `first time`, `next time`, and `last time`, so bare fragments like `Every time rollout happens in stages.` or `The last time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Every time we use Bun, support load stays low.` and `The next time we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Every time we` or `Last time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Every time we`, `Each time we`, `Any time we`, `Next time we`, `Last time we`, or `First time we`
- demonstrative and ordinal time subordinate opener authority semantics that extend the same shared token-sequence substrate across `this time`, `that time`, `another time`, and common low ordinal variants from `the second time` through `the tenth time`, while also recognizing article-stripped aliases like `second time`, `third time`, `fourth time`, and `fifth time`, so bare fragments like `This time rollout happens in stages.` or `The fifth time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Another time we use Bun, support load stays low.` and `The second time we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Another time we`, `Second time we`, `Third time we`, `Fourth time we`, or `Fifth time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Another time we`, `Second time we`, `Third time we`, `Fourth time we`, or `Fifth time we`
- generic ordinal-time subordinate opener authority semantics that extend the same shared subordinate substrate beyond hand-enumerated low ordinals by recognizing higher spelled ordinals like `the eleventh time` / `the twelfth time` and numeric ordinals like `the 21st time`, so bare fragments like `The eleventh time rollout happens in stages.` or `The 21st time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `The eleventh time we use Bun, support load stays low.` and `The 21st time we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Eleventh time we`, `Twelfth time we`, or `21st time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Eleventh time we`, `Twelfth time we`, or `21st time we`
- modified time subordinate opener authority semantics that extend the same shared time-opener helper across modifier-bearing variants like `the very first time`, `the very next time`, `the only time`, and `one time`, so bare fragments like `The very first time rollout happens in stages.` or `The only time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `The very next time we use Bun, support load stays low.` and `One time we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Very first time we`, `Very next time we`, `Only time we`, or `One time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Very first time we`, `Very next time we`, `Only time we`, or `One time we`
- plural and quantified time subordinate opener authority semantics that extend the same shared time-opener helper across surfaces like `every single time`, `the first few times`, `the first couple times`, `many times`, `several times`, and `the only other time`, so bare fragments like `Every single time rollout happens in stages.` or `The first few times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Every single time we use Bun, support load stays low.` and `The first couple times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Every single time we`, `First few times we`, `First couple times we`, `Many times we`, `Several times we`, or `Only other time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Every single time we`, `First few times we`, `First couple times we`, `Many times we`, `Several times we`, or `Only other time we`
- most/some/several time subordinate opener authority semantics that extend the same shared time-opener helper across bare plural heads like `most times` / `some times`, modified singular heads like `some other time` / `most every time`, and ordinal or deictic plural variants like `the first several times` / `the next several times` / `the last several times`, so bare fragments like `Most times rollout happens in stages.` or `The first several times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Most times we use Bun, support load stays low.` and `The next several times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Most times we`, `Some times we`, `Some other time we`, `Most every time we`, `First several times we`, `Next several times we`, or `Last several times we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Most times we`, `Some times we`, `Some other time we`, `Most every time we`, `First several times we`, `Next several times we`, or `Last several times we`
- quantified and prepositional time subordinate opener authority semantics that extend the same shared time-opener helper across quantified `... of the time` heads like `most of the time` / `some of the time` / `part of the time`, determiner-led `all the time`, article-led pluralized phrases like `a few times` / `a couple of times` / `a lot of times` / `a number of times`, bare `plenty of times`, article-stripped singular time phrases like `the other time` / `the same time`, and narrow prepositional time openers like `at the same time` / `for the first time` / `from the next time`, so bare fragments like `Most of the time rollout happens in stages.` or `For the first time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Most of the time we use Bun, support load stays low.` and `At the same time we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Most of the time we`, `A lot of times we`, `Same time we`, `At the same time we`, or `From the next time we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Most of the time we`, `A number of times we`, `Other time we`, `Same time we`, `At the same time we`, or `From the next time we`
- prepositional quantified time-tail subordinate opener authority semantics that extend the same shared `for/from + the + ...` branch to reuse the generic time-opener helper, so plural-tail variants like `For the first few times rollout happens in stages.` / `For the first several times rollout happens in stages.` / `From the first few times rollout happens in stages.` / `From the next several times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `From the first few times we use Bun, support load stays low.` and `From the next several times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `From the first few times we` or `From the next several times we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `For the first few times we`, `For the first several times we`, `From the first few times we`, or `From the next several times we`
- prepositional time-opener shared authority semantics that extend the same shared prepositional helper across `by + the + <generic time opener>`, `at + the + <generic time opener>`, and direct point-head variants like `from the moment` / `from the minute` / `from the instant` / `by the moment`, so bare fragments like `By the first time rollout happens in stages.` / `At the next several times rollout happens in stages.` / `From the moment rollout happens in stages.` / `By the moment rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `By the first time we use Bun, support load stays low.` and `From the moment we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `By the first time we`, `At the first few times we`, `From the moment we`, or `By the moment we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `By the next time we`, `At the next several times we`, `From the minute we`, or `By the moment we`
- article-less quantified time-tail subordinate opener authority semantics that extend the same shared ordinal/deictic time-opener helper across surfaces like `the first couple of times`, `the first handful of times`, `the next couple of times`, `by the first couple of times`, and `from the next handful of times`, so bare fragments like `The first couple of times rollout happens in stages.` / `The first handful of times rollout happens in stages.` / `By the first couple of times rollout happens in stages.` / `From the next handful of times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `The next couple of times we use Bun, support load stays low.` and `By the first couple of times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Next couple of times we`, `First handful of times we`, `First couple of times we`, or `From the next handful of times we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Next couple of times we`, `First handful of times we`, `First couple of times we`, or `From the next handful of times we`
- modified article-led quantified time subordinate opener authority semantics that extend the same shared article-led time helper across modifier-bearing quantity phrases like `a great many times`, `a fair few times`, `a fair number of times`, `a good couple of times`, and `a small handful of times`, while also treating their article-stripped aliases as the same subordinate-opener family after leading-topic normalization removes the initial `a`, so bare fragments like `A great many times rollout happens in stages.` or `A fair number of times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `A great many times we use Bun, support load stays low.` and `A good couple of times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Great many times we`, `Fair few times we`, `Fair number of times we`, `Good couple of times we`, or `Small handful of times we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Great many times we`, `Fair few times we`, `Fair number of times we`, `Good couple of times we`, or `Small handful of times we`
- count-based time subordinate opener authority semantics that extend the same shared time-opener helper across bare direct-times count heads like `countless times` / `multiple times` / `numerous times` / `half times`, plural repeat variants like `several more times`, count-noun repeats like `several dozen times` / `dozens of times` / `scores of times` / `hundreds of times` / `three times` / `twenty one times`, plural count-noun `... more times` variants like `dozens more times` / `scores more times` / `hundreds more times`, repeated and approximating plural count-noun variants like `dozens and dozens of times` / `dozens and dozens more times` / `dozens and dozens or so times` / `scores and scores of times` / `scores and scores more times` / `scores and scores or more times` / `hundreds and hundreds of times` / `hundreds and hundreds more times` / `hundreds and hundreds or so times`, compound cardinal ranges like `twenty one or twenty two times` / `twenty one or twenty two more times` / `twenty one or twenty two dozen times`, larger count-noun `... more times` variants like `several thousand more times` / `a couple thousand more times` / `few thousand more times`, half-quantity count-noun variants like `half dozen times` / `half dozen more times` / `half a dozen times` / `half a dozen more times` / `a half dozen times` / `a half dozen more times` / `a dozen and a half times` / `dozen and a half times` / `a dozen and a half more times` / `dozen and a half more times` / `a score and a half times` / `a score and a half more times` / `half a score times`, approximation/range variants like `a dozen or so times` / `dozen or so times` / `a dozen or more times` / `a dozen or two times` / `a score or two times` / `a hundred or two times` / `dozen or two times` / `score or two times` / `hundred or two times` / `a dozen or three more times` / `a couple or three times` / `one or two times` / `two or three times` / `one or two more times` / `two or three more times`, cardinal-range count-noun variants like `one or two dozen times` / `two or three hundred times` / `one or two hundred more times` / `two or three thousand more times`, and article-led or article-stripped variants like `a couple more times` / `a couple dozen times` / `a few dozen times` / `a couple dozen or so times` / `a few dozen or so times` / `couple dozen or so times` / `few dozen or so times` / `a couple dozen and a half times` / `a few dozen and a half more times` / `a couple hundred times` / `a few hundred times` / `a couple hundred or so times` / `a few hundred or so times` / `couple hundred or so times` / `few hundred or so times` / `couple hundred times` / `a dozen times` / `a few more times`, so bare fragments like `Countless times rollout happens in stages.` / `Dozens and dozens or so times rollout happens in stages.` / `Scores and scores or more times rollout happens in stages.` / `Hundreds and hundreds or so times rollout happens in stages.` / `A couple dozen and a half times rollout happens in stages.` / `A few dozen and a half more times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Countless times we use Bun, support load stays low.` / `Hundreds and hundreds or so times we use Bun, support load stays low.` / `A couple dozen and a half times we use Bun, support load stays low.` / `A score and a half more times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Countless times we`, `Dozens and dozens or so times we`, `Scores and scores or more times we`, `Hundreds and hundreds or so times we`, `Couple dozen and a half times we`, `Few dozen and a half more times we`, `Half times we`, `Dozen and a half more times we`, or `Score and a half more times we`, and `topic_sentences` remaining-topic inference no longer fabricates those same fake durable summaries
- count-noun-leading small-cardinal range subordinate opener semantics that extend the same shared count-based branch across `a couple dozen or three times`, `a couple dozen or three more times`, `a few hundred or two times`, `a few hundred or two more times`, `couple dozen or three times`, and `few hundred or two more times`, so bare fragments like `A couple dozen or three times rollout happens in stages.` / `A few hundred or two more times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `A couple dozen or three times we use Bun, support load stays low.` / `A few hundred or two times we use Bun, support load stays low.` / `Couple dozen or three times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Couple dozen or three times we` or `Few hundred or two times we`, and `topic_sentences` remaining-topic inference no longer fabricates those same fake durable summaries
- quantifier-led count-noun subordinate opener semantics that extend the same shared count-based branch across `several dozen or so times`, `several dozen or two times`, `several hundred or so times`, `several hundred or so more times`, `several hundred and a half times`, `several hundred and a half more times`, `several hundred or two more times`, `many hundred or so times`, and `many hundred or two times`, so bare fragments like `Several hundred or so times rollout happens in stages.` / `Several hundred and a half more times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Several dozen or so times rollout happens in stages, use feature flags.` / `Many hundred or two times rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Several dozen or so times we`, `Several hundred or so times we`, `Several hundred or two more times we`, `Many hundred or so times we`, or `Many hundred or two times we`
- lexical count-based time subordinate opener semantics that extend the same shared count-based branch across `once more`, `twice`, `twice more`, `thrice`, `thrice more`, `once or twice`, `once or twice more`, `twice or thrice`, and `twice or thrice more`, so bare fragments like `Twice rollout happens in stages.` / `Twice or thrice more rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Twice rollout happens in stages, use feature flags.` / `Once or twice more rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Twice we`, `Twice more we`, `Thrice we`, `Once or twice we`, or `Twice or thrice more we`
- article-led ordinal time subordinate opener semantics that extend the same shared ordinal/deictic time-opener helper across `a/an + ordinal time` surfaces like `a first time`, `a second time`, `a third time`, `an eighth time`, `an eleventh time`, and `a twelfth time`, plus direct wrappers like `for a first time`, `from a second time`, `at a first time`, and `by a second time`, so bare fragments like `A first time rollout happens in stages.` / `An eleventh time rollout happens in stages.` / `For a first time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, while continuations like `A first time we use Bun, support load stays low.` and `For a first time we use Bun, support load stays low.` still materialize normally and `topic_sentences` keeps those surfaces inside the same subordinate-opener substrate instead of treating them as standalone topic labels
- article-less prepositional generic-time subordinate opener semantics that extend the same shared time-opener substrate across direct `yet another time` plus article-less wrappers like `for another time`, `from another time`, `by next time`, `at same time`, `for some other time`, and `for yet another time`, so bare fragments like `Yet another time rollout happens in stages.` / `For another time rollout happens in stages.` / `By next time rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Yet another time we use Bun, support load stays low.` and `For yet another time we use Bun, support load stays low.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority like `Yet another time we`, `From another time we`, `By next time we`, or `At same time we`
- repetition- and frequency-adverbial time-opener semantics that extend the same shared temporal subordinate substrate across `from time to time`, `every so often`, `so often`, `all too often`, `more often than not`, `not infrequently`, `every now and again`, `every now and then`, `once in a while`, `every once in a while`, `once again`, `time and again`, `time and time again`, `time after time`, `again and again`, `now and again`, `now and then`, `back and forth`, `on and off`, `off and on`, `over and over`, and `over and over again`, while making leading opener matching prefer the longest protected sequence, keeping the generic conjunction splitter from breaking those `and`-bearing openers apart, and keeping the generic prefixed-topic extractor from misreading `on and off ...` as a standalone `on <topic>` label, so bare fragments like `From time to time rollout happens in stages.` / `Every so often rollout happens in stages.` / `More often than not rollout happens in stages.` / `On and off rollout happens in stages.` / `Over and over again rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Every so often rollout happens in stages, use feature flags.` / `Every now and again rollout happens in stages, use feature flags.` / `On and off rollout happens in stages, use feature flags.` / `Over and over rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority like `From time to time we`, `Every so often we`, `More often than not we`, `Again we`, `Forth we`, `Off we`, `On we`, `Over we`, or `Over again we`
- prepositional/adverbial time-opener semantics that extend the same shared temporal subordinate substrate across `at times`, `in the meantime`, `for now`, `for the time being`, `on occasion`, `on rare occasions`, `sooner or later`, `at one point`, `at some point`, and `at another point`, while also rejecting bare leading `now` as fake topic authority after prefix stripping, so bare fragments like `At times rollout happens in stages.` / `For the time being rollout happens in stages.` / `On occasion rollout happens in stages.` / `At some point rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `For now rollout happens in stages, use feature flags.` / `On rare occasions rollout happens in stages, use feature flags.` / `At another point rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority like `At times we`, `In the meantime we`, `For now we`, `Sooner or later we`, or `At some point we`
- temporal/adverbial phrase opener semantics that extend the same shared temporal subordinate substrate across `at intervals`, `at regular intervals`, `at irregular intervals`, `from time immemorial`, `in due course`, `before long`, `for a while`, `after a while`, `for quite a while`, `once in a blue moon`, `every blue moon`, plus the wider non-clausal temporal phrase ring `at first`, `at last`, `at present`, `at length`, `in time`, `in no time`, `all at once`, `all along`, `all this time`, `all the while`, `all of a sudden`, `in the end`, `in the long run`, `in short order`, `before too long`, `after a time`, `after some time`, `for a time`, `for some time`, `by then`, `until then`, `since then`, `ever since`, `to date`, `to this day`, `from the beginning`, `from the outset`, `at the outset`, `in the beginning`, `from day one`, `day after day`, `week after week`, `month after month`, `year after year`, `night after night`, `day by day`, `step by step`, `little by little`, `in the near term`, `in the short term`, `over time`, and `with time`, plus the adjacent deictic point/stage/future-horizon family `at this point`, `at that point`, `at this stage`, `at that stage`, `at some stage`, `at an early stage`, `in the early stages`, `in the later stages`, `from that point on`, `from this point on`, `from then on`, `from now on`, `for the foreseeable future`, `for the near future`, `in the near future`, `in the foreseeable future`, `in the medium term`, `in the long term`, and `in the short run`, plus the duration-unit temporal phrase family `for years`, `for months`, `for weeks`, `for days`, `for ages`, `for decades`, `for centuries`, `over the years`, `over the months`, `over the long term`, `in recent years`, `in recent months`, `in recent weeks`, and `in recent days`, plus the cadence/notice temporal phrase family `on a regular basis`, `on an irregular basis`, `on a daily basis`, `on a weekly basis`, `on a monthly basis`, `on a yearly basis`, `at regular times`, `at irregular times`, `on short notice`, `at short notice`, `in the fullness of time`, `for the first little while`, and `for the next little while`, plus the single-word temporal/frequency/discourse adverbials `afterward`, `afterwards`, `beforehand`, `concurrently`, `conventionally`, `currently`, `earlier`, `finally`, `formerly`, `gradually`, `henceforth`, `henceforward`, `hereafter`, `hereupon`, `historically`, `immediately`, `initially`, `instantly`, `later`, `lately`, `meanwhile`, `momentarily`, `nowadays`, `normally`, `occasionally`, `ordinarily`, `originally`, `permanently`, `presently`, `previously`, `promptly`, `recently`, `regularly`, `shortly`, `simultaneously`, `sometimes`, `soon`, `subsequently`, `temporarily`, `thereafter`, `thereupon`, `traditionally`, `typically`, `ultimately`, `usually`, and `eventually`, while also teaching the generic time-opener matcher to accept bare `some time`, teaching the conjunction splitter to protect leading subordinate sequences containing `then`, and teaching the trailing `for <summary>` extractor to skip texts that already start with a recognized subordinate/time-opener sequence, so bare fragments like `At intervals rollout happens in stages.` / `From time immemorial rollout happens in stages.` / `At first rollout happens in stages.` / `All along rollout happens in stages.` / `Day after day rollout happens in stages.` / `At this point rollout happens in stages.` / `Over the years rollout happens in stages.` / `On a regular basis rollout happens in stages.` / `Afterward rollout happens in stages.` / `Soon rollout happens in stages.` / `Nowadays rollout happens in stages.` / `Temporarily rollout happens in stages.` / `Usually rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Before long rollout happens in stages, use feature flags.` / `For some time rollout happens in stages, use feature flags.` / `By then rollout happens in stages, use feature flags.` / `From then on rollout happens in stages, use feature flags.` / `For years rollout happens in stages, use feature flags.` / `On short notice rollout happens in stages, use feature flags.` / `Meanwhile rollout happens in stages, use feature flags.` / `Simultaneously rollout happens in stages, use feature flags.` / `Ultimately rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `At regular intervals we`, `In due course we`, `After a while we`, `Every blue moon we`, `All this time we`, `From day one we`, `Step by step we`, `At this stage we`, `Years we`, `In recent years we`, `On a regular basis we`, `Currently we`, `Nowadays we`, `Regularly we`, `Usually we`, or `Thereafter we`
- single-word discourse/result adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `accordingly`, `consequently`, `therefore`, `thus`, `hence`, `thereby`, `hereby`, `instead`, `otherwise`, `likewise`, `similarly`, `conversely`, `alternatively`, `incidentally`, `explicitly`, `implicitly`, `practically`, `theoretically`, `collectively`, and `individually`, so bare fragments like `Accordingly rollout happens in stages.` / `Instead rollout happens in stages.` / `Collectively rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Consequently rollout happens in stages, use feature flags.` / `Alternatively rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Therefore we`, `Otherwise we`, `Similarly we`, `Alternatively we`, or `Explicitly we`
- single-word stance/evaluative adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `admittedly`, `apparently`, `arguably`, `broadly`, `certainly`, `conceivably`, `crucially`, `decidedly`, `definitely`, `evidently`, `fundamentally`, `generally`, `ideally`, `importantly`, `inevitably`, `manifestly`, `naturally`, `notably`, `officially`, `ostensibly`, `possibly`, `potentially`, `presumably`, `principally`, `realistically`, `remarkably`, `seemingly`, `specifically`, `surely`, `technically`, and `undoubtedly`, so bare fragments like `Admittedly rollout happens in stages.` / `Ideally rollout happens in stages.` / `Technically rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Apparently rollout happens in stages, use feature flags.` / `Technically rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Admittedly we`, `Importantly we`, `Fundamentally we`, `Generally we`, `Specifically we`, or `Technically we`
- single-word framing/approximation adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `basically`, `essentially`, `effectively`, `merely`, `simply`, `chiefly`, `mainly`, `primarily`, `predominantly`, `largely`, `partly`, `partially`, `nominally`, `virtually`, `approximately`, `roughly`, `loosely`, `strictly`, `narrowly`, `formally`, `informally`, `tentatively`, and `provisionally`, so bare fragments like `Basically rollout happens in stages.` / `Approximately rollout happens in stages.` / `Provisionally rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Simply rollout happens in stages, use feature flags.` / `Provisionally rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Basically we`, `Mainly we`, `Approximately we`, `Loosely we`, `Strictly we`, or `Provisionally we`
- single-word manner/comment adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `automatically`, `briefly`, `carefully`, `curiously`, `delicately`, `deliberately`, `fortunately`, `happily`, `interestingly`, `intentionally`, `mechanically`, `oddly`, `predictably`, `purposely`, `sadly`, `strangely`, `unexpectedly`, `unfortunately`, `unusually`, and `wisely`, so bare fragments like `Briefly rollout happens in stages.` / `Fortunately rollout happens in stages.` / `Predictably rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Briefly rollout happens in stages, use feature flags.` / `Unexpectedly rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Briefly we`, `Fortunately we`, `Interestingly we`, `Unexpectedly we`, or `Predictably we`
- single-word disclosure/manner/frequency adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `abruptly`, `clearly`, `commonly`, `confidentially`, `customarily`, `directly`, `frankly`, `frequently`, `honestly`, `indirectly`, `loudly`, `openly`, `personally`, `plainly`, `privately`, `publicly`, `quietly`, `rarely`, `readily`, `respectfully`, `routinely`, `secretly`, `seriously`, `sharply`, `slowly`, `suddenly`, `swiftly`, and `sympathetically`, so bare fragments like `Frankly rollout happens in stages.` / `Clearly rollout happens in stages.` / `Rarely rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Frankly rollout happens in stages, use feature flags.` / `Suddenly rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Frankly we`, `Personally we`, `Quietly we`, `Rarely we`, or `Confidentially we`
- single-word report/scope/domain adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `academically`, `administratively`, `allegedly`, `culturally`, `economically`, `financially`, `institutionally`, `legally`, `maybe`, `morally`, `notionally`, `operationally`, `organizationally`, `perhaps`, `politically`, `procedurally`, `professionally`, `reportedly`, `scientifically`, `socially`, `strategically`, `supposedly`, `tactically`, and `visibly`, so bare fragments like `Allegedly rollout happens in stages.` / `Legally rollout happens in stages.` / `Perhaps rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Reportedly rollout happens in stages, use feature flags.` / `Strategically rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Allegedly we`, `Legally we`, `Professionally we`, `Strategically we`, or `Perhaps we`
- single-word aspect/scope/discipline adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `clinically`, `commercially`, `conceptually`, `constitutionally`, `contractually`, `domestically`, `ethically`, `externally`, `functionally`, `globally`, `internationally`, `internally`, `juridically`, `locally`, `logistically`, `managerially`, `medically`, `methodologically`, `numerically`, `physically`, `psychologically`, `qualitatively`, `quantitatively`, `regionally`, `structurally`, `thematically`, `verbally`, and `visually`, so bare fragments like `Conceptually rollout happens in stages.` / `Ethically rollout happens in stages.` / `Structurally rollout happens in stages.` / `Numerically rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Methodologically rollout happens in stages, use feature flags.` / `Globally rollout happens in stages, use feature flags.` / `Numerically rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Conceptually we`, `Ethically we`, `Structurally we`, `Methodologically we`, `Globally we`, or `Numerically we`
- single-word analytic/symbolic/disciplinary adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `aesthetically`, `analytically`, `biologically`, `chronologically`, `demographically`, `ecologically`, `environmentally`, `experimentally`, `geographically`, `geologically`, `grammatically`, `ideologically`, `industrially`, `linguistically`, `mathematically`, `metaphorically`, `musically`, `narratively`, `philosophically`, `pragmatically`, `rhetorically`, `semantically`, `sociologically`, `statistically`, `symbolically`, `technologically`, `textually`, `topologically`, and `viscerally`, so bare fragments like `Analytically rollout happens in stages.` / `Biologically rollout happens in stages.` / `Statistically rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Metaphorically rollout happens in stages, use feature flags.` / `Technologically rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Analytically we`, `Biologically we`, `Statistically we`, `Technologically we`, or `Semantically we`
- single-word methodological/interpretive/normative adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `algorithmically`, `architecturally`, `canonically`, `civically`, `comparatively`, `computationally`, `contextually`, `critically`, `cynically`, `descriptively`, `diagnostically`, `dramatically`, `empirically`, `hermeneutically`, `iconically`, `judicially`, `literally`, `literarily`, `mechanistically`, `normatively`, `objectively`, `pedagogically`, `politely`, `prescriptively`, `probabilistically`, `speculatively`, `stylistically`, `systematically`, and `typologically`, so bare fragments like `Algorithmically rollout happens in stages.` / `Empirically rollout happens in stages.` / `Objectively rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Computationally rollout happens in stages, use feature flags.` / `Systematically rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Algorithmically we`, `Empirically we`, `Objectively we`, `Computationally we`, or `Systematically we`
- single-word abstract/concrete/interpretive/precision adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `abstractly`, `concretely`, `exactly`, `figuratively`, `intuitively`, and `precisely`, so bare fragments like `Abstractly rollout happens in stages.` / `Figuratively rollout happens in stages.` / `Precisely rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Concretely rollout happens in stages, use feature flags.` / `Intuitively rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Abstractly we`, `Concretely we`, `Figuratively we`, `Intuitively we`, or `Precisely we`
- single-word counterexpectation/surprise/irony adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `amazingly`, `coincidentally`, `counterintuitively`, `ironically`, `paradoxically`, `shockingly`, `surprisingly`, and `unsurprisingly`, so bare fragments like `Counterintuitively rollout happens in stages.` / `Ironically rollout happens in stages.` / `Surprisingly rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Coincidentally rollout happens in stages, use feature flags.` / `Unsurprisingly rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Amazingly we`, `Coincidentally we`, `Counterintuitively we`, `Ironically we`, `Surprisingly we`, or `Unsurprisingly we`
- single-word extended comment/evaluative adverbial opener semantics that extend the same shared leading adverbial/subordinate substrate across `alarmingly`, `astonishingly`, `bizarrely`, `conveniently`, `expectedly`, `famously`, `incredibly`, `inexplicably`, `mercifully`, `mysteriously`, `notoriously`, `puzzlingly`, `regrettably`, `thankfully`, `tragically`, `understandably`, `unbelievably`, and `wonderfully`, so bare fragments like `Alarmingly rollout happens in stages.` / `Famously rollout happens in stages.` / `Inexplicably rollout happens in stages.` / `Thankfully rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Conveniently rollout happens in stages, use feature flags.` / `Understandably rollout happens in stages, use feature flags.` / `Wonderfully rollout happens in stages, use feature flags.` still materialize normally, and `topic_sentences` no longer fabricates fake topic authority or fake durable summaries like `Alarmingly we`, `Famously we`, `Incredibly we`, `Mysteriously we`, `Regrettably we`, `Understandably we`, or `Wonderfully we`
- pre-article and article-stripped quantified time authority semantics that extend the same shared quantified-time substrate across pre-article prefixes like `quite a few times`, `just a few times`, and `only a few times`, while also treating article-stripped modifier stacks like `very large number of times` as the same subordinate-opener family and preserving `an awful lot of times` / `a whole bunch of times`, so bare fragments like `Quite a few times rollout happens in stages.` / `Only a few times rollout happens in stages.` / `A very large number of times rollout happens in stages.` / `An awful lot of times rollout happens in stages.` now fail closed instead of wrong-successing as durable answers, continuations like `Quite a few times we use Bun, support load stays low.` and `An extremely large number of times we use Bun, support load stays low.` no longer false-positive as explicit topic authority like `Quite a few times we`, `Only a few times we`, `Very large number of times we`, `Awful lot of times we`, or `Whole bunch of times we`, and `topic_sentences` remaining-topic inference no longer fabricates fake durable summaries like `Quite a few times we`, `Just a few times we`, `Very large number of times we`, `Extremely large number of times we`, `Awful lot of times we`, or `Whole bunch of times we`
- declarative wh-clause auxiliary-chain copula semantics that let inner clauses like `What we do is ...`, `What we have is ...`, `What we can do is ...`, `What we can't do is ...`, `What we shouldn't do is ...`, and `What we wouldn't do is ...` materialize as durable answers instead of false-positiving as hidden questions, while fragmentary forms like `What we can do now.` still fail closed because the embedded inner-predicate finder only accepts those auxiliary-like lexical predicates when a real outer predicate follows
- declarative wh-clause trailing-`for <topic>` semantics that stop inner-clause phrases like `What we do for rollout is ...`, `What we have for rollout is ...`, `What we can do for rollout depends on ...`, and `What we should do for rollout is ...` from being misread as explicit trailing topic authority, so direct `single_pending`, paragraph-backed topic-family parsing, and `auto` fallback now materialize those declaratives as durable answers while fragmentary forms like `What we can do for rollout.` still fail closed

What is still missing:

- frontend product integration inside `packages/frontend`: the preserved React/Vite baseline is now reconnected to live backend state through the board, docs, decisions, planning-requests, workflows, preferences, reconcile, runtime, assistant, and assistant-bundle APIs, and it now also exposes direct decision open/resolve, answer-first decision bundle capture through `/decisions/answer` and `/decisions/answers`, shared-answer planner context on direct planning and workflow mutations, planning-request creation, direct workflow-batch creation, workflow-key extension, visible workflow reuse, and durable preference write/record/retire flows, but it still does not surface the full current backend product capability set
- deeper answer interpretation when assistant should infer more than one pending answer or brand-new durable decision topics or planner-answer summaries directly from one less-structured reply without first relying on explicit reusable `answerSources`, explicit reusable `answerSources` matched by labels or hints, repeated-consumer `matching_runs`, embedded `matching_opening_runs` with one known anchor at the start of each answer stretch, embedded `matching_closing_runs` with one known anchor at the end of each answer stretch, embedded `matching_middle_runs` with one known anchor in the middle of each answer stretch, embedded `question_spans` with one known question anchor at the start of each answer stretch, embedded `question_spans` / `question_middle_spans` / `question_closing_spans` with one inferable canonical `What should <summary> be` anchor per answer stretch, embedded `topic_clauses` with one explicit or inferable topic-bearing conjunction segment per answer stretch, embedded `topic_spans` with one inferable or already-known opening topic anchor at the start of each answer stretch, embedded `topic_middle_spans` with one inferable or already-known middle topic anchor in the middle of each answer stretch, embedded `topic_closing_spans` with one inferable or already-known topic-closing anchor at the end of each answer stretch, exact repeated durable question text, deterministic prompt-core reuse, deterministic prompt-keyword anchors, explicit durable `matchHints`, explicit question sentences or paragraphs, explicit conjunction boundaries, explicit sentence boundaries, explicit clause boundaries, explicit paragraph boundaries, explicit topic-bearing anchor sentences or paragraphs, explicit inline topic labels, line-based labeled sections, ordered reply structure, grounded excerpts, per-topic mapping inside the action payload, or `sourceResponseFormat: "auto"` either finding one already-implemented deterministic surface that fully captures the reply or failing closed on already-established explicit label, reusable-source, or question/topic-anchor authority

Track C direction is corrected: `packages/frontend` is again the product frontend package, and its original frontend codebase has been restored as the baseline. The remaining work is to evolve that baseline against current backend capabilities instead of replacing it with the backend UI.

## Recommended Next Work

Next high-leverage phase:

1. Continue frontend work inside the restored `packages/frontend` React/Vite codebase: add the remaining product capabilities there, especially reusable-answer-source flows and other richer interpretation-backed follow-through authoring flows for the already-visible durable surfaces, without copying backend UI structure wholesale.
2. Continue deeper answer interpretation only if product needs assistant to infer more than one pending answer or brand-new durable decision topics or planner-answer summaries directly from one less-structured reply without first relying on explicit reusable `answerSources`, explicit reusable `answerSources` matched by labels or hints, repeated-consumer `matching_runs`, embedded `matching_opening_runs` with one known anchor at the start of each answer stretch, embedded `matching_closing_runs` with one known anchor at the end of each answer stretch, embedded `matching_middle_runs` with one known anchor in the middle of each answer stretch, embedded `question_spans` with one known question anchor at the start of each answer stretch, embedded `question_spans` / `question_middle_spans` / `question_closing_spans` with one inferable canonical `What should <summary> be` anchor per answer stretch, embedded `topic_clauses` with one explicit or inferable topic-bearing conjunction segment per answer stretch, embedded `topic_spans` with one inferable or already-known opening topic anchor at the start of each answer stretch, embedded `topic_middle_spans` with one inferable or already-known middle topic anchor in the middle of each answer stretch, embedded `topic_closing_spans` with one inferable or already-known topic-closing anchor at the end of each answer stretch, exact repeated durable question text, deterministic prompt-core reuse, deterministic prompt-keyword anchors, explicit durable `matchHints`, explicit question sentences or paragraphs, explicit conjunction boundaries, explicit sentence boundaries, explicit clause boundaries, explicit paragraph boundaries, explicit topic-bearing anchor sentences or paragraphs, explicit inline topic labels, line-based labeled sections, ordered reply structure, grounded excerpts, per-topic mapping in the action payload, or `sourceResponseFormat: "auto"` either finding one already-implemented deterministic surface that fully captures the reply or failing closed on already-established explicit label, reusable-source, or question/topic-anchor authority.

Keep this out of the next phase unless explicitly requested:

- A complex queue service.
- A database.
- Compatibility with deleted prototype schema fields.
- Preserved-frontend Track C is still active; the latest slices front-loaded the exact `sourceResponseFormat: "labeled_sections"` seams plus the narrow explicit-consumer and infer-topic/open-decision `inline_topics` seams, so the frontend now blocks infer-topic `requires at least one labeled section` / `Multiple existing decisions match inferred label ...`, infer-open-decision `requires at least one labeled section to match an open decision ...`, duplicate labeled-section rejection, nested labeled-section-in-value rejection, exact `left N unconsumed labeled sections` rejection, explicit-consumer `No labeled section matched ... in sourceResponse.` rejection, explicit-consumer `inline_topics` duplicate/no-match/unconsumed rejection, and batch `inline_topics` infer-topic/open-decision `requires at least one inline topic clause` / `requires at least one inline topic clause to match an open decision` / `Multiple existing decisions match inferred label ...` rejection across the preserved shared-answer surfaces before materialization.
- Preserved-frontend Track C is still active; the newest shared-answer slice also front-loads exact `inline_topics` standalone-authority rejection across direct resolve, single/batch `Answer Bundles`, `Planning Requests`, and `Workflow Graphs`, so preserved frontend now blocks runtime-only `still included standalone question authority ... outside inline topic clauses` / `still included standalone topic authority for ... outside inline topic clauses` failures on explicit-consumer paths and the batch infer-topic/open-decision path before materialization.
- Preserved-frontend Track C is still active; the latest follow-on slice also front-loads the exact `sourceResponseFormat: "auto"` inline-topic mixed-authority seam, so those same preserved shared-answer surfaces now block `mixed separator-style inline topic authority with additional verbal inline topic authority ...` before runtime falls through to an inline-topic auto-probe failure.
- Preserved-frontend Track C is still active; the latest follow-on slice now also front-loads exact `labeled_sections` standalone-authority rejection across direct resolve, single/batch `Answer Bundles`, `Planning Requests`, and `Workflow Graphs`, so preserved frontend blocks runtime-only `still included standalone question authority ... outside labeled sections` / `still included standalone topic authority for ... outside labeled sections` failures before labeled-section materialization.
- Preserved-frontend Track C is still active; the latest follow-on slice now also front-loads exact `ordered_items` / `ordered_blocks` leftover-consumer rejection plus the `ordered_blocks` collapsed-multi-item-line structure error across direct resolve, single/batch `Answer Bundles`, `Planning Requests`, and `Workflow Graphs`, so preserved frontend blocks runtime-only `left N unconsumed ordered items/blocks` and `collapsed multiple ordered item lines into one ordered block` failures before ordered materialization.
- Preserved-frontend Track C is still active; the latest follow-on slice now also front-loads the narrow `question_blocks` structure and leftover-consumer seam across direct resolve, single/batch `Answer Bundles`, `Planning Requests`, and `Workflow Graphs`, so preserved frontend blocks runtime-only `sourceResponseFormat question_blocks requires sourceResponse to start with a question block.`, `Question block "..." in sourceResponse did not include an answer block.`, and `left N unconsumed question blocks` failures before question-block materialization.

## Handoff Checklist

Before handing off again:

- Run `bun run check` from the repo root.
- Confirm `git status --short` is clean or explain every remaining change.
- Update this document if the current state, commands, or next work changed.
- Commit documentation updates after verification.
