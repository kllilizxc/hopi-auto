{
  "name": "hopi-agent-orchestrator",
  "version": "0.1.0",
  "module": "src/mvpServer.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/mvpServer.ts",
    "dev": "bun --watch src/mvpServer.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "check": "bun run typecheck && bun run lint && bun test",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "bun-plugin-tailwind": "0.1.2",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  }
}
