# HOPI Frontend

Status: legacy prototype.

This package still uses Vite from the original scaffold. It is not part of the Phase 1 verification gate and should not be treated as the target architecture.

Before substantial frontend work, read:

- `docs/agent-handoff.md`
- `docs/hopi-phase-1-authority.md`

The next frontend pass should be Bun-first unless the project owner explicitly chooses otherwise. The confirmed product need is step/run inspection: users should be able to switch between workflow steps and see the message history for the selected step.

Useful current command:

```sh
bun run dev:frontend
```
